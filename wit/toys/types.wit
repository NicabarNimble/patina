package patina:host@0.1.0;

interface types {
    enum health-status {
        healthy,
        degraded,
        unhealthy,
    }

    record child-health {
        status: health-status,
        reason: option<string>,
    }

    record toy {
        name: string,
        command: string,
        args: list<string>,
    }
}
