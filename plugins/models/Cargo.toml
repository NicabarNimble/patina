[package]
name = "patina-ai-extension-models"
version = "0.1.0"
edition = "2021"
description = "Embedding model path resolution plugin for Mother daemon"

[lib]
crate-type = ["cdylib"]

[dependencies]
patina-sdk = { path = "../../sdk/patina-sdk", features = ["mother-child"] }
serde_json = "1.0"
