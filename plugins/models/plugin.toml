[plugin]
name = "patina-models"
version = "0.1.0"
description = "Embedding model path resolution for Mother daemon"
world = "mother-child"
role = "extension"
patina_min = "0.17.0"

[capabilities]
host_log = true

[provides]
child = "models"
