[plugin]
name = "patina-doctor"
version = "0.1.0"
description = "Check project health and environment"
world = "command"
role = "extension"
patina_min = "0.17.0"

[capabilities]
host_log = true
host_layer = true
host_query = ["context"]
host_measure = true

[provides]
commands = ["doctor"]
