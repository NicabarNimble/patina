[package]
name = "patina-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Patina typed protocol contracts"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
