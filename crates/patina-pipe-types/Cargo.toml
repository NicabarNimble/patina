[package]
name = "patina-ai-pipe-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared type definitions for Patina pipe protocol — Fact, PipeError, Capabilities, canonical JSON"
repository = "https://github.com/NicabarNimble/patina"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
blake3 = "1"
toml = "0.8"
