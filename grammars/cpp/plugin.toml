[plugin]
name = "grammar-cpp"
version = "0.1.0"
description = "C++ language grammar extraction via tree-sitter (compiled to WASM via wasi-sdk)"
world = "pipeline"
patina_min = "0.22.0"

[provides]
pipeline_ops = ["parse"]
languages = ["cpp", "hpp", "cc", "cxx"]
