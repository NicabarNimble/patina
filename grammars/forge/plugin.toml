[plugin]
name = "grammar-forge"
version = "0.1.0"
description = "Forge staging file extraction — reads staged JSON, returns ExtractedPayload"
world = "pipeline"
patina_min = "0.25.0"

[provides]
pipeline_ops = ["parse"]
languages = ["forge-issue", "forge-pr"]
