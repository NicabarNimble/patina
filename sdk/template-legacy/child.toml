[child]
name = "{{ package_name }}"
version = "0.1.0"
description = "{{ description }}"
kind = "child"
role = "app"
patina_min = "0.41.0"

[child.artifact]
wasm = ".patina/dev/components/{{ child_name }}.wasm"

[needs]
toys = ["log"]

[provides]
child = "{{ child_name }}"

[relationships]
emits = []
listens = []
