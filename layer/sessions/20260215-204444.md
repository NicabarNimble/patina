---
type: session
id: 20260215-204444
title: 'Explore: Workspace Structure — Root Sprawl'
status: archived
llm: claude
created: 2026-02-16T01:44:45Z
start_timestamp: 1771206285042
git:
  branch: patina
  starting_commit: aceee36a96ddfcdc26bed32f7b8303606809681f
  start_tag: session-20260215-204444-claude-start
---

## Previous Session Context
Archived 21 non-active specs with git tags, then wrote three new belief-focused specs grounded in deep code reading: cross-project-beliefs (federated search via graph.db), belief-truthfulness (staleness detection + health scoring), and git-tag-system (tag classification + knowledge diff). Pushed 173 backlogged commits, created and merged PR #92 to main. Also fixed a `[1m]` long-context suffix in settings that was breaking background Explore agents.

## Goals
- [x] Read core values: dependable-rust, unix-philosophy, patina-identity
- [x] Read paths.rs — check for root-level dir references
- [x] Read root Cargo.toml — workspace member analysis
- [x] Read all 9 grammar-*/Cargo.toml — standalone analysis
- [x] Read all 9 patina-*/Cargo.toml — dependency analysis
- [x] Check pre-push-checks.sh for hardcoded paths
- [x] Check CI workflows for hardcoded paths
- [x] Check src/ for references to root-level dirs
- [x] Evaluate candidate directory structures
- [x] Discuss recommendation with user
- [x] Create belief: [[root-communicates-identity]]
- [x] Create spec: [[workspace-cleanup]] (fix, status: ready)
- [x] 6 rounds of spec review and tightening
- [ ] 2 remaining gaps identified for next session

## Activity Log
### 20:44 - Session Start
Session initialized with goal: Explore: Workspace Structure — Root Sprawl
Working on branch: patina
Tagged as: session-20260215-204444-claude-start

### 20:45 - Deep Code Reading (parallel)
Read all 26 files simultaneously across 3 Explore agents:
- Agent 1: All 9 grammar-*/Cargo.toml + build configs
- Agent 2: All 9 patina-*/Cargo.toml + dependency analysis
- Agent 3: CI workflows, scripts, .gitignore for hardcoded paths

Also read: paths.rs, root Cargo.toml, pre-push-checks.sh, grammars.rs, bench/grammar.rs,
3 test fixture Cargo.toml files, dependable-rust.md, unix-philosophy.md, patina-identity.md.

### 20:50 - Key Findings

**paths.rs**: CLEAN — zero references to root-level crate dirs.

**Grammar crates (9)**: Completely standalone. Own Cargo.lock, wasm32-wasip2 target.
Depend on patina-sdk from crates.io (NOT path deps). Safe to move without workspace impact.

**Workspace crates (8)**: Entangled. 3 active plugins have `path = "../patina-sdk"`.
4 deprecated crates (superseded by patina-sdk) kept alive for WIT distribution + test fixtures.

**patina-metal**: Dead. Empty scaffold. Not referenced by any source code.

**Hardcoded paths**: grammars.rs find_source_root() heuristic, pre-push-checks.sh WIT checks,
grammar-compare.sh, 3 test fixture relative paths, .gitignore.

**CI**: Clean. Uses workspace-level cargo commands, no hardcoded crate paths.

### 20:55 - Recommendation: Phased Approach

**Phase 1** (high impact, low risk): Move 9 grammar-* into grammars/ — root goes 26→18.
Only code change: find_source_root() in grammars.rs + grammar-compare.sh.

**Phase 2** (medium risk): Move 3 active plugin crates + sdk into plugins/.
Requires updating path deps, workspace members, WIT hard links.

**Phase 3**: Delete 4 deprecated API crates (migrate test fixtures to patina-sdk first).

**Phase 4**: Delete patina-metal (dead directory).

**End state**: 10 clean root dirs. src/, layer/, wit/, grammars/, plugins/, resources/,
tests/, scripts/, examples/, target/. Communicates what patina IS.

### 21:00 - Belief Created
Created [[root-communicates-identity]] in `layer/surface/epistemic/beliefs/`.
Statement: root should communicate what Patina IS — not list every grammar plugin.
5 evidence links anchored in [[patina-identity]], [[dependable-rust]], [[unix-philosophy]].
Supports 3 core beliefs. Attacks/attacked-by documented.

### 21:05 - Spec Created: [[workspace-cleanup]]
Created `layer/surface/build/fix/workspace-cleanup/SPEC.md` (status: ready).
4 sequential phases (A→D), each leaving repo green. Indexed by `patina scrape`.

### 21:10–22:30 - Six Rounds of Spec Review

**Round 1** — 5 gaps identified by user:
- Phase A: grep coverage incomplete (added full reference table + rg gate)
- Phase B: publishing/release risk (verified clean — package name, not path)
- Phase C: SDK parity unverified (confirmed zero API gaps across all 4 deprecated crates)
- Phase C: WIT distribution unclear post-deletion (documented canonical `wit/` stays)
- Phase D: launch.json fate (confirmed generic Node.js config, safe to delete)

**Round 2** — 5 more gaps:
- rg gate excluded moved dirs (removed `--glob '!grammars/**'` and `--glob '!plugins/**'`)
- Phase B/C pre-push ordering unclear (spelled out concrete script edits per phase)
- "Independently valuable" contradicted Phase C→B dependency (changed to explicit A→B→C→D ordering)
- Phase A only verified grammar-rust (added grammar-cairo — the only non-tree-sitter grammar)
- SDK WIT drift guard missing post-Phase C (added Step 1b diff loop for 8 publishable WIT files)

**Round 3** — 3 gaps:
- Empty `for crate_dir in; do` is invalid bash (restructured: Phase B only updates moved crates, Phase C handles all deprecated removal)
- Conflicting Phase B/C WIT instructions (clean separation: B updates paths, C removes + adds SDK guard)
- Path rewrite gate just dumped lines (replaced with concrete fail-on-match `rg '../patina-sdk'`)
- SDK WIT guard skipped deps/host.wit (expanded to diff both world.wit AND host.wit)
- Phase B removed checks for still-existing crates (kept deprecated crate checks alive until Phase C deletion)

**Round 4** — 3 gaps:
- `git mv` fails without `mkdir -p` (added `mkdir -p grammars` and `mkdir -p plugins`)
- False positives: `rg 'grammar-'` matches crate names (restored `--glob '!grammars/**'` for scan 1, kept scan 2 for path deps)
- Phase A verification skipped build/test (added full `cargo build --release && cargo test --workspace` to every phase)

**Round 5** — 3 gaps:
- Stale reference scan can never pass (dropped generic scan entirely — crate names indistinguishable from dir paths in Cargo.lock/Cargo.toml)
- SDK WIT guard lacked failure hook (specified insertion point: before existing `if [ "$wit_ok" = false ]` guard)
- Phase A didn't exercise `patina setup grammars` (added `cargo run --release -- setup grammars --list`)

**Round 6** — 2 gaps:
- Shared gate fails before Phase B (`plugins/` doesn't exist, `tests/` has legitimate old paths) — made all path dep checks phase-specific
- `grammars.rs` doc comments (lines 3-4) still say `grammar-*/` — added to Phase A code changes

**Round 7** — 3 gaps:
- `grammar-compare.sh` PLUGIN_DIRS maps to installed `~/.patina/pipeline/` not source dirs — removed from Phase A changes, added to "What does NOT change"
- `cargo install --path .` fails without `--force` — replaced with `cargo run --release --`
- README.md structure diagram not updated — added to Phases A, B, C, D doc updates

**Round 8** — 2 gaps:
- README/CLAUDE stale after Phases C/D — added doc updates to Phase C Step 3 and Phase D
- `.gitattributes`, `.ignore` reference patina-metal — added cleanup to Phase D
- `resources/bench/patina-commits-v1.json` historical entry — documented as no-change (ground truth data)

### 22:30 - Two Remaining Gaps (for next session)
1. Vendored grammar flags (.gitattributes linguist-vendored, .ignore) disappear after Phase A — need replacement entries for `grammars/` locations
2. Phase D verification `rg 'patina-metal'` always fails — `grammars/cairo/src/parser.rs` line 1 references patina-metal in doc comment, plus layer/surface docs

### 22:35 - Continuation Prompt Written
Wrote self-contained prompt for next session covering both remaining gaps plus full spec context.


## Beliefs Captured: 1

## Session Classification
- Work Type: pattern-work
- Files Changed: 3
- Commits: 1
- Patterns Modified: 2
- Beliefs Captured: 1
- Session Tags: session-20260215-204444-claude-start..session-20260215-204444-claude-end

## User Prompts (12)

1. `[Pasted text #1 +73 lines]`
2. `capture this as a belief, then get this into a single fix spec ... patina spec --help`
3. `[Pasted text #1 +12 lines]`
4. `[Pasted text #1 +10 lines]`
5. `[Pasted text #1 +10 lines]`
6. `[Pasted text #1 +6 lines]`
7. `[Pasted text #1 +7 lines]`
8. `[Pasted text #1 +6 lines]`
9. `[Pasted text #1 +4 lines]`
10. `[Pasted text #1 +4 lines]`
11. `we are low on context and i want to continue this tightening of our spec next session. write a pr...`
12. `/session-end`
