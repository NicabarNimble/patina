---
type: session
id: 20260212-121824
title: PHASE 2 AUDIT
status: archived
llm: claude
created: 2026-02-12T17:18:24Z
start_timestamp: 1770916704194
git:
  branch: patina
  starting_commit: 1b5d3d0d999fa1c4c71a1b91583b5c29cbbb3b28
  start_tag: session-20260212-121824-claude-start
---

## Previous Session Context
Completed all 7 remaining Phase 2 plugin system tasks: wired CLI dispatch (WASM-first with bundled fallback), feature-gated `bundled-doctor`, built `patina plugin list` command, installed doctor WASM plugin to `~/.patina/plugins/`, wrote CommandEngine integration tests, updated pre-push WIT checks, and measured binary size delta (negligible for doctor alone — real savings come with larger extractions in Phase 3). Also ran a full Phase 2 audit finding 5 minor and 2 nit issues, captured the `graceful-extraction` belief, and wrote a continuation prompt. 7 commits, 10 files changed.

**Audit findings from previous session (5 minor, 2 nit):**
1. `plugin-cache.toml` not implemented (spec says Phase 2, reality is manual copy)
2. Host functions call `find_project_root()` 10x per doctor invocation — cache in `CommandHostState`
3. CLI dispatch hardcodes `patina-doctor.wasm` filename — doesn't generalize for Phase 3
4. `src/plugin/internal.rs` at 1,352 lines — approaching split threshold
5. `CommandEngine` doesn't check capabilities internally (asymmetry with `PluginEngine`)
6. No unit test for `patina plugin list`
7. Mother-child guest crates carry unused command WIT

## Goals
- [ ] PHASE 2 AUDIT — address findings from previous session's audit

## Activity Log
### 12:18 - Session Start
Session initialized with goal: PHASE 2 AUDIT
Working on branch: patina
Tagged as: session-20260212-121824-claude-start


### 12:36 - Update (covering since 12:18)

**Git Activity:**
- Commits this session: 1
- Files changed: 0
- Last commit: 38 seconds ago

**Work completed:**
- **Full independent Phase 2 audit**: Read all three anchor docs ([[dependable-rust]], [[spec-driven-design]], [[patina-identity]]) and Phase 1 final audit before examining any Phase 2 code. Then independently read every file touched by Phase 2 (28 files): `src/plugin/internal.rs` (1,356 lines), `src/plugin/mod.rs`, `src/commands/plugin.rs`, `src/commands/doctor.rs`, `src/commands/mod.rs`, `src/main.rs` (Doctor dispatch), `wit/command/command.wit`, `wit/mother-child/mother-child.wit`, `wit/command/deps/patina-host/host.wit`, `patina-command-api/src/lib.rs`, `patina-doctor/src/lib.rs`, `patina-doctor/plugin.toml`, `patina-doctor/Cargo.toml`, `patina-command-api/Cargo.toml`, `patina-plugin-api/src/lib.rs`, `resources/git/pre-push-checks.sh`, `Cargo.toml`. Also checked WIT inode structure across all guest crates and ran `cargo test --workspace`.
- **Verified all 7 prior findings**: Every finding from the [[20260212-113744]] session audit was independently confirmed against actual source code. No false positives.
- **Discovered 1 additional finding**: Benchmark test `benchmark_plugin_performance` is flaky due to OnceLock cold-start — `PluginEngine::new()` measures 152ms on first invocation in process (engine singleton init), 1ms on subsequent. This causes `cargo test --workspace` to fail intermittently.
- **[[7d028107]] Audit report written**: `layer/surface/reports/audit/2026-02-12-plugin-system-phase2.md` (578 lines). Follows Phase 1 audit format. Final tally: 0 critical, 0 important, 6 minor, 3 nit. Also archived previous session file.

**Key decisions:**
- Elevated the benchmark flakiness from "not a finding" to minor — it gates CI (`cargo test --workspace` fails when benchmark runs first). Three fix options documented: warm-up call, increase threshold, or separate engine init measurement.
- Kept finding 2.2 (doctor reimplementation drift) as minor rather than important — both implementations are tested and produce matching output. Risk is maintenance burden, not correctness.

**Patterns observed:**
- Per [[spec-driven-design]], audit format matches Phase 1 audit exactly — frontmatter, numbered sections (spec compliance, correctness, architecture, safety, completeness), severity table, verdict. Consistent audit structure makes cross-phase comparison trivial.
- Hard-link strategy for WIT files works well: `host.wit` has refcount 4 across all locations. `command.wit` has refcount 2. Pre-push check catches drift regardless of link strategy.


### 12:44 - Final Update (covering since 12:36)

**Git Activity:**
- Commits this session: 1
- Files changed: 1
- Last commit: 9 minutes ago

**Work completed:**
- **Discussed fix approach for all 6 minor + 3 nit findings**: Aligned on F4 (option A — add manifest param to `run_command()`), F5 (corrected: `OnceCell<RefCell<...>>` won't compile because `RefCell` not `Sync` — use `WasmCell<UnsafeCell<...>>` pattern per [[restructure-over-unsafe]]), F1 (single `tests.rs` file for shared fixtures).
- **[[plugin-system-phase2-audit-fixes]] fix spec written**: `layer/surface/build/fix/plugin-system-phase2-audit-fixes/SPEC.md`. 6 code fixes + 1 spec amendment. Build order: F6 (spec text) → F1 (split internal.rs) → F2 (benchmark warm-up) → F3 (cache project root) → F4 (capability check) → F5 (WasmCell migration). Follows same format as [[plugin-system-final-audit-fixes]].

**Key decisions:**
- F5 design: `OnceCell<RefCell<Box<dyn T>>>` rejected — `RefCell` is not `Sync`, compiler requires `Sync` for `static` items even on WASM targets. Correct pattern is `UnsafeCell` wrapper with explicit `unsafe impl Sync` and safety comment citing WASM single-threaded model. Eliminates `static mut` (edition 2024 target) while keeping `unsafe` scoped and documented.
- F4 design: Chose option A (manifest param on `run_command()`) over option B (separate `run_command_checked()`). Consistency with `PluginEngine::instantiate_child()` outweighs test convenience. Tests already construct manifests.
- Build order rationale: F5 last because it requires WASM cross-compile (`wasm32-wasip2` fixture rebuild). F4 after F3 because both touch `CommandEngine`.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 3
- Commits: 2
- Patterns Modified: 2
- Beliefs Captured: 0
- Session Tags: session-20260212-121824-claude-start..session-20260212-121824-claude-end

## User Prompts (7)

1. `[Pasted text #1 +90 lines] Verify these initial audit finding and expose any additional findings ...`
2. `commit this`
3. `/session-update`
4. `i want to address these and fix or do them before we leave phase 2 and finish v17. they should be...`
5. `looks good .. but lets make sure you and i are on same page with these: [Pasted text #2 +15 lines...`
6. `yes write the spec`
7. `/session-end `
