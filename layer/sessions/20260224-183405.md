---
type: session
id: 20260224-183405
title: 'Session Goal: Implement spec-mutation-cleanup (Steps 1 + 2)'
status: archived
llm: claude
created: 2026-02-24T23:34:06Z
start_timestamp: 1771976046021
git:
  branch: patina
  starting_commit: 170ee33f95c50eb22c23d46b05f7a6d623b6a625
  start_tag: session-20260224-183405-claude-start
---

## Previous Session Context
Completed [[spec-module-split]] lifecycle (promoted → active → complete, released v0.30.1). Drafted [[spec-mutation-cleanup]] through 3 iterations of Gjengset-perspective review — collapsed 7 fixes into 3 refactors + mechanical cleanup, addressed pitfalls around two-tier find/load, ID source-of-truth, rollback contracts, and payload inventory. Investigated session-spec linkage gap and confirmed it's an intentional scope-out.

## Goals
- [x] Step 1: Mechanical cleanup — remove 7 `// ====` banners + fix `archive_spec_inner` signature
- [x] Step 2: Refactor 1 — add `LoadedSpec`/`load_spec`, `mutate_spec` takes `LoadedSpec`, `MutationOutput` struct, `with_content_rollback`, update all callers

## Activity Log
### 18:34 - Session Start
Session initialized with goal: Implement [[spec-mutation-cleanup]] Steps 1 + 2
Working on branch: patina
Tagged as: session-20260224-183405-claude-start

### 18:34 - 19:15 - Implementation

**Git Activity:**
- Commits this session: 5 ([[commit-00e106c1]], [[commit-b57906de]], [[commit-eb7f0405]], [[commit-4c08392a]], [[commit-a8dacbc2]])
- Files changed: 5
- Lines: +215 / -187

**Work completed:**

1. **Step 1: Mechanical cleanup** ([[commit-00e106c1]])
   - Removed 7 `// ====` fossil banners across 5 `internal/` files (`mutations.rs`, `split.rs`, `archive.rs`, `queue.rs`, `queries.rs`)
   - Changed `archive_spec_inner` signature from `&Option<PathBuf>` to `Option<&Path>`, updated 5 call sites to `spec_dir.as_deref()`

2. **Step 2a: LoadedSpec + load_spec** ([[commit-b57906de]])
   - Added `LoadedSpec` struct to `archive.rs` — holds file_path, status, title, content, frontmatter, body
   - Added `load_spec(id)` — calls `find_spec` + `read_to_string` + `parse_spec_file` in one shot
   - ID source-of-truth assertion: bails if `frontmatter.id != lookup_key`
   - `find_spec` remains unchanged for read-only callers

3. **Step 2b+2c+2d: Core refactor** ([[commit-eb7f0405]])
   - `mutate_spec` signature changed: takes `LoadedSpec`, returns `MutationOutput { file_path, pre, post }`
   - `load_and_mutate(id, closure)` convenience wrapper for `promote_spec_value`
   - `with_yaml_rollback` renamed to `with_content_rollback` — takes explicit backup content string instead of re-reading file
   - All 7 `_value()` callers migrated:
     - `promote_spec_value`: uses `load_and_mutate`
     - `complete_spec_value` / `abandon_spec_value`: `load_spec` + validate + `mutate_spec`
     - `pause_spec_value` / `block_spec_value`: `load_spec` + `with_content_rollback` + `mutate_spec`
     - `resume_spec_value`: uses `loaded.frontmatter` for blocker check + `paused_at_tag` (kills triple-read)
     - `split_spec_value`: `mutate_spec` replaces manual read-parse-mutate-write-DB
   - Removed unused `parse_spec_file` import from `mutations.rs`
   - `split.rs` dropped `rusqlite`, `serialize_spec_file`, `parse_spec_file`, `find_spec`, `DB_PATH` imports

4. **Formatting + clippy** ([[commit-4c08392a]], [[commit-a8dacbc2]])
   - `cargo fmt` for long lines
   - `#[allow(dead_code)]` on `MutationOutput.pre` — consumed by Step 4 (typed results)

5. **Verification:**
   - All tests pass (245 unit + 6 doc + 2 integration)
   - `cargo clippy` clean
   - Pre-push checks green
   - Manual smoke test: promote (draft→ready→active) → pause → resume → complete — all work

**Key decisions:**
- Steps 2b+2c+2d committed together as one logical unit — they're mechanically entangled (can't change `mutate_spec` signature without updating all callers, can't rename `with_yaml_rollback` without updating pause/block)
- `MutationOutput.pre` marked `#[allow(dead_code)]` rather than removed — it's structurally correct and will be consumed by Step 4
- `split_spec_value` now uses `mutate_spec` instead of manual DB update — the "original sin" that started the whole spec

**What remains for [[spec-mutation-cleanup]]:**
- Step 3: Extract `release_and_archive` helper (shared logic between complete + split)
- Step 4: Typed `MutationResult` + `MutationDetail` + `SplitResult` (replace `serde_json::Value` returns)

## Beliefs Captured: 0
_No beliefs captured this session_


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: feature
- Files Changed: 5
- Commits: 5
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260224-183405-claude-start..session-20260224-183405-claude-end

## User Prompts (3)

1. `[Pasted text #1 +152 lines]`
2. `i want to continue work on spec-mutation-cleanup next session.  include what we can safely do ina...`
3. `/session-end`
