---
type: session
id: 20260208-103844
title: Semantic-Structural Split — Phase 1 (RESUME MID-FLIGHT)
status: archived
llm: claude
created: 2026-02-08T15:38:44Z
start_timestamp: 1770565124561
git:
  branch: patina
  starting_commit: f6f1f83dfc411444ca54465cbf5b4bc13cbc2685
  start_tag: session-20260208-103844-claude-start
---

## Previous Session Context
Completed the additive phase of the semantic-structural split: moved factual oracles to `assay` with 3 clean commits — `assay search` (ranked FTS5), `assay cochange` (temporal co-change), and `assay belief` (belief grounding with evidence/co-citation). Then began the reductive phase — simplifying scry's QueryEngine to semantic-only by removing factual oracle imports and the intent module. **Stopped mid-compile with 2 errors remaining:** `src/commands/eval/mod.rs:14` imports removed `intent::detect_intent`, and `src/retrieval/fusion.rs` imports removed `intent::IntentWeights`.

## Goals
- [x] Fix 2 remaining compile errors (eval/intent, fusion/intent imports)
- [x] Clean up unused oracle files and dead code
- [x] Verify all tests pass, clippy clean
- [x] Build release and test with live install
- [x] Wire MCP server for new assay query types
- [x] Document eval baseline honestly

## Activity Log
### 10:38 - Session Start
Session initialized with goal: Semantic-Structural Split — Phase 1 (RESUME MID-FLIGHT)
Working on branch: patina
Tagged as: session-20260208-103844-claude-start

### 10:40-10:50 — Fix compile errors + clean up warnings
Read all 6 broken/affected files. Fixed two compile errors:
1. `src/retrieval/fusion.rs` — removed `use super::intent::IntentWeights`, replaced
   `rrf_fuse`/`rrf_fuse_weighted` with types-only module (no functions, no tests).
2. `src/commands/eval/mod.rs` — removed `use crate::retrieval::intent::detect_intent`
   and the "By Detected Intent" analysis section from `execute_nl()`.

Cleaned up 4 additional warnings:
- Removed unused `rayon::prelude` import from `src/retrieval/engine.rs`
- Removed unused `Oracle` trait re-export from `src/retrieval/mod.rs`
- Removed unused `scry_lexical` re-export from `src/commands/scry/mod.rs`
- Added `#[allow(dead_code)]` on `QueryOptions` (field read by MCP, not engine)

Commit [[cb586c3c]]: `refactor(scry): simplify to semantic-only — remove RRF, intent, factual oracles`
328 tests pass, clippy clean.

### 10:50-10:55 — Delete unused oracle files
Removed 5 files no longer compiled (already excluded from `mod.rs`):
- `src/retrieval/oracles/lexical.rs`
- `src/retrieval/oracles/temporal.rs`
- `src/retrieval/oracles/persona.rs`
- `src/retrieval/oracles/belief.rs`
- `src/retrieval/intent.rs`

1,189 lines deleted. Commit [[8a1eb75e]]: `refactor(scry): delete factual oracle files — moved to assay`

### 10:55-11:10 — Wire MCP server for new assay query types
Read `src/mcp/server.rs` handle_tool_call. Updated:
- Added `search`, `cochange`, `belief` to assay tool's `query_type` enum in MCP schema
- Added `query` parameter to assay tool schema for search text
- Added routing: `search` accepts `query` or `pattern`, `cochange` and `belief` use `pattern`
- Validated with pattern-required checks and error messages

Commit [[9100d9d3]]: `refactor(mcp): wire assay search/cochange/belief query types`

### 11:10-11:20 — Build release, install, eval
Built release binary (`cargo build --release`), installed (`cargo install --path .`).
Tested live:
- `patina assay search "semantic oracle"` — 10 ranked FTS5 results, working
- `patina assay cochange src/retrieval/engine.rs` — 17 co-change partners, working
- `patina eval --nl` — 0% across all metrics (expected, documented honestly)

Post-split eval baseline (semantic-only):
```
Mean P@5: 0.0%   Mean P@10: 0.0%   MRR: 0.000
```
All zeros expected: NL eval designed for multi-oracle ablation; semantic index
polluted with 27K session events (88%); doc_ids don't resolve to file paths
eval expects. Phase 2 (knowledge domain) and Phase 4 (eval redesign) address.

Updated [[semantic-structural-split]] spec: marked Phase 1 complete with eval baseline.
Commit [[718abccc]]: `docs: mark Phase 1 complete, document eval baseline`

### Patterns Observed
- **Continuation prompts work** — the triage prompt from [[20260208-101655]] captured
  exact error state, file paths, and fix path. This session picked up without re-reading
  any code beyond the 6 files listed. Context crash recovery in ~10 minutes.
- **Additive-then-reductive** — the [[semantic-structural-split]] spec's "no flag day"
  approach (3 additive commits, then 4 reductive commits) meant every intermediate
  state was independently shippable. No flag day, no broken trunk.
- **Eval honesty** — 0% eval is the correct result. The NL eval was designed for the
  old architecture. Documenting it honestly (per [[andrew-ng-over-shoulder]]) prevents
  the temptation to "fix" the number by patching the eval rather than fixing the system.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 14
- Commits: 4
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260208-103844-claude-start..session-20260208-103844-claude-end

## User Prompts (2)

1. `[Pasted text #2 +76 lines] AND THIS IS OUR TRIAGE POST CONTEXT CRASH PROMPT: [Pasted text #3 +55 ...`
2. `/session-end`
