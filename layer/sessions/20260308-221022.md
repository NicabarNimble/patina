---
type: session
id: 20260308-221022
title: 'Seam 1 Multi-Destination Broker — Phase C: Broker Routing (continue)'
status: archived
llm: claude
created: 2026-03-09T02:10:22Z
start_timestamp: 1773022222794
git:
  branch: patina
  starting_commit: 545db5d2598bbdc4d1b341a8d5c32a88a8e8e8a7
  start_tag: session-20260308-221022-claude-start
---

## Previous Session Context
Last session "Schema Contract Model — Expand Parser to Match Authoring Surface" (20260308-213707) completed Seam 2 Phase 1: expanded the schema parser in `internal.rs` with `IdentityFields`, `ProjectionDef`, `ColumnDef`, `ContractDef`, and `LakeConfig` types, updated both schema.toml sources, and promoted [[spec-connector-owns-tables]] to active. All 609 tests pass including 11 new schema tests. Critically, the session established that schema contract types in `src/commands/schema/internal.rs` are the single source of truth — broker, lakehouse, and transport code must import them, not duplicate. Session ended with a prompt to begin Seam 1 (multi-destination broker) next.

## Goals
- [x] Seam 1 Multi-Destination Broker — Phase C: Broker Routing (continue)

## Activity Log
### 22:10 - Session Start
Session initialized with goal: Seam 1 Multi-Destination Broker — Phase C: Broker Routing (continue)
Working on branch: patina
Tagged as: session-20260308-221022-claude-start


### 22:19 - Update: Phase C broker routing complete

**Git Activity:**
- Commits this session: 2 ([[commit-0c51380e]], [[commit-6169739b]])
- Files changed: 6 (`crates/patina-pipe/src/sandbox.rs`, `src/broker/mod.rs`, `src/broker/sources.rs`, `src/broker/spawn.rs`, `src/commands/schema/internal.rs`, `src/commands/schema/mod.rs`)
- Working tree: clean (uncommitted only in `layer/events.jsonl` + session files)

**Phase C: Broker routing branch** ([[commit-0c51380e]])
Read all 4 anchoring values, `src/broker/mod.rs` (run_source), `src/broker/sources.rs` (Destination enum),
`src/broker/routing.rs` (validation + write), `src/broker/lifecycle.rs` (BrokerChild trait), and
`src/broker/spawn.rs` (spawn_native return type) before writing any code.

Refactored `run_source()` in `src/broker/mod.rs`:
- Steps 1-3 (connection, credential, spawn) remain common
- `match &source.destination` branches after spawn
- `write_to_project()` — pure extraction of steps 4-10, zero behavioral changes
- `route_to_lake()` — stub that logs intent, shuts down child cleanly, returns error

Applied [[unix-philosophy]]: routing is transport, not domain. Each function does one thing.
Applied [[contracts-before-consumers]]: did NOT touch `write_facts_with_cursor` for lakes (Trap #1).
Applied [[dependable-rust]]: helper functions are private, public interface unchanged.

**Formatting + clippy fixes** ([[commit-6169739b]])
Pre-existing `cargo fmt` diffs across 5 files from prior sessions. Replaced manual `Default for Destination`
with `#[derive(Default)]` + `#[default]` attribute (clippy). Added `#[allow(unused_imports)]` on schema
re-exports since consumers don't exist yet.

**Stop conditions respected:**
- Did NOT touch schema contract types
- Did NOT rewrite consumers
- Did NOT build lakehouse binary
- Did NOT generalize write_facts_with_cursor
- `write_to_project` is a pure extraction — diff shows only the function boundary change

**Validation:** All 366 tests pass. `./resources/git/pre-push-checks.sh` passes all 7 checks.
Broker integration test confirms project-scope scrape still works unchanged.


### 22:26 - Session wrap-up + next session prompt

**No new commits since last update.** Working tree clean except `layer/events.jsonl` + session files.

**Audit agent review received** — confirmed Seam 1 is correctly finished at the architectural level.
Key validation: `route_to_lake()` as explicit fail path is the right move. Broker understands the
routing boundary without claiming durability or cursor semantics that don't exist yet.

**Next session prompt written** — Seam 2 consumer migration. Anchored in all four core values.
Priority from audit: migrate one consumer end-to-end against the shared schema model. Prerequisite
check: verify schema types are importable from a neutral location before touching consumers. If
`commands::schema` feels wrong as an import source for broker/search/scrape, extract types to a
neutral module first. Rule: do not touch lake control plane yet.

**Session goal achieved:** Seam 1 Phase C (broker routing) is complete. [[implementation-is-three-seams]]
Seam 1 is now fully closed across Phases A, B, and C. Next work is Seam 2 consumer migration.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: experiment
- Files Changed: 6
- Commits: 2
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260308-221022-claude-start..session-20260308-221022-claude-end

## User Prompts (3)

1. `[Pasted text #1 +87 lines]`
2. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
3. `/session-end`
