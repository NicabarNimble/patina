---
type: session
id: 20260306-150846
title: Pipe Architecture Spec Rewrite — Protocol + Broker Model
status: archived
llm: claude
created: 2026-03-06T20:08:46Z
start_timestamp: 1772827726749
git:
  branch: patina
  starting_commit: ad214e369330c4e321edcd770e6988d86b107ac7
  start_tag: session-20260306-150846-claude-start
---

## Previous Session Context
Session 8 (20260306-123021) was a deep architecture design discussion that reframed the pipe-architecture spec. Key decisions: Pipe = protocol (JSON-RPC 2.0 + WIT types), not a process type; Children = all managed services (connectors, lakehouse, transforms); Mother = broker (Netflix/Kafka pattern, routes based on pub/sub declarations); Connection = pipe + auth linked (`patina connect`). Also clarified: `host_emit` IS already pipe protocol — the rewrite formalizes and extends it, plus renames `host_*` to semantic names for LLM comprehension. Produced a comprehensive rewrite prompt but deferred the actual spec rewrite to this session.

## Goals
- [x] Audit related specs for alignment (core-extraction, forge-plugin-extraction, continuous-operation, persona-federation, lake-registry)
- [x] Rewrite pipe-architecture SPEC.md with protocol + broker model
- [x] Rewrite pipe-architecture DESIGN.md with updated architecture
- [x] Flag belief updates needed (3 of 8 need revision)
- [x] Present rewritten spec for review
- [x] CLI naming decision: `patina connect` + `patina mother` (no `patina pipe`/`patina child`)

## Activity Log
### 15:08 - Session Start
Session initialized with goal: Pipe Architecture Spec Rewrite — Protocol + Broker Model
Working on branch: patina
Tagged as: session-20260306-150846-claude-start

### 15:30 - Grounding Complete
Read all required materials: 4 values ([[spec-driven-design]], [[dependable-rust]], [[unix-philosophy]], [[safety-boundaries]]), current [[spec-pipe-architecture]] SPEC.md + DESIGN.md, 3 session files ([[session-20260305-224446]], [[session-20260306-100718]], [[session-20260306-123021]]), all 8 beliefs ([[pipes-are-processes-not-wasm]], [[host-proxied-io-is-the-security-model]], [[mother-holds-connections-pipes-transform]], [[pipe-protocol-is-transport-agnostic]], [[persona-keypair-is-node-identity]], [[wit-defines-pipe-contract-not-runtime]], [[wit-is-contract-wasm-is-one-runtime]], [[patina-is-domain-agnostic-knowledge-system]]), 6 implementation files (`src/plugin/internal/host_support.rs`, `src/plugin/internal/mod.rs`, `src/mcp/server/mod.rs`, `src/secrets/mod.rs`, `plugins/forge/src/github.rs`, `plugins/forge/plugin.toml`). Related spec audit delegated to agent.

### 16:00 - Related Spec Audit Complete (Phase 1)
No hard contradictions across 8 specs ([[spec-core-extraction]], [[spec-forge-plugin-extraction]], [[spec-continuous-operation]], [[spec-persona-federation]], [[spec-lake-registry]], [[spec-scrape-simplification]], [[spec-mother-maturation]], [[spec-core-plugin-extraction]]). Key reconciliation: `host_emit` is WASM transport for pipe protocol, connector as plugin role aligns with child taxonomy, [[spec-continuous-operation]]'s belief router IS the broker pattern. Three specs most affected: [[spec-forge-plugin-extraction]], [[spec-continuous-operation]], [[spec-mother-maturation]].

### 16:30 - SPEC.md Rewrite Complete (Phase 2)
Full rewrite of [[spec-pipe-architecture]] SPEC.md: title changed from "Data Flow Primitive" to "Protocol + Broker Model." New framing: pipe = protocol, children = managed services, Mother = broker. Updated exit criteria (6), separation of concerns (5-column), child taxonomy, connection model, pub/sub destinations. Removed "every arrow is a pipe", "universal data flow primitive", LakeReaderPipe.

### 17:00 - DESIGN.md Rewrite Complete (Phase 3)
Full rewrite of [[spec-pipe-architecture]] DESIGN.md addressing all 10 audit issues: streaming delivery (FactEmitter), canonical serialization (sorted JSON for blake3), typed errors (PipeError with 4 variants), delivery guarantees (at-least-once + dedup), persona enforcement (scoping, enforcement points, multi-persona), failure mode catalog (child/Mother/network), encryption gap (Mother-side model), async position (sync-first), child framework (lifecycle, manifest, GitHub example), crate structure (pipe-types/sdk/pipe).

### 17:10 - Belief Alignment Check (Phase 4)
3 of 8 beliefs need revision: [[pipes-are-processes-not-wasm]] (pipe=protocol now), [[host-proxied-io-is-the-security-model]] (dual model: WASM + native), [[mother-holds-connections-pipes-transform]] (transport children hold connections). 5 aligned: [[pipe-protocol-is-transport-agnostic]], [[persona-keypair-is-node-identity]], [[wit-defines-pipe-contract-not-runtime]], [[wit-is-contract-wasm-is-one-runtime]], [[patina-is-domain-agnostic-knowledge-system]].

### 17:15 - Commit
[[commit-3b520fad]] — spec(pipe-architecture): rewrite for protocol + broker model. 2 files, +1218 -980.

### 17:20 - Review Discussion
Compared spec against Netflix/Kafka/Google enterprise patterns. Core model landed right. Three issues identified: (1) separation of concerns table has 5 columns but should have 4 (protocol and connection aren't actors), (2) "pipe" leaks as a noun in CLI commands, (3) inconsistent "pipe" language throughout.

### 17:40 - CLI Naming Decision
Discussed who uses the Patina CLI. Answer: builders and operators — both know the architecture. No casual users. Patina is consumed by LLMs; humans using CLI are building or managing a Patina system. Decision: `patina connect` for setup (auth + connector config), `patina mother` for management (status, health, run, logs). No `patina pipe` commands (pipe is protocol, invisible). No `patina child` commands (children are Mother's concern, invisible). User talks to Mother or sets up connections.

### 17:45 - Next Session Prompt
Wrote prompt for tightening session: fix separation of concerns table, apply CLI naming decision, fix inconsistent "pipe" language, revise 3 beliefs, add alignment notes to 3 related specs.

## Key Decisions Made This Session
1. **SPEC.md + DESIGN.md fully rewritten** for protocol + broker model
2. **CLI naming: `patina connect` + `patina mother`** — no `patina pipe` or `patina child` commands. Pipe is protocol (invisible), children are Mother's concern (invisible).
3. **Patina CLI users = builders + operators** — both know the architecture, no casual users. Patina is consumed by LLMs.
4. **Separation of concerns needs 4 columns** (actors) not 5 — Pipe Protocol and Connection are properties, not actors.
5. **3 beliefs flagged for revision** — deferred to next session.

## Open Questions for Next Session
1. Tighten separation of concerns table (4-column, show Mother's dual role)
2. Apply CLI naming decision throughout spec/design
3. Fix inconsistent "pipe" language (pipe-as-noun → child)
4. Revise 3 beliefs ([[pipes-are-processes-not-wasm]], [[host-proxied-io-is-the-security-model]], [[mother-holds-connections-pipes-transform]])
5. Add alignment notes to [[spec-forge-plugin-extraction]], [[spec-continuous-operation]], [[spec-mother-maturation]]


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 2
- Commits: 1
- Patterns Modified: 2
- Beliefs Captured: 0
- Session Tags: session-20260306-150846-claude-start..session-20260306-150846-claude-end

## User Prompts (11)

1. `[Pasted text #1 +286 lines]`
2. `yes make updates i can go through if changes need after `
3. `how did we land compared to : [Pasted text #1 +89 lines]`
4. `lets chat what do you think?`
5. `/context `
6. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
7. `what if mother was the command since mother should spawn children as needed .. not sure we should...`
8. `so who is a patina user .. walkthrough that `
9. `i think this is true .. mostly patina is consumed by an llm . if youre in the system and not via ...`
10. `yes`
11. `/session-end `
