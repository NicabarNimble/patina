---
type: session
id: 20260216-132758
title: 'Session Goal: continued review and cleanup of cross-project-beliefs SPEC'
status: archived
llm: claude
created: 2026-02-16T18:27:59Z
start_timestamp: 1771266479009
git:
  branch: patina
  starting_commit: 9318534b4fd1c098cdb7774d33e044fe8455133d
  start_tag: session-20260216-132758-claude-start
---

## Previous Session Context
Split the [[cross-project-beliefs]] SPEC into two documents: a buildable mechanism SPEC (Phase A/B/C) and a [[mother-design]] draft SPEC parking design principles and evolution roadmap. Validated the SPEC against the actual codebase — discovered `mother graph sync` already exists (naming collision), `scry` MCP is wrong home for cross-project search, and `context` is already a composition tool. Revised architecture: extend `mother graph sync` for belief sync, add CLI `patina mother search` + new MCP `mother` tool as 4th atomic tool, and extend `context` MCP as the composition layer above scry/assay/mother.

## Goals
- [x] Session Goal: continued review and cleanup of cross-project-beliefs SPEC

## Activity Log
### 13:27 - Session Start
Session initialized with goal: Session Goal: continued review and cleanup of cross-project-beliefs SPEC
Working on branch: patina
Tagged as: session-20260216-132758-claude-start


### 13:36 - Update (covering since 13:27)

**Git Activity:**
- Commits this session: 2
- Files changed: 1 (`layer/surface/build/feat/cross-project-beliefs/SPEC.md`)
- Last commit: [[commit-46b99205]]

**Work completed:**
- Full implementability review of [[cross-project-beliefs]] SPEC against all 12 referenced source
  files. Read every file in the prescribed read order before forming opinions, per
  [[ground-assertions-or-pay-review-tax]].
- Verified all 10 evidence table line references against current source — all accurate (no source
  code changed in last session's layer-only commits).
- Found and fixed 4 issues in 2 scalpel commits:
  1. MCP tool count: "Four" → "Three" (only scry, assay, context exist) [[commit-af42ee1b]]
  2. Knowledge source field: project UID → registry name (UIDs don't match graph node IDs,
     would break joins and CLI display)
  3. SQL comment consistency: updated to match Phase B text
  4. Nonexistent `belief_evidence` table → `belief_verifications` (actual table name) [[commit-46b99205]]
- Validated Phase B feasibility: `sync_from_registry()` in `src/commands/mother/graph.rs:16-68`
  is cleanly extensible — has `Graph::open()` access and registry walk with path+name per project.
- Validated Phase C MCP feasibility: `handle_list_tools()` and `handle_tool_call()` in
  `src/mcp/server.rs` support adding a 4th tool trivially (one array entry + one match arm).
- Confirmed `context` extension path: `get_project_context()` in `src/commands/context.rs`
  already has `if let Some(t) = topic` pattern that can add cross-project knowledge section.
- Noted pre-existing issue: `context` MCP tool accepts `repo`/`all_repos` params that
  `get_project_context()` ignores (dead params, not this SPEC's problem).

**Key decisions:**
- **Registry name over UID for knowledge source** — graph node IDs are registry names (human-readable),
  not `.patina/uid` values. Using UIDs would require a resolution step for CLI display and break
  future node→knowledge joins. Registry names are the natural key.

**Patterns observed:**
- The "read all referenced files before forming opinions" discipline caught the UID-vs-name
  mismatch that wouldn't be visible from reading the SPEC alone — needed to see how
  `sync_from_registry()` constructs node IDs to realize the source field was wrong.
- Evidence table line references survived 3 commits because those commits only touched layer
  files. The SPEC's evidence grounding is robust to non-source changes.


### 14:23 - Update (covering since 13:36)

**Git Activity:**
- Commits this session: 4
- Files changed: 1 (`layer/surface/build/feat/cross-project-beliefs/SPEC.md`)
- Last commit: [[commit-30f49126]]

**Work completed:**
- Trimmed `context` extension from Phase C to non-goals — recognized it's a composition
  concern that should follow after the core mechanism (sync + search) proves out.
  Removed Phase C context section, dropped exit criterion #4, moved to non-goals. [[commit-9841a129]]
- Closed 7 implementability gaps identified during collaborative review: [[commit-30f49126]]
  1. **Project resilience**: skip-with-warning for missing patina.db, legacy schema, ref repos
  2. **Persona parsing rules**: pinned minimal schema (id+statement required), defaults
     (entrenchment=medium, status=active, facets=[]), malformed=warn+skip
  3. **Schema migration**: `CREATE TABLE IF NOT EXISTS` sufficient for rebuildable cache
  4. **Rebuild atomicity**: single SQLite transaction — DELETE+repopulate+COMMIT, rollback on failure
  5. **Search API contract**: default limit=10, FTS5 rank ordering, return fields specified,
     CLI truncates 200 chars, MCP returns full statement, no filters in v1
  6. **Source naming collisions**: registry keys unique by HashMap construction, (id,source) PK
  7. **Future edge data**: source matches nodes.id, adoption in edges.evidence, no duplication

**Key decisions:**
- **Context is a follow-up** — the SPEC's job is mechanism (sync + search), not composition.
  `context` integration comes after we prove the mechanism works.
- **No schema versioning** — knowledge is a rebuildable cache, not source data. `mother graph sync`
  rebuilds from source, same as `patina rebuild`. PRAGMA user_version is over-engineering.
- **No filters in v1** — FTS5 query is the only search input. Facet/kind/source filters are
  future scope. Keep the first version simple.

**Patterns observed:**
- User review caught 7 gaps that the codebase validation pass missed — structural correctness
  (do the APIs exist?) is different from implementability (what happens at the edges?). Both
  passes are needed. The codebase pass catches "this won't compile." The user pass catches
  "this will compile but has undefined behavior at runtime."


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 1
- Commits: 4
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260216-132758-claude-start..session-20260216-132758-claude-end

## User Prompts (7)

1. `[Pasted text #1 +64 lines]`
2. `/session-update`
3. `tldr what was done this session`
4. `is context a foot note more or less in this spec or is a central part? as i worry its more next l...`
5. `yes do it`
6. `[Pasted text #1 +14 lines]`
7. `/session-end`
