---
type: session
id: 20260309-145807
title: 'Build spec patina-connect — Session 2 of 4: Store + Auth Resolution (commits 3-4)'
status: archived
llm: claude
created: 2026-03-09T18:58:07Z
start_timestamp: 1773082687855
git:
  branch: patina
  starting_commit: 6fa400024e583bde70db8f2310601b00ef754742
  start_tag: session-20260309-145807-claude-start
---

## Previous Session Context
Session 20260309-144700 executed DESIGN.md commits 1-2 for patina-connect. Commit 1 added `paths::connections` module (`connections_dir()` + `connection_path()`) to `src/paths.rs`. Commit 2 built the full domain model in `src/connect/internal/model.rs`: `ConnectionRecord`, `AuthConfig`, `InjectionStrategy` (tagged enum with serde), `AuthPlan`/`ResolvedCredential` (ephemeral, no serde), `ConnectError` (7 variants), plus 12 tests including TOML round-trip. A fmt cleanup commit followed. All pre-push checks passed. Handoff: domain model types ready, session 2 builds store + auth resolution.

## Goals
- [x] Commit 3: `connect: add store (TOML persistence + referential integrity)` — `load()`, `list()`, `create()`, `remove()` with reference checks, uses `paths::connections`, tests with temp dirs
- [x] Commit 4: `connect: add auth plan resolution — fail closed` — `resolve_auth()` seam, tests for each failure mode, missing credential = error not warning

## Activity Log
### 14:58 - Session Start
Session initialized with goal: Build spec patina-connect — Session 2 of 4: Store + Auth Resolution (commits 3-4)
Working on branch: patina
Tagged as: session-20260309-145807-claude-start


### 15:10 - Update (covering since 14:58)

**Git Activity:**
- Commits this session: 2 ([[commit-e8df91a4]], [[commit-d0494b43]])
- Files changed: 5 (`src/connect/internal/mod.rs`, `src/connect/internal/model.rs`, `src/connect/internal/resolve.rs`, `src/connect/internal/store.rs`, `src/connect/mod.rs`)
- All changes committed, working tree clean (except layer/events.jsonl + session files)

**Work completed:** Executed [[spec-patina-connect]] DESIGN.md commits 3-4 (model-out build).

1. **[[d0494b43]] `connect: add store (TOML persistence + referential integrity)`** — Created `src/connect/internal/store.rs` with CRUD operations following [[dependable-rust]]:
   - `load(name)` → reads from `paths::connections::connection_path()`, returns typed `ConnectError::NotFound` or `MalformedConfig`
   - `list()` → reads connections dir, skips non-TOML, computes status via `compute_status()` (checks `last_error`, `expires_at`, `last_validated`)
   - `create(record, credential)` → writes TOML + stores credential in global vault via `secrets::add_secret`
   - `remove(name, force)` → referential integrity check via `broker::sources::scan_all_sources()` before deletion, returns `ConnectError::InUse` with project references. Vault cleanup is best-effort.
   - Added `ConnectionSummary` type to model for list output
   - Public API wired: `connect::load()`, `list()`, `create()`, `remove()`
   - 9 tests: load errors, TOML roundtrip, compute_status for all 4 states, referential integrity

2. **[[e8df91a4]] `connect: add auth plan resolution — fail closed`** — Created `src/connect/internal/resolve.rs` implementing the consumption layer seam:
   - `resolve_auth(record)` → checks child binary via `broker::spawn::resolve_child_binary()`, decrypts vault via `secrets::get_global_secret()`, returns `AuthPlan`
   - FAIL CLOSED: `ChildNotFound` (missing binary), `CredentialMissing` (vault returns None), `VaultError` (decryption failure)
   - Replaces "proceeding without auth" pattern in `broker/mod.rs:44` (broker integration deferred to session 4 commit 6)
   - Public API: `connect::resolve_auth()`
   - 7 tests: child not found, error construction, AuthPlan with Bearer/Header/InProcess strategies, allowed_domains propagation

**Key decisions:**
- `remove()` does best-effort vault cleanup — if vault entry removal fails (e.g., no vault exists), it warns but still removes the TOML. Connection TOML is the primary record.
- `compute_status()` uses simple ISO 8601 string comparison for expiry checks — works because ISO 8601 sorts lexicographically. No chrono parsing overhead.
- `resolve_auth()` checks child binary existence BEFORE attempting vault decryption — fail fast, avoid unnecessary Touch ID prompts.

**Pre-push checks:** All 8 checks pass (WIT, fmt, clippy, tests, schema consistency). 36 total connect tests.

**Handoff to Session 3:** Store + auth resolution complete. Session 3 builds commit 5: Provider trait + GitHub OAuth device flow (RFC 8628). Session 4 builds commits 6-9: broker refactor (consume AuthPlan, sever plugin imports, extract HTTP utils), CLI commands, end-to-end verification.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: experiment
- Files Changed: 5
- Commits: 2
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260309-145807-claude-start..session-20260309-145807-claude-end

## User Prompts (2)

1. `[Pasted text #1 +61 lines]`
2. `/session-end`
