---
type: session
id: 20260307-143548
title: Close Mother-Broker Design Gaps + Alignment Audit
status: archived
llm: claude
created: 2026-03-07T19:35:49Z
start_timestamp: 1772912149100
git:
  branch: patina
  starting_commit: cd73e0b47c07fa2f999d75772cf53484542f4ca0
  start_tag: session-20260307-143548-claude-start
---

## Previous Session Context
Built pipe-mother-io ECs 1-3: implemented pipe/http protocol with `PipeIo` context (unified fact emission + HTTP), `HttpHandler` callback pattern to keep patina-pipe free of reqwest, and sandbox tightening (deny-all network on both macOS/Linux). Also added audit logging for pipe/http requests. Activated mother-broker spec (draft → ready → active) and audited its DESIGN.md, uncovering 7 concrete design gaps (connection config format, credential delivery, cursor storage, content-hash dedup, pipe/http production handler integration, NativeChild wrapping, schema validation scope). Session ended with explicit decision: do NOT start building mother-broker until design gaps are closed — next session is design gap closure + alignment audit, not coding.

## Goals
- [x] Close Mother-Broker Design Gaps + Alignment Audit

## Activity Log
### 14:35 - Session Start
Session initialized with goal: Close Mother-Broker Design Gaps + Alignment Audit
Working on branch: patina
Tagged as: session-20260307-143548-claude-start


### 14:43 - Update (covering since 14:35)

**Git Activity:**
- Commits this session: 0
- Files changed: 3 (DESIGN.md +325 lines, active-session.md, events.jsonl)
- Last commit: 15 minutes ago (from previous session)

**Work completed:**
- Phase 1 (Read Code): Read all 11 files from the session prompt — [[spec-mother-broker]] SPEC.md + DESIGN.md, [[spec-pipe-mother-io]] SPEC.md, `src/mother/mod.rs`, `src/mother/child.rs`, `src/commands/mother/mod.rs`, `src/eventlog.rs`, `src/plugin/internal/host_support.rs`, `src/secrets/mod.rs`, `src/secrets/vault.rs`, `crates/patina-pipe/src/harness.rs`, `crates/patina-pipe-types/src/manifest.rs`. Also read [[spec-patina-connect]] SPEC.md and `src/plugin/internal/mod.rs` (plugin.toml credential mapping patterns) for additional context.
- Phase 2 (Gap Analysis): Synthesized all 7 gaps into concrete proposals with code sketches. Presented to user for review.
- Phase 3 (Design Update): User approved all 7 with specific refinements. Updated [[spec-mother-broker]] DESIGN.md with:
  - §8: Connection config format — `schema_version = 0`, minimal + rich format compatibility
  - §9: Credential delivery — Mother-injected pipe/http by default, `auth.requires_in_process_token` opt-in
  - §10: Cursor storage — reuse `scrape_meta` with `cursor:{source}` key convention
  - §11: Content-hash dedup — partial UNIQUE index, `INSERT OR IGNORE`, `WriteResult.dedup_skipped` metric
  - §12: Pipe/HTTP production handler — reuses `host_support` functions (`pub(crate)`), per-child HTTP client
  - §13: NativeChild adapter — wraps `ChildConnection`, documents streaming refactor target + error attribution
  - §14: Schema validation scope — warn + pass-through, once-per-schema-per-run log line
  - Converted 3 Open Questions to 7 Resolved Questions
  - Updated full flow diagram, module structure, commit plan (8→11 commits), key files

**Key decisions:**
- Credential split: Mother handles all HTTP auth injection transparently; child gets raw token ONLY if `auth.requires_in_process_token = true` in manifest. Default is secure (child never sees secret)
- No new cursor table — `scrape_meta` with `cursor:` prefix is sufficient and already exists
- `schema_version = 0` in connection config for forward compatibility with [[spec-patina-connect]]
- Schema validation is correctness, not security — warn + pass-through on missing schemas
- NativeChild adapter collects-then-iterates (not truly streaming) — acceptable because transactional write needs full batch anyway

**Discussion context:**
- User refined every gap with actionable constraints: schema versioning for connection format, per-child HTTP client caching, error attribution tracing, periodic-not-per-fact schema warnings, future `patina schemas status` CLI
- Anchored in [[spec-driven-design]] (SPECs decide, code executes), [[host-proxied-io-is-the-security-model]] (credentials via Mother, not env vars), [[safety-boundaries]] (no network without consent)

**Patterns observed:**
- Reading all existing code before writing design prevents duplication — discovered `scrape_meta` already has exactly the shape needed for cursors
- The host_support functions are well-factored enough that `pub(crate)` visibility change is the only modification needed for broker reuse
- Connection config versioning is a pattern worth generalizing — any config that crosses spec boundaries should carry a schema_version


### 15:18 - Update (covering since 14:43)

**Git Activity:**
- Commits this session: 3 ([[commit-3ea764cd]], [[commit-9356d602]], [[commit-19807efb]])
- Files changed: 2 (`DESIGN.md`, `SPEC.md`)
- Last commit: 2 minutes ago

**Work completed:**
- Two rounds of design hardening on [[spec-mother-broker]] DESIGN.md:
  - §8: Connection versioning changed from "reject unknown" to compatibility map (warn on unknown, attempt read)
  - §9: Credential delivery documented as v1 escape hatch with known limitations (no revocation, no per-request scopes)
  - §10: Replaced `scrape_meta` reuse with dedicated `broker_cursors` table — added CHECK constraint on cursor length, concurrency failure mode table, migration path (first run = full sync), stale cursor cleanup via `--prune`
  - §11: Added validation gate (reject facts without content_hash), debug-level dedup logging
  - §12: Made HTTP client construction fallible, added domain normalization (lowercase, port-stripped)
  - §13: Added `max_batch_size` limit (10k default), documented v2 streaming path
- Phase 4 (Alignment Audit): mapped all 9 SPEC.md ECs to DESIGN.md decisions. Found 3 mismatches + 2 gaps:
  - Schema validation: added §6 decision table (undeclared = drop, uninstalled = warn + pass-through) [[commit-9356d602]]
  - Credentials EC: rewritten to cover Tier 1 (header injection) and Tier 2 (pipe/initialize token)
  - `source_id` format: `child:{child_name}` documented in §6
  - Sandbox enforcement: new section — default sandbox-on, `--no-sandbox` override, failure surfacing
  - Mother status: new section — broker_cursors + WriteResult + eventlog feeding status output
- Scope narrowing: removed 4 SPEC.md contradictions [[commit-3ea764cd]]:
  - Scheduling: v1 = manual + on-scrape only (stream/hourly/daily → [[spec-continuous-operation]])
  - Fan-out: removed shared-spawn promise, v1 = one spawn per source + content-hash dedup
  - Uniform lifecycle: EC `mother-spawns-children` now accurately describes WASM legacy path
  - Mother status: EC clarifies standalone mode = historical data only

**Key decisions:**
- `broker_cursors` dedicated table over `scrape_meta` reuse — eliminates namespace collision risk
- Schema validation decision table distinguishes child bugs (undeclared schema → drop) from deployment gaps (uninstalled schema → warn + pass-through)
- SPEC scope narrowed to match design reality — stream lifecycle, fan-out optimization, and timer scheduling all explicitly punted to named future specs

**Challenges faced:**
- Initial cursor design (reuse `scrape_meta`) was challenged for hidden coupling — correct criticism, fixed by dedicating a table
- Connection config "reject unknown version" was brittle — fixed with compatibility map that attempts read on unknown versions
- SPEC.md had accumulated aspirational language that contradicted DESIGN.md constraints — systematic audit caught all 4


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 2
- Commits: 3
- Patterns Modified: 2
- Beliefs Captured: 0
- Session Tags: session-20260307-143548-claude-start..session-20260307-143548-claude-end

## User Prompts (13)

1. `[Pasted text #1 +91 lines]`
2. `[Pasted text #2 +28 lines]`
3. `great .. i'm sorry this part wasnt meant for you: Happy to wordsmith DESIGN.md once you settle on...`
4. `oh no you did correct .. i had added a bit about me updating and me wordsmith`
5. `ok lets walk trhough where we are`
6. `[Pasted text #1 +5 lines]`
7. `[Pasted text #2 +5 lines]`
8. `[Pasted text #3 +4 lines]`
9. `Looks good from here. DESIGN.md is locked in, the implementation watch-list is clear, and the onl...`
10. `[Pasted text #4 +11 lines]`
11. `[Pasted text #5 +6 lines]`
12. `is there a phase 5?`
13. `/session-end`
