---
type: session
id: 20260212-102737
title: 'Session: Build fix spec [[plugin-system-final-audit-fixes]]'
status: archived
llm: claude
created: 2026-02-12T15:27:37Z
start_timestamp: 1770910057333
git:
  branch: patina
  starting_commit: 244961701c004be2e77cf14e9e57ba20d24c03eb
  start_tag: session-20260212-102737-claude-start
---

## Previous Session Context
Completed a full Phase 1 final audit of the plugin system — 0 critical findings, 9 total. Created fix spec [[plugin-system-final-audit-fixes]] with 6 fixes (F0-F5): F0 eliminates `unsafe impl Sync` on WasmChild, F1 fixes registry RwLock poison handling, F2 adds toy deduplication, F3 adds CI WIT consistency check, F4 adds toy capability gating via manifest allowlist, F5 adds design doc comments. Captured 2 beliefs: [[no-deferrals-without-blockers]] and [[world-boundary-is-type-safety]]. Zero deferrals — all unblocked work folded into the spec.

## Goals
- [x] Build all 6 fixes from [[plugin-system-final-audit-fixes]] spec in prescribed order

## Activity Log
### 10:27 - Session Start
Session initialized with goal: Session: Build fix spec [[plugin-system-final-audit-fixes]]
Working on branch: patina
Tagged as: session-20260212-102737-claude-start


### 10:41 - Update: All 6 Fixes Complete

**Work completed:**
- Read all 3 anchor docs ([[dependable-rust]], [[spec-driven-design]], [[patina-identity]]) and all source files before writing anything
- Built all 6 fixes in spec-prescribed order: F5→F0→F3→F1→F4→F2
- F5 [[4023de3e]]: Design decision doc comments — string dispatch in `wit/mother-child.wit`, tick split in `src/mother/child.rs`, PluginEngine create-once in `src/plugin/internal.rs`
- F0 [[c1ede205]]: Eliminated `unsafe impl Sync` for WasmChild — moved `bindings::MotherChild` instance behind same Mutex as Store using `WasmChildInner` struct. Required struct destructuring pattern (`let WasmChildInner { store, instance } = &mut *inner`) to split borrows across MutexGuard
- F3 [[0a5adf39]]: Added WIT consistency check as step 1/4 in `resources/git/pre-push-checks.sh` — `diff -r` for all 3 guest crate wit/ dirs
- F1 [[3808fa9a]]: Replaced 4 bare `.unwrap()` on RwLock in `src/commands/mother/registry.rs` with `.unwrap_or_else(|e| e.into_inner())` — one poison recovery pattern across entire daemon
- F4 [[a75df13f]]: Toy capability gating — added `allowed_toy_commands` to `PluginManifest` (parsed from `[capabilities.toys].commands`), stored in `WasmChild`, filtered in `tick()`. Updated `patina-plugin-repos/plugin.toml`. 3 new tests (manifest parsing, default empty, end-to-end gating)
- F2 [[95cdc6d1]]: Toy deduplication — `Arc<Mutex<HashSet<String>>>` for in-flight tracking in heartbeat loop. Skip duplicates, clean up on completion/failure, handle thread spawn failure
- F3 spec marked complete [[fe0a0775]]: all 19 exit criteria checked, status log updated
- All pre-push checks pass: WIT consistency, fmt, clippy, test

**Key decisions:**
- F0 borrow splitting: destructuring MutexGuard via `let WasmChildInner { store, instance } = &mut *inner` is the idiomatic way to split borrows when one field needs `&self` and another needs `&mut` — avoids needing separate locks or RefCell
- F4 enforcement at WasmChild adapter, not daemon: per spec Option A — compiled-in children (SecretsCacheChild) are trusted and don't go through this path
- F4 `allowed_toy_commands` on WasmChild (outside Mutex): immutable after construction, like `name` — no lock needed for the allowlist check in tick()

**Discovery:**
- Guest crate `wit/` directories are symlinks (`wit -> ../wit`), not independent copies as the audit reported. This means F3's CI check is defense-in-depth rather than primary gate. F5a only required modifying the canonical `wit/mother-child.wit` — the 3 "copies" are symlinks that point to it. Updated F3 spec section to reflect actual state.

**Patterns observed:**
- Struct destructuring to split borrows across a MutexGuard is a pattern worth remembering for any Mutex<Struct> where different fields need different borrow kinds
- The spec's prescribed build order (docs→soundness→CI→small-fixes→large-fixes) worked well — each commit was independently testable and the dependencies were correctly ordered (F4 before F2 because F4 changes tick, F2 changes spawn)


### 10:50 - Final Update: Belief + Audit Conclusion

**Work completed:**
- Captured belief [[restructure-over-unsafe]] [[96c80102]]: "When the borrow checker blocks you, restructure data — don't reach for unsafe." Enriched with supports ([[compiler-enforced-safety]], [[world-boundary-is-type-safety]]), attacks/attacked-by reasoning, and applied-in reference to F0
- Added post-fix conclusion to final audit report (`layer/surface/reports/audit/2026-02-12-plugin-system-phase1-final.md`) [[e54ad21f]]: Jon Gjengset perspective review covering F0 as standout fix, Zed comparison table (8 dimensions), corrected Phase 2 readiness assessment (command world + doctor extraction, not third-party plugins), clean verdict with zero technical debt

**Key decisions:**
- Corrected Jon's forward-looking items: Phase 2 is `doctor` extraction (internal), not third-party plugins. Capability grant versioning, plugin registries, and signed manifests are not Phase 2 concerns
- Audit conclusion captures the Zed comparison as a permanent reference — validates design decisions (string dispatch intentional, sync-first correct, heartbeat model justified) and identifies real gaps (WIT types inside world block, CLI-direct loading unproven)

## Discoveries

1. **Guest crate wit/ dirs are symlinks, not copies** — the audit reported "four independent copies" but they're `wit -> ../wit` symlinks. F3 CI check added as defense in depth, F5a only needed 1 file edit. Updated both spec and audit report to reflect actual state.

2. **Struct destructuring splits borrows across MutexGuard** — `let WasmChildInner { store, instance } = &mut *inner` is the idiomatic pattern when one field needs `&self` and another needs `&mut`. Captured as [[restructure-over-unsafe]].

3. **Spec-prescribed build order works** — docs→soundness→CI→small-fixes→large-fixes made each commit independently testable. F4 before F2 was correct because F4 changes tick() and F2 changes spawn_toy(), avoiding merge conflicts within the same session.

## Beliefs Captured: 1
- [[restructure-over-unsafe]] — prefer data restructuring over unsafe


## Beliefs Captured: 1

## Session Classification
- Work Type: pattern-work
- Files Changed: 10
- Commits: 9
- Patterns Modified: 3
- Beliefs Captured: 1
- Session Tags: session-20260212-102737-claude-start..session-20260212-102737-claude-end

## User Prompts (8)

1. `[Pasted text #1 +95 lines]`
2. `/session-update`
3. `yes, capture that belief`
4. `ok wheer did we land .. anythig diverge from the current fixes plan?`
5. `whats Jon's take on our system now .. with his understanding of how zed uses wasm how we use wasm...`
6. `one correction wasnt this pahase 1 and phase 2?`
7. `ok capture jons take into our last audit report to show a conclusion`
8. `/session-end`
