---
type: session
id: 20260207-153429
title: continue retrieval-tuning — BM25 scale fix + pattern ID collision
status: archived
llm: claude
created: 2026-02-07T20:34:29Z
start_timestamp: 1770496469409
git:
  branch: patina
  starting_commit: e9c04eee8217326e45086f4bc1fe2869b6b317a0
  start_tag: session-20260207-153429-claude-start
---

## Previous Session Context
Ran Phase 2.5 diagnostics: semantic sanity battery confirmed e5-base-v2 is functional but session-event-dominated (semantic suppression validated), and FTS5 corpus audit found core docs ARE indexed but discovered **F6 bug** — pattern results returned `id` instead of `file_path` as doc_id. Fixed F6 in lexical+semantic paths but eval showed near-zero impact. Error analysis (interrupted) revealed two deeper root causes: **(1) pattern ID collision** — `dependable-rust` exists in both `layer/core/` and `layer/dust/archive/`, dust version overwrites core via PRIMARY KEY conflict; **(2) BM25 scale mismatch** — `scry_lexical()` naively mixes scores from 3 FTS5 tables with different column counts, making scores incomparable.

## Goals
- [x] Root cause 1: fix pattern ID collision (layer scraper ignores .gitignore)
- [x] Root cause 2 attempt 1: inner RRF — fixed scale but regressed P@5
- [x] Spec min-max normalization as replacement for inner RRF
- [ ] Root cause 2 attempt 2: min-max normalization — code written, not yet tested

## Activity Log
### 15:34 - Session Start
Session initialized with goal: continue retrieval-tuning — BM25 scale fix + pattern ID collision
Working on branch: patina
Tagged as: session-20260207-153429-claude-start

### 15:40-16:00 - Error analysis completion + spec
Completed error analysis from [[20260207-150836]]. Traced two root causes:
(1) layer scraper uses `walkdir::WalkDir` ignoring `.gitignore` — 730 dust
patterns indexed, `dependable-rust` collision. Code scraper already uses
`ignore::WalkBuilder`. (2) BM25 scores incomparable across FTS5 tables with
different column counts/corpus sizes. Updated [[retrieval-tuning]] spec.
Committed: [[5750eebc]]

### 16:00-16:15 - Root cause 1 fix (gitignore)
Swapped `collect_md_files` from `walkdir::WalkDir` to `ignore::WalkBuilder`
with `.git_ignore(true)`, matching `src/commands/scrape/code/extract_v2.rs`.
79 patterns (was 808), pruned 729 stale entries. `dependable-rust` now
correctly maps to `layer/core/dependable-rust.md`.
Eval: P@10 +1.3pp (39.0%), structural +9.0pp (47.0%).
Committed: [[ce2ca931]]

### 16:15-16:40 - Root cause 2 attempt 1 (inner RRF)
Applied rank-based fusion (RRF k=60) within `scry_lexical()` across the
three FTS5 tables. P@10 jumped +6.1pp (43.8%), test P@10 +6.7pp (44.2%),
Definition +19.0pp. But **P@5 regressed -3.4pp** (25.4%).
Committed: [[6e651aea]], [[2d639575]]

### 16:40-17:20 - P@5 regression analysis + min-max spec
Applied [[andrew-ng-over-shoulder]]: inner RRF threw away BM25 magnitude,
creating RRF-of-RRF (rank-smoothed twice). 9/52 queries lost P@5 hits —
correct results pushed from positions 1-5 to 6-10 by equal-weight
interleaving. Discussed with user: the right fix is min-max normalization
(log1p + per-table [0,1] scaling) — preserves within-table magnitude while
fixing cross-table comparability. No tunable knobs in v1.
Committed spec: [[3927425a]]

### 17:20 - Min-max code written, not yet tested
`normalize_table()` function added to `src/commands/scry/internal/search.rs`.
Passes clippy. Needs: build release, install, eval, compare against all
three baselines (Phase 2 / RC1 / inner RRF). **Uncommitted changes in
`src/commands/scry/internal/search.rs`.**

### Patterns Observed
- **Scraper must respect .gitignore** — code scraper already used
  `ignore::WalkBuilder` but layer scraper used raw `walkdir`. Patina's own
  `scry` found the existing pattern (`src/paths.rs`, `extract_v2.rs`).
- **RRF-of-RRF dilutes signals** — adding rank-based fusion inside an oracle
  that feeds into rank-based outer fusion loses magnitude information twice.
  Score-based merging inside, rank-based outside is the correct architecture.
- **[[andrew-ng-over-shoulder]] caught the P@5 regression** — we would have
  shipped inner RRF if we hadn't asked "is the improvement real?" and looked
  at P@5. The test set validated P@10 but P@5 told a different story.
- **Spec-before-code discipline paid off** — user caught me about to write
  code without speccing the min-max approach first.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 3
- Commits: 5
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260207-153429-claude-start..session-20260207-153429-claude-end

## User Prompts (16)

1. `[Pasted text #1 +88 lines]`
2. `layer/dust is gitignore and has no damn plac in our work .. wtf thats the issue`
3. `we also have ignore right .. we need to be more smart here ALSO you were about to make a change w...`
4. `also dont we have a path or dir thing that helps us with paths?? feels we are once again building...`
5. `ok .. tldr what is going on .. what is andrew ng over the shoulder telling us`
6. `The honest question: Is the eval measuring the right thing, or are we chasing a metric?`
7. `/context `
8. `proceed with the RC2 fix`
9. `lets look at the P@5 drop before moving to phase 3`
10. `whats our over the shoulder andrew ng tell us`
11. `do it — swap inner RRF to min-max normalization BUT make sure to spec it .. and i want to have ...`
12. `thoughts on this analysis: [Pasted text #1 +119 lines]`
13. `so help me understand what you plan to do`
14. `what doesnt this fix?`
15. `[Pasted text #2 +118 lines] if you agree then lets spec this`
16. `we are going to run out of context .. is the spec written? if yes then we need to /session-end `
