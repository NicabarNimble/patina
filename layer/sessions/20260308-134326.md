---
type: session
id: 20260308-134326
title: V1 Working Lake — Spec Restructuring for Clear Implementation Path
status: archived
llm: claude
created: 2026-03-08T17:43:26Z
start_timestamp: 1772991806405
git:
  branch: patina
  starting_commit: 01681c5b833897080456500db1785a4fa4663bae
  start_tag: session-20260308-134326-claude-start
---

## Previous Session Context
Completed git history cleanup: removed `.git-backup/` via filter-repo (762MB → 20MB, 97% reduction), force-pushed both branches, and synced all 1,960 tags to GitHub. Fixed 121 stale `[[commit-*]]` references across 17 files using the filter-repo commit-map, and added a pre-commit hook to block files >10MB from entering history. Both PRs (#105, #106) merged to main; repo fully synced and backed up on GitHub.

## Goals
- [x] V1 Working Lake — Spec Restructuring for Clear Implementation Path

## Activity Log
### 13:43 - Session Start
Session initialized with goal: V1 Working Lake — Spec Restructuring for Clear Implementation Path
Working on branch: patina
Tagged as: session-20260308-134326-claude-start


### 14:40 - Spec restructuring complete

**Git Activity:**
- Commits this session: 8 ([[commit-9c76ee80]], [[commit-aef93f2b]], [[commit-73c6c964]], [[commit-89b3248d]], [[commit-1ca1692f]], [[commit-91f84902]], [[commit-3a224d9a]], [[commit-7c971ca8]])
- Files changed: 10 across 5 specs
- Working tree: clean

**Full audit of spec landscape.** Read all 5 relevant specs (SPEC.md + DESIGN.md), 3 archived specs from git tags, the [[connectors-own-tables-schemas-are-contracts]] belief, all 4 core principles, existing code in `children/github-connector/`, `src/broker/`, `src/commands/scrape/events.rs`, and `src/commands/oxidize/mod.rs`. Used `patina scry` and `patina assay` to verify data-architecture-v3 landscape.

**Findings — 3 gaps, 3 conflicts:**
- GAP: No spec defines raw lake storage format or layout
- GAP: No spec owns the v1 end-to-end lake path
- GAP: [[spec-connector-owns-tables]] conflates v1 and full multi-consumer architecture
- CONFLICT: connector-owns-tables data flow implies "lake events.db" — wrong for v1 (raw lake IS Parquet files, not another eventlog)
- CONFLICT: [[spec-data-architecture-v3]] marked complete but had 2 unchecked lake ECs
- CONFLICT: connector-owns-tables exit criteria mix v1 essentials with full architecture proofs

**Outside agent review** confirmed direction, sharpened 3 points:
1. Raw lake is "capture contract first" not "query path first"
2. Mother writes Parquet, not connector — connector emits domain records, Mother owns destination format
3. "Same child multi-scope" is desirable demonstration, not architectural requirement

**Spec changes made:**
- Created [[spec-raw-lake-ingestion]] — v1 proving slice with 8 exit criteria covering lake registration, Parquet capture, dedup, cursor tracking, and end-to-end litmus test ([[commit-7c971ca8]], [[commit-3a224d9a]])
- Narrowed [[spec-connector-owns-tables]] from 9 ECs to 4 (project-scope only), moved lake/block/transform to raw-lake-ingestion and future specs ([[commit-91f84902]])
- Fleshed out [[spec-lake-registry]] with 3-zone model, lake_registry/lake_sync table schemas, 3 exit criteria ([[commit-1ca1692f]])
- Cleaned up [[spec-data-architecture-v3]] — removed 2 unchecked lake ECs now owned by lake-registry ([[commit-89b3248d]])
- Updated [[spec-mother-maturation]] to reference raw-lake-ingestion ([[commit-73c6c964]])
- Added `identity_fields` to `children/github-connector/schema.toml` for dedup support ([[commit-aef93f2b]])

**Key architectural decision: responsibility split.**
- Connector owns: domain record shape, schema/version, identity fields for dedup, API behavior
- Mother owns: destination selection, lake registration, cursor truth, persona scoping
- Lakehouse child owns: Parquet format, file layout, dedup enforcement, append mechanics
- Shared: dedup (connector declares identity fields, lakehouse enforces), schema evolution

### 15:30 - Alignment and architectural correction

**Mother-child framing history scan.** Traced the evolution across sessions:
- Phase 1 (Nov 2025): "Mothership" as persona daemon
- Phase 2 (Jan 2026): Graph + registry + naming refactor
- Phase 3 (Feb 2026): **Wrong turn** — Mother v2 vision, inert scaffolding, reverted
- Phase 4 (Feb 2026): Children = plugins like Obsidian, MotherChild trait
- Phase 5 (Mar 2026): "Mother is connection and continuity"
- Phase 6 (Mar 2026): **Wrong turn** — pipe = process type (too vague, 3 contradictory protocols)
- Phase 7 (Mar 2026): **Correction** — pipe = protocol, children = managed services, Mother = broker

Pattern: vision-first architecture (wrong) → problem-first architecture (correct).

### 16:00 - Lakehouse child as real boundary

**Outside alignment agent** identified role-smearing in the initial spec: "Mother writes Parquet inline for v1" is a shortcut that makes Mother both control plane AND storage executor. Same mistake pattern as forge logic hardening inside core.

**Corrected:** Lakehouse child is a real child from day one ([[commit-03a50b6f]]). Clean role boundaries:
- Mother governs persona-scoped nodes
- Connector child = source-boundary adapter
- Lakehouse child = storage-boundary worker
- Transform child = derivation-boundary worker (future)

Added `pipe/ingest` method to pipe protocol: Mother sends batched records to lakehouse child, lakehouse writes Parquet and returns write receipt, Mother advances cursor after receipt.

Role alignment test (5 questions, all pass):
1. Replace connector without touching storage? Yes
2. Replace lakehouse without touching connector? Yes
3. Sync beliefs without exposing lakes? Yes
4. Persona isolation before sharing? Yes
5. Mother governing, not executing? Yes

**Beliefs captured:** [[mother-owns-destination-format]] (revised: Mother routes, lakehouse writes), [[raw-lake-is-capture-contract-first]] ([[commit-eebd7b48]])

**Lake-registry updated:** Added persona column for forward compatibility ([[commit-793d70ab]])

### 16:42 - Session end

**Next session pressure points (from alignment agent):**
1. [[spec-connector-owns-tables]] is next pressure point — if connectors are source-boundary adapters, then project materialization (tables, search, read models) should move to transform/materializer children
2. `pipe/ingest` needs hard specification — persona, source, lake, schema, records, provenance envelope → write receipt → cursor advance
3. SDK/protocol surface still encodes old world: `host.wit` defines emit as events.db write, `mother-child.wit` is broad daemon world not role-specific, `sources.rs` has no destination/persona model


## Beliefs Captured: 2

## Session Classification
- Work Type: pattern-work
- Files Changed: 12
- Commits: 11
- Patterns Modified: 9
- Beliefs Captured: 2
- Session Tags: session-20260308-134326-claude-start..session-20260308-134326-claude-end

## User Prompts (8)

1. `[Pasted text #1 +137 lines]`
2. `my outside agent has this to say about your plan: [Pasted text #2 +96 lines] lets discuss`
3. `reply from outside audit agent: [Pasted text #3 +73 lines]`
4. `yes and then tldr me with what you did`
5. `looking back on our layer.sessions we have bounced around the idea of mother child .. often we la...`
6. `working through some alignment with one of my agents ... i have noticed drift and this is helping...`
7. `from my outside alignment agent: [Pasted text #2 +69 lines]`
8. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
