---
type: session
id: 20260217-210850
title: patina-polymorphic-extraction — Session 2 of 3 (Phase C)
status: archived
llm: claude
created: 2026-02-18T02:08:50Z
start_timestamp: 1771380530698
git:
  branch: patina
  starting_commit: 9ee67d70dbd4b0890cf4c1d152312345460a2fc5
  start_tag: session-20260217-210850-claude-start
---

## Previous Session Context
Session 1 (Phases A+B) delivered the `ExtractedPayload` tagged union with `Code`/`Issue`/`PullRequest` variants and kind-based routing in the pipeline host. Two scalpel commits ([[commit-9d7ac11f]], [[commit-4fd8e948]]) across 4 files, 235 lines added. Deserialization is backward-compatible (no `kind` → fallback to `Code`), forge insert functions extracted as reusable public APIs, and 5 new deserialization tests added. All 423 tests pass, pre-push checks clean.

## Goals
- [x] Phase C: Forge connector separation — write JSON to staging instead of direct DB insert
- [x] Phase C: Create `grammar-forge` WASM plugin to claim staged JSON format
- [x] Phase C: Extend `extract_v2` to scan staging tree alongside source tree
- [x] All tests pass, pre-push checks pass

## Activity Log
### 21:08 - Session Start
Session initialized with goal: patina-polymorphic-extraction — Session 2 of 3 (Phase C)
Working on branch: patina
Tagged as: session-20260217-210850-claude-start

### 21:12 - Context Loading
Read all required files per build prompt: [[dependable-rust]], [[unix-philosophy]],
[[adapter-pattern]], [[patina-identity]], [[patina-polymorphic-extraction]] SPEC,
[[fact-schema-registry]] SPEC, [[fact-crdt-substrate]] SPEC. Read code:
`src/commands/scrape/forge/mod.rs`, `src/commands/scrape/code/extract_v2.rs`,
`src/commands/scrape/code/extracted_data.rs`, `src/forge/types.rs`, `src/paths.rs`,
`src/forge/sync/internal.rs`, `src/forge/sync/mod.rs`, `src/commands/scrape/database.rs`.
Read grammar plugin patterns: `grammars/go/plugin.toml`, `grammars/go/Cargo.toml`,
`grammars/go/src/lib.rs`, `src/plugin/internal/pipeline.rs`, `src/plugin/internal/mod.rs`,
`plugins/sdk/src/pipeline.rs`.

Key design decisions from reading code:
- Plugin dispatch is by file extension via `pipeline_plugins.get(ext)` where ext comes
  from `Path::extension()`. Chose `.forge-issue` / `.forge-pr` extensions for staging files.
- `Language::from_path` filters source tree walk — staged files need separate walk.
- Sync module writes directly to DB — left as-is for Session 3 cleanup.

### 21:20 - Forge connector writes to staging
[[commit-208a9742]] — Changed `run()` in `src/commands/scrape/forge/mod.rs` to write
JSON files to `.patina/local/data/forge/{owner}-{repo}/` instead of inserting directly.
Added `write_issues_staging()` and `write_prs_staging()` functions. Each issue →
`{number}.forge-issue`, each PR → `{number}.forge-pr`. Removed `insert_issues`/
`insert_prs`/FTS5 calls from `run()`. Kept incremental timestamps and sync.

### 21:23 - grammar-forge WASM pipeline plugin
[[commit-319c7f77]] — Created `grammars/forge/` with `plugin.toml`, `Cargo.toml`,
`src/lib.rs`. Pure compute: reads staged JSON, injects `"kind": "issue"` or
`"kind": "pull-request"`, returns for host routing. Claims `languages = ["forge-issue",
"forge-pr"]`. Built to wasm32-wasip2, installed to `~/.patina/pipeline/grammar-forge/`.
231KB compiled WASM — no tree-sitter needed.

### 21:25 - extract_v2 scans staging tree
[[commit-0994418b]] — Added second `WalkBuilder` walk over
`paths::project::data_dir(work_dir).join("forge/")` in `extract_code_metadata_v2`.
Staged files dispatched directly to pipeline plugins by extension (no `Language`
detection). Handles missing staging dir gracefully.

### 21:27 - Dead code fix (project belief: no #[allow(dead_code)])
[[commit-9921e87d]] — `InsertStats.skipped` was unread after removing the old `run()`
print statements. Per [[dead-code-is-design-smell]] belief, added `forge_skipped`
tracking in extract_v2 pipeline routing instead of suppressing the warning. Print
"(N unchanged)" when dedup skips items.

### 21:30 - FTS5 preservation fix
[[commit-ad280ad9]] — Discovered `populate_fts5()` in `database.rs` was doing
`DELETE FROM code_fts` (wiping forge entries). Two fixes:
1. Scoped DELETE to `WHERE event_type LIKE 'code.%'` (preserves forge entries)
2. Moved forge FTS5 population to `code/mod.rs` after code FTS5 rebuild (idempotent)

### 21:32 - End-to-end verification
Live test on Patina repo:
- `patina scrape forge` → 14 issues + 78 PRs staged as `.forge-issue`/`.forge-pr` files
- `patina scrape` → grammar-forge processes 92 staged files → inserts to DB → FTS5 indexed
- `sqlite3` verification: `forge_issues` = 14, `forge_prs` = 78, FTS5 = 14 + 78
- 423 tests pass, `pre-push-checks.sh` passes

### Session 2 Complete
Phase C delivered as 5 scalpel commits. Two-verb flow proven end-to-end:
`patina scrape forge` (fetch → staging) + `patina scrape` (staging → pipeline → DB).

Session 3 scope: Phase D (end-to-end verification, sync migration to staging, cleanup,
spec status → complete).

## Beliefs Captured: 0
_No new beliefs — existing [[dead-code-is-design-smell]] belief was applied._


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: feature
- Files Changed: 7
- Commits: 5
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260217-210850-claude-start..session-20260217-210850-claude-end

## User Prompts (3)

1. `[Pasted text #1 +139 lines]`
2. `we have a belief on allow dead code`
3. `/session-end`
