---
type: session
id: 20260217-053457
title: belief-graph review before build
status: archived
llm: claude
created: 2026-02-17T10:34:57Z
start_timestamp: 1771324497236
git:
  branch: patina
  starting_commit: b28d1400639838ffe16f251ea736ed03c0881a36
  start_tag: session-20260217-053457-claude-start
---

## Previous Session Context
Completed code-grounded review pass 6 of the belief-graph SPEC (4 findings fixed: defeated behavior, edge rebuild timing, sync dedup bug, edge write separation), then fixed 3 regressions from an outside agent review (stale `## Origin` reference, missing `imported` column spec, sync projection gap) and restructured the SPEC from 860→496 lines by condensing review history and consolidating SQL blocks. Final commits added `imported` column to graph.db DDL and `belief_applied_in` per-source rebuild pattern. SPEC has had 7 review passes with 30+ findings all resolved — ready for Phase A implementation.

## Goals
- [x] Code-grounded review of [[belief-graph]] SPEC against all 6 source files
- [x] Validate SPEC reuse map claims against actual code
- [x] Plan build strategy: split into 2 sessions (A+B+C / D+E)
- [x] Write self-contained continuation prompt for Session 1 build

## Activity Log
### 05:34 - Session Start
Session initialized with goal: belief-graph review before build
Working on branch: patina
Tagged as: session-20260217-053457-claude-start

### 05:45 - Code-Grounded SPEC Review

**Files read and verified against SPEC:**
- `src/mother/graph.rs` — confirmed `KnowledgeEntry` struct (7 fields), `knowledge`/`knowledge_search` DDL, 3 methods to rename, `sanitize_fts5_query()`, test coverage
- `src/mother/mod.rs` — confirmed `KnowledgeEntry` re-export at line 39
- `src/commands/mother/graph.rs` — confirmed `collect_project_beliefs()` reads 5 columns, `sync_from_registry()` has dedup bug (current project + registry double-count)
- `src/commands/mother/mod.rs` — confirmed `GraphCommands` enum structure, `MotherCommands::Search`
- `src/commands/scrape/beliefs/mod.rs` — confirmed `extract_file_metrics()` parses `## Attacks`/`## Attacked-By` with `wikilink_re`, defeated entries silently skipped (line 412-418), `BeliefMetrics` has `attacks_ids`/`attacked_by_ids` but no `supports_ids`, `create_materialized_views()` structure
- `src/mcp/server.rs` — confirmed `handle_mother_search()` at line 1774, `mother` tool description, current `query`+`limit` only
- `src/commands/belief/mod.rs` — confirmed `BeliefCommands` has only `Audit` variant

**SPEC accuracy:** All reuse map claims verified correct. File paths, line references, and existing pattern descriptions match the code. No new findings.

### 06:00 - Build Strategy Decision

Decided: **one SPEC, two sessions**.

- Session 1: Phases A + B + C (infrastructure — beliefs as graph nodes, edge tables, richer sync)
- Session 2: Phases D + E (features — query traversal, import command)

Rationale: phases cross-reference each other so splitting the spec creates drift risk. But 5 phases is too much for one session — context degrades. Natural boundary: data shape (A-C) vs user tools (D-E).

### 06:10 - Continuation Prompt

Wrote self-contained prompt for Session 1 build. Includes:
- All 3 layer/core values anchored
- Exact file paths and line references per phase
- Exit criteria for Session 1
- Build recipe and anti-patterns


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: exploration
- Files Changed: 0
- Commits: 0
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260217-053457-claude-start..session-20260217-053457-claude-end

## User Prompts (5)

1. `anchor in layer/core values. read code before write code. gather context from recent git history ...`
2. `tldr what this spec builds?`
3. `should ths spec be split into 2 specs to allow context when we build or is it fine as it is and w...`
4. `We will build belief-graph spec over the next two sessions. write a prompt (print to screen) that...`
5. `/session-end`
