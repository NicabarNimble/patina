---
type: session
id: 20260216-211931
title: 'Session Goal: implementability review of belief-graph SPEC'
status: archived
llm: claude
created: 2026-02-17T02:19:32Z
start_timestamp: 1771294772000
git:
  branch: patina
  starting_commit: cfb0234f45a1bdb0f5fa4f96fab415494f51ae9a
  start_tag: session-20260216-211931-claude-start
---

## Previous Session Context
Real-world tested mother MCP from another project, fixing 3 bugs: FTS5 query sanitization (column name collision), per-source rebuild (cross-project sync was wiping other projects' data), and error message improvement. Then pivoted to a fundamental architecture rethink — mother was doing FTS5 search (duplicating assay) instead of graph routing. Collaborated on designing belief-graph architecture: beliefs as first-class graph nodes with SUPPORTS/ATTACKS edges, human-driven cross-project import with provenance. Wrote and promoted the belief-graph SPEC, completed and archived cross-project-beliefs spec, released v0.24.0.

## Goals
- [x] Implementability review of [[belief-graph]] SPEC (3 passes, 17 findings, all fixed)

## Activity Log
### 21:19 - Session Start
Session initialized with goal: implementability review of [[belief-graph]] SPEC
Working on branch: patina
Tagged as: session-20260216-211931-claude-start

### 21:45 - Review Pass 1 [[commit-3478e3a4]]

**Git Activity:**
- Commits: 1
- Files changed: 1 (`layer/surface/build/feat/belief-graph/SPEC.md`)

Read all 12 referenced files (SPEC, 4 layer/core docs, 5 source files,
1 belief, 1 session archive) and checked 8 criteria:

**Issues found and fixed (4):**
1. **Scraper `## Supports` parsing gap** — Reuse Map falsely claimed scraper
   parses `## Supports`. Verified `extract_file_metrics()` only handles
   `## Attacks`/`## Attacked-By`. Updated 6 sections.
2. **Redundant `from_source` column** — edge tables had both `from_source`
   and `source_project` (always identical). Simplified PK.
3. **`belief_applied_in` deferral** — derivable from `beliefs.source` before
   Phase E. Added deferral note.
4. **Per-source rebuild column specificity** — added explicit column names
   for edge deletion queries.

**Verified OK:** schema correctness, code path accuracy, CLI integration,
`KnowledgeEntry` → `BeliefEntry` rename path, exit criteria testability,
Phase B feasibility (Option 2).

### 22:15 - Review Pass 2 [[commit-f224e854]]

**Git Activity:**
- Commits: 1
- Files changed: 1 (same file, +151/-8)

Addressed 8 user-identified design gaps:

5. **Schema migration path** — enumerated all 5 files with `knowledge*`
   references, mandated single-commit rename, added DROP sequence,
   documented backward compatibility (old binary → empty tables, no crash).
6. **Schema version guard** — table-existence check for patina.db
   relationship tables, matching existing `collect_project_beliefs()` pattern.
7. **`## Supports` parsing semantics** — audited 131 belief files: 228/234
   entries (97.4%) use `[[wikilink]]`, 6 use bare `belief-name:` format.
   Spec'd wikilink-first extraction with bare ID fallback + stderr diagnostic.
8. **Project identity stability** — added Project Identity section: registry
   key is canonical, `file_name()` for unregistered, future escape hatch
   (`project_id` in config.toml).
9. **Phase D/E timing** — `query projects` derives from `beliefs.source`
   until Phase E creates `belief_applied_in`.
10. **Import path discovery** — resolve via `Graph::get_node(project).path`,
    actionable errors.
11. **Scrape-after-import** — import writes markdown only, prints reminder.
12. **MCP validation** — -32602 for unknown modes, `belief_id` param
    requirement for supports/attacks modes.

### 22:50 - Review Pass 3 [[commit-7d8b4e61]]

**Git Activity:**
- Commits: 1
- Files changed: 1 (same file, +132/-6)

Resolved 5 interconnected structural concerns:

13. **Belief ID ambiguity in edge tables** — edges model *conceptual*
    relationships per Dung's framework, not project-scoped instances.
    Same-ID collisions are naming bugs surfaced by query commands.
    Anchored in [[belief-identity-is-slug-not-hash]].
14. **`defeated` flag semantics** — full truth table mapping `(status:
    defeated)` from both `## Attacks` and `## Attacked-By`. Both describe
    same edge from opposite perspectives. Scraper needs to emit defeated
    entries as edge rows (currently only counted).
15. **Import path portability** — explicit local-first constraint, no remote
    fetch, actionable error for stale paths.
16. **`belief_applied_in` write path** — concrete data flow: scraper detects
    `## Origin` → `imported` flag → sync populates `originated` column.
17. **Dangling edge validation** — post-sync validation query, warn-not-delete
    policy (target may exist in unsynced project).

**Key patterns observed:**
- Each review pass surfaced progressively deeper issues. Pass 1 found
  factual errors (scraper doesn't parse ## Supports). Pass 2 found
  operational gaps (how does migration actually work?). Pass 3 found
  semantic ambiguities (what does a belief ID mean in an edge?).
- The [[ground-assertions-or-pay-review-tax]] belief proved itself:
  every claim that was grounded against code survived review. Every
  ungrounded claim was wrong (e.g., "scraper already parses ## Supports").
- SPEC grew from 293 to 659 lines across 3 passes. The review notes
  section (lines 544-659) is a permanent audit trail.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 1
- Commits: 3
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260216-211931-claude-start..session-20260216-211931-claude-end

## User Prompts (4)

1. `[Pasted text #1 +90 lines]`
2. `[Pasted text #1 +8 lines]`
3. `[Pasted text #1 +6 lines]`
4. `We will review belief-graph spec next session. write a prompt (print to screen) that will allow u...`
