---
type: session
id: 20260309-065645
title: Seam 2 Phase 4 — Generic Projection Engine (connector-owns-tables)
status: archived
llm: claude
created: 2026-03-09T10:56:45Z
start_timestamp: 1773053805570
git:
  branch: patina
  starting_commit: 8f09e4df45ca288d315de3e2f7160c60ea8d0a6b
  start_tag: session-20260309-065645-claude-start
---

## Previous Session Context
Last session "Seam 2 Phase 3: Write-Side Event-Type Source-of-Truth Migration" (20260308-233938) completed 5 commits migrating the write-side from hardcoded forge event types to schema-driven resolution. Key work: unified `insert_issues()`/`insert_prs()` to accept `event_type` parameter, added `resolve_event_type()` with priority-based ordering (active connectors with `[[contracts]]` get priority=1 over legacy schemas), replaced hardcoded doctor emission coverage with runtime schema-driven types, and extended eventlog migration to cover all schema prefixes. An external audit caught a P1 where alphabetical ordering kept `forge.*` as the write label — fixed by adding `priority` column to `schema_registry`. Belief `contracts-signal-active-writer` captured. Next: generic projection engine to replace hardcoded `create_materialized_views()` and `project_from_events()` with schema-driven SQL. Table rename decision (forge_* → github_*) flagged as requiring up-front resolution.

## Goals
- [x] Build generic projection engine (schema-driven CREATE TABLE + INSERT from `[[projections]]`)
- [x] Wire projection engine into scrape pipeline (replace `project_from_events()` + `create_materialized_views()`)
- [x] Handle forge→github table rename migration (one-time `ALTER TABLE RENAME`)
- [x] Replace hardcoded FTS5 with generic FTS5 engine reading `[[indexes]]` (already partially done)
- [x] Build contract registry from `[[contracts]]` (replace `ends_with(".pr")` in enrichment/search)
- [x] Delete connector-specific domain code from events.rs (domain types, hardcoded DDL, hardcoded projection SQL)
- [ ] Verify all exit criteria: schema-drives-projection, schema-drives-search, core-has-no-connector-knowledge, domain-change-schema-only
- [ ] CARRY FORWARD: commit uncommitted consumer changes (search, enrichment, oxidize, offsets, plugin tests, projection patina.db migration)

## Activity Log
### 06:56 - Session Start
Session initialized with goal: Seam 2 Phase 4 — Generic Projection Engine (connector-owns-tables)
Working on branch: patina
Tagged as: session-20260309-065645-claude-start


### 07:45 - Update: Generic projection engine built and wired (3 commits)

**Git Activity:**
- Commits this session: 3 ([[commit-0b404dcf]], [[commit-7ebbc635]], [[commit-f349cc7f]])
- Files changed: 6 (`children/github-connector/schema.toml`, `src/commands/scrape/projection.rs`, `src/commands/scrape/mod.rs`, `src/commands/scrape/events.rs`, `src/commands/scrape/code/extract_v2.rs`, `src/commands/scrape/code/mod.rs`)
- Working tree: 8 files with uncommitted changes (all compile, all tests pass)

**Work completed:**

1. [[commit-0b404dcf]]: Schema alignment — deleted `.patina/schemas/forge/`, updated installed github schema and child source. `[[indexes]]` now point to `github_issues`/`github_prs` (not `forge_*`). `[[projections]]` expanded to include all consumer-needed columns: labels, author, url, linked_issues, approvals. Embedding offset_slot stays at 5.

2. [[commit-7ebbc635]]: Generic projection engine — new `src/commands/scrape/projection.rs`. `project_from_schemas()` reads `[[projections]]` from all installed schemas and generates CREATE TABLE DDL + INSERT OR REPLACE SQL mechanically from column declarations and json_paths. Auto-adds event_seq/ingested_at provenance columns. Includes one-time forge→github migration: renames forge_* tables to github_*, updates forge.* event types to github.* in events.db, drops forge_refs. Schema registry moved here from events.rs (without priority column). 9 tests.

3. [[commit-f349cc7f]]: Pipeline rewire — `extract_v2.rs` calls `project_from_schemas()` instead of the old `project_from_events()`. `insert_issues()`/`insert_prs()` simplified: write directly to `github_issues`/`github_prs` with fixed `github.issue`/`github.pr` event types. Removed all forge compatibility from events.rs: `create_materialized_views()`, `project_from_events()`, `resolve_event_type()`, `resolve_dedup_event_types()`, shared-table dedup, priority column logic, schema_registry functions. events.rs: ~1250 → ~430 lines.

**Uncommitted but compiling + passing (8 files):**

4. Consumer contract-driven display: `enrichment.rs` and `search.rs` replaced `ends_with(".pr")`/`ends_with(".issue")` with `display_kind_for_event_type()` that loads `[[contracts]]` from installed schemas. `search.rs` replaced `LIKE '%.issue' OR LIKE '%.pr'` with generic `1=1` filter for connector-contributed FTS5 rows.

5. Schema corpus queries: `oxidize/mod.rs` replaced hardcoded `LIKE '%.issue' OR LIKE '%.pr'` corpus query with iteration over installed schemas' `[embedding].corpus_query` — fully generic.

6. `offsets.rs` updated slot 5 comment from "forge" to "connector events". `FORGE_ID_OFFSET` name kept for embedding index compatibility.

7. Plugin test fixtures updated from forge → github (schema name, event types, assertions).

8. `projection.rs` added `migrate_forge_events_in_patina()` for patina.db eventlog migration.

**Key decisions:**
- User directive: remove forge entirely, no legacy support. Real migration of old forge.* events to github.*.
- Generic projection engine uses schema declarations only — zero hardcoded table/column names.
- `display_kind_for_event_type()` loads `[[contracts]]` from schemas at query time — avoids contract_registry DB table for now.
- `FORGE_ID_OFFSET` constant name preserved to avoid invalidating existing embedding indices.

**Validation:**
- 643 tests pass (316 + 311 + 10 + 6), zero warnings
- Build: `cargo build --release` clean
- Pre-push checks: NOT YET RUN (uncommitted changes need commit first)

**What's left for next session:**
- Commit the 8 uncommitted files (items 4-8 above)
- Run pre-push checks
- Live test: `patina scrape code` end-to-end
- Update 3 comments in host_support.rs and doctor.rs
- Verify exit criteria and mark checked on [[spec-connector-owns-tables]]
- Continuation prompt written and printed to screen


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: feature
- Files Changed: 6
- Commits: 3
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260309-065645-claude-start..session-20260309-065645-claude-end

## User Prompts (4)

1. `[Pasted text #1 +142 lines]`
2. `Tell the builder this:

Use Option B, but remove \`forge\` compatibility entirely. We are pre-v1, s...`
3. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
4. `/session-end`
