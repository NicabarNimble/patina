---
type: session
id: 20260208-101655
title: Semantic-Structural Split
status: archived
llm: claude
created: 2026-02-08T15:16:55Z
start_timestamp: 1770563815560
git:
  branch: patina
  starting_commit: 69d377ceac500c2f21be7b6ff33f17ba80eda3d4
  start_tag: session-20260208-101655-claude-start
---

## Previous Session Context
Identified that scry's retrieval tuning difficulties (6 sessions of cascading fixes) trace back to a root cause: scry mixes semantic and structural concerns in one pipeline — 5 oracles, 3 FTS5 tables, RRF fusion — making every fix expose the next layer of mixing complexity. Wrote the [[semantic-structural-split]] spec (5 phases: move factual oracles to assay, build knowledge semantic domain, consumer-level fusion, eval redesign, discover more domains). Captured [[beliefs-are-entities-not-documents]] belief. Key design decision: multi-domain semantic architecture where domains are earned through measurement, not declared by fiat.

## Goals
- [ ] Implement Semantic-Structural Split (Phase 1) — ~70% complete

## Activity Log
### 10:16 - Session Start
Session initialized with goal: Semantic-Structural Split
Working on branch: patina
Tagged as: session-20260208-101655-claude-start

### 10:20-10:45 — Read all key files
Read every file listed in [[semantic-structural-split]] spec Phase 1:
- `src/commands/assay/mod.rs` — current assay interface (structural queries only)
- `src/commands/scry/mod.rs` — scry with 5 oracles + RRF fusion
- `src/commands/scry/internal/search.rs` — `scry_lexical`, `normalize_table`
- `src/commands/scry/internal/hybrid.rs` — 5-oracle orchestrator
- `src/retrieval/engine.rs` — QueryEngine (parallel oracle dispatch)
- `src/retrieval/fusion.rs` — RRF fusion
- `src/retrieval/intent.rs` — intent weights for cross-oracle balance
- All 5 oracle implementations in `src/retrieval/oracles/`
- MCP server handler for scry and assay tools

### 10:45-11:10 — Commit [[ae7f971f]]: `assay search`
Created `src/commands/assay/internal/search.rs` — ranked FTS5 search
with per-table min-max normalization (moved from scry's `scry_lexical`).
Created `src/commands/assay/internal/query_prep.rs` — FTS5 query
preparation (moved from scry). Added `Search` variant to `QueryType`
enum. Wired through CLI (`assay search <query>`) and MCP server.
All tests pass, clippy clean.

### 11:10-11:25 — Commit [[d58e7435]]: `assay cochange`
Created `src/commands/assay/internal/temporal.rs` — co-change analysis
logic (moved from `src/retrieval/oracles/temporal.rs`). Added `Cochange`
variant. Wired through CLI (`assay cochange <file>`) and MCP.
All tests pass, clippy clean.

### 11:25-11:45 — Commit [[f6f1f83d]]: `assay belief`
Created `src/commands/assay/internal/belief.rs` — belief grounding with
evidence lookup, reached code files, related beliefs via co-citation.
FTS5 belief search for keyword matching. Added `Belief` variant.
Wired through CLI (`assay belief <id>`) and MCP.
All tests pass, clippy clean.

### 11:45-12:10 — Scry simplification (IN PROGRESS, NOT COMPILED)
Began rewriting `src/retrieval/engine.rs` to semantic-only QueryEngine.
Removed factual oracle imports, simplified to wrap SemanticOracle directly.
Updated `src/retrieval/oracles/mod.rs` to only export SemanticOracle.
Removed `pub mod intent` from `src/retrieval/mod.rs`.
Switched `src/commands/context.rs` from BeliefOracle to assay FTS.

**TWO COMPILE ERRORS REMAIN:**
1. `src/commands/eval/mod.rs:14` — `use crate::retrieval::intent::detect_intent` (module removed)
2. `src/retrieval/fusion.rs` — `use super::intent::IntentWeights` (module removed)

**Fix path:** Remove intent imports from eval and fusion. Fusion's
`rrf_fuse_weighted` is no longer called — either remove or keep types
without intent dependency. Then clean up unused oracle files.

### Patterns Observed
- **Additive before reductive** — the spec's "no flag day" approach worked
  well. Adding assay search/cochange/belief first (3 clean commits) before
  removing from scry meant each commit was independently shippable.
- **Oracle trait is a good seam** — the Oracle trait made it easy to identify
  exactly what code was factual vs semantic. Each oracle was self-contained
  with clear data sources and scoring types.
- **Context window pressure** — ran out of context before completing the
  scry simplification. The continuation prompt captures exact error state
  for the next session to pick up without re-reading everything.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: feature
- Files Changed: 8
- Commits: 3
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260208-101655-claude-start..session-20260208-101655-claude-end

## User Prompts (4)

1. `[Pasted text #1 +77 lines]`
2. `write a prompt (print to screen) that will allow us to continue next session where we will not ha...`
3. `re out of context i need a prompt`
4. `/session-end `
