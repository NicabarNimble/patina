---
type: session
id: 20260218-192625
title: launching patina from remote issues
status: archived
llm: claude
created: 2026-02-19T00:26:25Z
start_timestamp: 1771460785928
git:
  branch: patina
  starting_commit: 31c34e58650add4909f925d45045bda989585652
  start_tag: session-20260218-192625-claude-start
---

## Previous Session Context
Completed spec-launcher-tmux: all 9 exit criteria passed during live testing from Ghostty. Fixed a UX issue where tmux reattach was mirroring instead of taking over the session — added `-D` flag to `tmux new-session -A` (commit 886dce47). Also confirmed mouse scrolling, `--no-tmux`, `PATINA_TMUX=0`, and no-nesting behaviors all work correctly. Spec marked complete (commit 31c34e58).

## Goals
- [x] Research remote launch auth issues (Claude Code SSH, setup-token)
- [x] Draft [[spec-launcher-auth]] — Long-Lived Token via Patina Secrets
- [x] Address 6 review concerns and move spec to active
- [ ] Implement spec-launcher-auth (deferred to next session)

## Activity Log
### 19:26 - Session Start
Session initialized with goal: launching patina from remote issues
Working on branch: patina
Tagged as: session-20260218-192625-claude-start

### 19:30 - Research Phase
Investigated the remote launch auth problem from multiple angles:
- Searched yolo context via `patina scry` and session history — yolo solved container auth via `.credentials.json` bind-mount + `--dangerously-skip-permissions`, but that's container-specific
- Fetched [GitHub issue #25661](https://github.com/anthropics/claude-code/issues/25661) — Claude Code SSH ignores identity files, workaround is `ssh-add`. Fix incoming next release.
- Reviewed [article on Claude Code SSH](https://androidstudio.substack.com/) — confirms tmux pattern we already built with [[spec-launcher-tmux]]
- Discovered `claude setup-token` command — generates 1-year OAuth token (`sk-ant-oat01-...`) for headless auth, bills against Max subscription via `CLAUDE_CODE_OAUTH_TOKEN` env var
- Confirmed `--dangerously-skip-permissions` is yolo-only (line 136 in `src/commands/yolo/mod.rs`), NOT in the launcher

### 19:50 - Spec Drafted
Created [[spec-launcher-auth]] at `layer/surface/build/fix/spec-launcher-auth/SPEC.md`:
- Type: fix (patch to existing launcher)
- Core flow: `claude setup-token` → patina secrets vault (age encrypted) → launcher injects `CLAUDE_CODE_OAUTH_TOKEN` when adapter == "claude"
- Claude-adapter-only gating, no permissions bypass
- Graceful fallback on every failure path

### 20:00 - Spec Review (6 concerns addressed)
User review surfaced 6 design concerns, all resolved:
1. **Token creation UX** — requires browser once, documented constraint
2. **Secret lookup surface area** — new `get_global_secret()` helper, never touches project vaults
3. **Vault unlock timing** — Touch ID fires before exec, platform table added
4. **Env injection path** — `let mut cmd` refactor spelled out with code
5. **Conflict guard semantics** — checks `ANTHROPIC_API_KEY` + pre-existing `CLAUDE_CODE_OAUTH_TOKEN`, warns to stderr
6. **Token expiry detection** — v1 accepts annual re-auth, defers `patina doctor` check

Added warning timing fix: all stderr warnings print before tmux takes over, with explicit flush.

### 20:15 - Spec Ready → Active
Moved [[spec-launcher-auth]] to ready, then active. Wrote continuation prompt for next session implementation.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 1
- Commits: 1
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260218-192625-claude-start..session-20260218-192625-claude-end

## User Prompts (16)

1. `tmux fixed the pickup where i left off from a remote computer or phone BUT I still have this logi...`
2. `https://github.com/anthropics/claude-code/issues/25661`
3. `[Pasted text #1 +390 lines]`
4. `i cant use my max account`
5. `how could that work with patina secrets?`
6. `https://www.reddit.com/r/Anthropic/comments/1qxplzf/confused_about_claude_setuptoken_billing_with...`
7. `lets spec this out before we wire it ... also patina launcher has option to switch adapter and so...`
8. `yeah i think we'll rewrite how yolo is done later .. for now its launcher and secrets and long-li...`
9. `so walkme through what this spec builds`
10. `whats the spec location again`
11. `[Pasted text #1 +12 lines]`
12. `Read through SPEC.md again—nice job folding in all six fixes. The added platform table and onbo...`
13. `move to active or whatever is correct patina spec --help `
14. `can we build this in a single session?`
15. `write a prompt (print to screen) that will allow us to continue next session where we will
  not ...`
16. `/session-end `
