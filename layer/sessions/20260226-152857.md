---
type: session
id: 20260226-152857
title: 'Postmortem: data-architecture-v2 Vision + data-db-split Implementation'
status: archived
llm: claude
created: 2026-02-26T20:28:57Z
start_timestamp: 1772137737800
git:
  branch: patina
  starting_commit: 86b9e774b7b999b648e16e8f1b8f01fd6eca4fde
  start_tag: session-20260226-152857-claude-start
---

## Previous Session Context
Addressed all 11 gaps identified in data-architecture-v2 across 8 commits: cleaned up SPEC.md (event registry status column, grounded token-aware serving principle, replaced untestable exit criterion), resolved 4 DESIGN.md open questions, and filled out both skeleton sub-specs (data-emission-completeness with 7 gaps mapped to file locations, data-mother-schema with 10-column expansion plan). Promoted data-architecture-v2 from draft to ready with sub-specs (data-db-split, data-emission-completeness, data-mother-schema) set as formal blockers. data-db-split was subsequently completed in a separate session (v0.33.1 release).

## Goals
- [x] Part 1-3: Architecture, Spec, and Implementation audit (22 findings)
- [x] Create fix spec for postmortem gaps (data-db-split-fixes)
- [x] Capture missing belief (correctness-by-construction-not-convention)
- [x] Add cross-db patterns + deferred type-safety to vision DESIGN.md
- [ ] Part 4-5: Belief audit + process reflection (in progress)
- [ ] Draft spec-audit-gate (created, content pending)

## Activity Log
### 15:28 - Session Start
Session initialized with goal: Postmortem: data-architecture-v2 Vision + data-db-split Implementation
Working on branch: patina
Tagged as: session-20260226-152857-claude-start


### 16:23 - Update (covering since 15:28)

**Git Activity:**
- Commits this session: 7 ([[commit-cd4f2748]], [[commit-c98056bf]], [[commit-741d6646]], [[commit-e7cb5fce]], [[commit-fa288da3]], [[commit-11709699]], [[commit-fe1c65fa]])
- Files changed: 7 (`SPEC.md`, `DESIGN.md`, `SPEC.md`, `SPEC.md`)
- Last commit: 8 seconds ago

**Work completed:**

Full postmortem of [[spec-data-architecture-v2]] vision + [[spec-data-db-split]]
implementation. Read every line of: 3 core value files, both specs (vision +
implementation via git show), 5 beliefs, full implementation diff (981 lines
across 14 src files), 4 session records, 2 remaining sub-specs.

*Postmortem findings (22 total: 16 good, 4 concerns, 2 action items):*

Architecture audit found solid unix-philosophy decomposition (5 areas, clean
dependency graph) and dependable-rust compliance (API surface got simpler with
emit() losing its conn parameter). Safety-boundaries mostly held — rebuild
guards events.db correctly — but two gaps: silent degradation on corruption
(`let _ =` swallowing write failures) and no recovery path (JSONL replica
designed but unimplemented).

*Fix spec created: [[spec-data-db-split-fixes]] ([[commit-11709699]], [[commit-c98056bf]])*

6 steps, 11 exit criteria. Synthesized from two expert perspectives:
- Schickling (LiveStore): JSONL durability replica (`layer/events.jsonl` export,
  auto-export on session end, import for recovery, doctor staleness check)
- Gjengset (Rust for Rustaceans): loud write failures via `emit_or_warn()` helper,
  TOCTOU fix with idempotent migration, `OnceLock` init once per process,
  thread-local connection reuse for MCP server hot path, `PRAGMA quick_check`

Set [[spec-data-emission-completeness]] blocked by this fix spec — Area 2
can't start until the event store is durable.

*Belief captured: [[correctness-by-construction-not-convention]] ([[commit-741d6646]])*

"Safety properties should be enforced by data layout rather than code
conventions." The pivotal insight from [[session-20260226-094014]] that drove
the entire db-split — file separation makes the wrong thing (destroying events
during rebuild) impossible because rebuild never opens events.db.

*Vision DESIGN.md expanded ([[commit-cd4f2748]]):*

Two new cross-cutting decision sections:
- Cross-database access patterns: ATTACH (JOINs), dual-conn (dual-write),
  sequential-open (independent reads) with current consumers and when to use each
- Deferred type-safety improvements: event type enum (trigger: registry stabilizes)
  and ATTACH newtype (trigger: second consumer appears), with interim
  `fn attach_events()` helper suggestion

*Draft spec created: [[spec-spec-audit-gate]] ([[commit-fe1c65fa]])*

Adds `audit` state to spec lifecycle (draft → ready → active → audit → complete).
Content pending — parked to focus on postmortem findings first.

**Discussion context:**

Session structured as conversational postmortem — findings discussed before
writing, not written then reviewed. User directed: "lets discuss this instead
of writing it down." Schickling and Gjengset consulted as design advisors —
their perspectives shaped the fix spec's steps. User refined Non-Goals section
with precise trigger conditions for deferred type-safety work.

**Key decisions:**
- Fix spec blocks Area 2 — durability is foundation, not polish
- Three cross-db patterns are intentional (not inconsistency) — each fits its use case
- Deferred type-safety has explicit triggers, not vague "someday"
- `emit_or_warn()` centralizes error policy instead of fixing 7+ call sites independently
- `PRAGMA quick_check` over `integrity_check` for routine doctor checks (speed)


### 18:46 - Update (covering since 16:23)

**Git Activity:**
- Commits this session: 9 ([[commit-e3cdab01]], [[commit-f252ad86]], [[commit-cd4f2748]], [[commit-c98056bf]], [[commit-741d6646]], [[commit-e7cb5fce]], [[commit-fa288da3]], [[commit-11709699]], [[commit-fe1c65fa]])
- Files changed: 9 (`SPEC.md`, `DESIGN.md`, `SPEC.md`, `SPEC.md`)
- Last commit: 7 minutes ago

**Work completed:**

Continued postmortem discussion — outside agent reviewed [[spec-data-db-split-fixes]]
and raised 9 implementation concerns. Resolved 5 directly in the spec:

- Export atomicity: at-least-once pattern (JSONL-first, marker-second), crash-safe
- Import dedup: seq as PRIMARY KEY via `INSERT OR IGNORE` (not brittle timestamp triple)
- Session-end export: best-effort, warn on failure, don't block archival
- Doctor staleness: compare max seq (not line counts), deterministic
- Tracking schema: `scrape_meta` with key `last_exported_seq`, import doesn't touch it

Remaining 2 concerns (MCP warning channel, thread-local connection reuse) revealed
a broader gap: the MCP server has no observability infrastructure at all. Created
[[spec-mcp-server-hardening]] ([[commit-f252ad86]]) to address holistically. Cut
Step 4 (connection reuse) and MCP-specific parts of Step 2 from fix spec — those
belong in MCP hardening, not a durability fix.

Investigated actual MCP codebase: confirmed no structured logging, no warning
channel, `Response::error` is the only error path, event recording uses `.ok()?`
(silent). `eprintln!` is the entire warning system project-wide.

Wrote implementation prompt for next session (data-db-split-fixes). Steps 2-5
first (surgical fixes), Step 1 last (JSONL infrastructure). Natural split point
if context runs low.

**Key decisions:**
- MCP concerns are their own spec, not bolted onto a durability fix
- Fix spec is now 5 steps / 10 exit criteria, CLI-focused
- Process learnings from postmortem Part 5 stay as session observations,
  not beliefs — they're good habits, not architectural principles


## Beliefs Captured: 1

## Session Classification
- Work Type: pattern-work
- Files Changed: 9
- Commits: 9
- Patterns Modified: 9
- Beliefs Captured: 1
- Session Tags: session-20260226-152857-claude-start..session-20260226-152857-claude-end

## User Prompts (25)

1. `[Pasted text #1 +126 lines]`
2. `lets discuss this instead of writing it down .. in fact we probably should add an audit pass to o...`
3. `lets make a draft spec that adds draft → ready → active → audit → complete`
4. `we will circle back to the audit after this post mortem .. i need to understadn what you did and ...`
5. `ok so what were the 4 concerns from the postmortem`
6. `what would Johannes Schickling  say about 1 and 2`
7. `why was it deffered? is it in another area?`
8. `ok we need a fix spec for the one that was just completed .. it will also block the area 2 spec ....`
9. `lets get this into actionable steps for our spec fix [Pasted text #1 +23 lines]`
10. `ok lets now bring in jon gjengset to keep our rust design solid .. what would he say about the co...`
11. `i want all of his fixes added to our spec and adjust anything that needs adjusting`
12. `explain: Non-Goals explicitly defers Gjengset's type-safety suggestions (event type enum, ATTACH
...`
13. `[Pasted text #1 +2 lines]`
14. `yes capture it, then lets keep moving`
15. `i wonder if those ideas should land in our vision spec as they span outside this fix`
16. `lets do all 3`
17. `lets commit what we have and do a session update`
18. `from outside agent on fix spec: [Pasted text #1 +18 lines]`
19. `lets add 1-5 to our spec and i need help with 6 and 7`
20. `so what we do for mcp on other mcp commands is? not sure i understood this .. als owhat do we in ...`
21. `yes exactly lets draft a mcp draft spec to address [Pasted text #1 +5 lines]`
22. `lets keep going through the postmortem, whats left`
23. `so 1-3 ++ other things are in our fix spec? and 4 and 5 are kinda just observations on the proces...`
24. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
25. `/session-end`
