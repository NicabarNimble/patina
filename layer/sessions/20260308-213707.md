---
type: session
id: 20260308-213707
title: Schema Contract Model — Expand Parser to Match Authoring Surface
status: archived
llm: claude
created: 2026-03-09T01:37:07Z
start_timestamp: 1773020227464
git:
  branch: patina
  starting_commit: 39304424d7194ce0618d80a4ce49836ffcc5b718
  start_tag: session-20260308-213707-claude-start
---

## Previous Session Context
Last session "Finalize Protocol Alignment — PipeError vs Transport Errors" (20260308-210134) resolved the PipeError vs transport error inconsistency by aligning spec to code (Option a: PipeError stays at 4 variants, JSON-RPC -32600..-32700 codes stay at transport layer). Then performed a full implementation gap analysis across all three specs, identifying three major seams: multi-destination broker (Seam 1), schema contract layer (Seam 2), and lake control plane (Seam 3). An outside audit agent identified 5 specific build traps. Session concluded with the decision to start Seam 2 next — expand the schema parser to handle `identity_fields`, `projections`, `contracts`, `path_template`, and lake metadata as a shared contract model before touching consumers. Two beliefs captured: [[implementation-is-three-seams]] and [[contracts-before-consumers]].

## Goals
- [ ] Schema Contract Model — Expand Parser to Match Authoring Surface

## Activity Log
### 21:37 - Session Start
Session initialized with goal: Schema Contract Model — Expand Parser to Match Authoring Surface
Working on branch: patina
Tagged as: session-20260308-213707-claude-start


### 21:46 - Update: Schema contract model landed

**Git Activity:**
- Commits this session: 3 ([[commit-233b3f54]], [[commit-d01e67d1]], [[commit-ced88514]])
- Files changed: 5 (`children/github-connector/schema.toml`, `wit/schema/github/schema.toml`, `src/commands/schema/internal.rs`, `src/commands/schema/mod.rs`, [[spec-connector-owns-tables]] `SPEC.md`)
- Working tree: clean (uncommitted changes only in `layer/events.jsonl`)

**Spec promoted** ([[commit-ced88514]], [[commit-d01e67d1]])
[[spec-connector-owns-tables]] promoted draft → ready → active.

**Schema contract model expansion** ([[commit-233b3f54]])
Read all 4 anchoring values, the full SPEC + DESIGN for [[spec-connector-owns-tables]],
the full parser (`src/commands/schema/internal.rs`, 1133 lines), both schema.toml sources,
consumers (`events.rs`, `routing.rs`), and the Seam 2 gap analysis before writing any code.

Applied [[dependable-rust]]: types are the public interface, parsing is internal.
Applied [[unix-philosophy]]: parser's job is parse declarations into types — not project, consume, or migrate.
Applied [[contracts-before-consumers]]: land the model, stop before touching consumers.

**New types added to `internal.rs`:**
- `FactDef.identity_fields: Vec<String>` — per-fact-type identity for dedup (was silently ignored)
- `ProjectionDef` — `fact`, `table`, `primary_key`, `columns: Vec<ColumnDef>`
- `ColumnDef` — `name`, `col_type` (`#[serde(rename = "type")]`), `json_path`
- `ContractDef` — `name`, `event_type`, `display_kind`
- `LakeConfig` — `path_template: Option<String>`
- `SchemaMetadata` gains `projections`, `contracts`, `lake` fields (all with `#[serde(default)]`)

All types re-exported through `mod.rs` for future consumers. `show_schema` displays all new sections.

**Both schema sources updated:**
- `wit/schema/github/schema.toml` — added `identity_fields`, `[[projections]]`, `[[contracts]]`, `[lake]`
- `children/github-connector/schema.toml` — added `[[projections]]`, `[[contracts]]`, `[lake]`

**Validation:**
- `patina schema show github` displays all new sections correctly
- JSON output includes all new fields
- 25 schema tests pass (11 new), 609 total tests pass
- Round-trip test: parse → serialize → parse produces identical result
- Fixture tests for each new section (identity_fields, projections, contracts, lake)
- `parse_github_connector_schema` test confirms live connector schema parses with all sections

**Stop conditions respected:**
- Did NOT touch `events.rs`, `routing.rs`, `search.rs`, `enrichment.rs`, or `oxidize/mod.rs`
- Did NOT add migration logic or projection engine
- One commit for the contract model only

**Compression check:** No duplication. Types are clean serde structs with no command-layer dependencies. The `col_type` field uses `#[serde(rename = "type")]` to handle TOML's `type` keyword vs Rust's reserved word — correct approach, no workaround needed. Unused import warnings on re-exports are expected (consumers don't exist yet).


### 21:51 - Update: Session wrap-up + next session prompt

**Git Activity:**
- No new commits since last update.
- Working tree: clean (uncommitted changes only in `layer/events.jsonl`)

**Next session prompt written** — Seam 1 multi-destination broker. Anchored in all four
core values. Carries forward the module gravity warning from outside audit: schema contract
types in `src/commands/schema/internal.rs` are the single source of truth. Broker, lakehouse,
and transport code must import them, not duplicate them. If crate boundaries make import
awkward, extract schema types to a neutral module as the first commit.

**Session goal achieved:** [[spec-connector-owns-tables]] Phase 1 (schema parser expansion)
is complete. The authoring surface and code surface now match.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 5
- Commits: 3
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260308-213707-claude-start..session-20260308-213707-claude-end

## User Prompts (3)

1. `[Pasted text #1 +98 lines]`
2. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
3. `/session-end`
