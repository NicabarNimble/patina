---
type: session
id: 20260309-112026
title: Build spec schema-build-pipeline — 4 phases
status: archived
llm: claude
created: 2026-03-09T15:20:26Z
start_timestamp: 1773069626720
git:
  branch: patina
  starting_commit: 449ec3b8552f310e5c4265afdc262c0fec09dc6d
  start_tag: session-20260309-112026-claude-start
---

## Previous Session Context
Last session (20260309-090701) "Forge→GitHub Regression Audit" completed 14 commits across 10 files. Fixed 4 of 7 audit gaps (PR comments projection mismatch, `--with-issues` stub removal, `since` early-stop for PRs, linked-issue extraction), deferred 3 (old scrape UX, scheduled sync, per-ref resolve). Then consolidated schemas — deleted `children/github-connector/schema.toml` duplicate, established `wit/schema/<name>/` as single source of truth. Created and iterated on `spec-schema-build-pipeline` with SPEC + DESIGN across 3 commits, incorporating external audit feedback: fail-closed validation, single `routing::validate_fact()` entry point, `patina schema check` as Rust subcommand, install-to-temp validation.

## Goals
- [ ] Build spec schema-build-pipeline — 4 phases

## Activity Log
### 11:20 - Session Start
Session initialized with goal: Build spec schema-build-pipeline — 4 phases
Working on branch: patina
Tagged as: session-20260309-112026-claude-start


### 11:54 - Note [patina@faf93164]
User note: patina schema build validates+installs but does NOT generate unless flags are passed. Build may imply full regeneration to some users. Current behavior is spec-correct (DESIGN says optional generate) but worth revisiting if the mental model shifts.

### 12:18 - Update (covering since 11:20)

**Git Activity:**
- Commits this session: 11
- Files changed: 12
- Last commit: 5 minutes ago

**Work completed:** Built [[spec-schema-build-pipeline]] across 4 phases (11 commits), then audited spec queue and designed [[spec-pipe-contract-safety]].

1. **Phase 4 (first):** Deleted `src/generated/schemas/` — 4 dead files (forge-only, zero imports). Files were never git-tracked so no commit needed.

2. **Phase 1 — [[commit-147dd64a]]:** Broker fact-type validation. Added Step 3 to `routing::validate_fact()` — loads installed schema event_types on first fact per schema (cached in `HashMap<String, HashSet<String>>`), rejects unknown fact_types. Fail closed: missing installed schema = reject all facts. Added `load_fact_types()` as minimal TOML parse in routing.rs to keep broker self-contained (can't cross crate boundary to `commands::schema` since broker is in lib crate, commands in bin crate). 3 new tests, all 7 routing tests green.

3. **Phase 2 — [[commit-92c45411]]:** `patina schema check` Rust subcommand. Validates canonical schemas parse, installs to temp dir to prove installability, diffs installed copy against canonical (full directory, not just TOML), checks connector manifest package versions. Pre-push script step 8/8. Used `std::env::temp_dir()` instead of `tempfile` crate (dev-dependency only).

4. **Phase 3 — [[commit-8ee32d0d]]:** `patina schema build <name>` — validate → install, optional generate behind `--types`/`--migrations`/`--embeddings` flags.

5. **Spec completion — [[commit-faf93164]]:** All 5 exit criteria checked, spec completed and archived with tag `spec/schema-build-pipeline`. Released as v0.41.0.

6. **Spec queue audit:** Reviewed all 12 drafts + 2 active specs against codebase. External audit agent identified [[spec-pipe-mother-io]] should be `active` (3/4 criteria done), and [[spec-pipe-contract-safety]] has substantial existing code but broker doesn't use shared types yet.

7. **[[commit-8d96fb61]] + [[commit-d9f301f7]]:** Promoted [[spec-pipe-mother-io]] draft → ready → active.

8. **[[commit-b7708905]] + [[commit-4098b1c3]]:** Wrote DESIGN.md for [[spec-pipe-contract-safety]]. Revised per audit: include `FetchResult` deletion (response-side drift, not just request-side), fix test placement (colocated with types: `config.rs` for InitializeParams/FetchParams, `fact.rs` for FetchResult).

**Key decisions:**
- `load_fact_types()` in routing.rs rather than cross-crate `load_schema_metadata()` — broker is in lib crate, can't import from bin crate's `commands::schema`. Minimal TOML parse keeps broker self-contained per [[dependable-rust]].
- `BrokerFact` stays separate from `pipe_types::Fact` — `content_hash: Option<String>` is intentional (broker validates presence in routing Step 1).
- `build_init_params()` keeps `serde_json::Value` return type — `ChildConnection::request()` takes Value. Changing that is [[spec-pipe-native-transport]] scope.

**Compression pass:** Clean. No duplication introduced. `load_fact_types()` could theoretically share parsing with `commands::schema::parse_schema_toml()` but they're in different crates — acceptable duplication at crate boundary.

**Next session prompt written** for [[spec-pipe-contract-safety]] — 3 commits, anchored in layer/core values.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 12
- Commits: 11
- Patterns Modified: 4
- Beliefs Captured: 0
- Session Tags: session-20260309-112026-claude-start..session-20260309-112026-claude-end

## User Prompts (9)

1. `[Pasted text #1 +102 lines]`
2. `My only mild note is that schema build github does not generate anything unless flags are passed,...`
3. `ok so whats up with the specs in the que .. what has partial work done on it or what work done is...`
4. `from my audit outside agent: [Pasted text #1 +11 lines]`
5. `promote pipe-mother-io and then lets discuss whats in pipe-contract-safety`
6. `we might as well fill in the design doc`
7. `[Pasted text #2 +6 lines]`
8. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
9. `/session-end`
