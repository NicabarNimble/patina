---
type: session
id: 20260309-144700
title: 'Build patina-connect: Domain Model + Paths'
status: archived
llm: claude
created: 2026-03-09T18:47:00Z
start_timestamp: 1773082020578
git:
  branch: patina
  starting_commit: de75c8b7357fcb3815520bed2190492dcec18140
  start_tag: session-20260309-144700-claude-start
---

## Previous Session Context
Session 20260309-131917 rewrote SPEC.md and DESIGN.md for patina-connect, elevating it from a CLI convenience to a 3-layer product subsystem (Acquisition/Persistence/Consumption). Designed ConnectionRecord with identity + auth metadata, AuthPlan resolution seam, Provider trait, and a 9-commit build plan. Key decisions: persistence is the center, dispatch on auth strategy not provider, fail closed on auth-required children. No code written — spec + design only.

## Goals
- [x] Commit 1: paths — add connections path API to src/paths.rs
- [x] Commit 2: connect — add domain model (ConnectionRecord, AuthConfig, AuthPlan, InjectionStrategy, ConnectError) with TOML round-trip tests

## Activity Log
### 14:47 - Session Start
Session initialized with goal: Build patina-connect: Domain Model + Paths
Working on branch: patina
Tagged as: session-20260309-144700-claude-start

### 14:55 - Update (covering since 14:47)

**Git Activity:**
- Commits this session: 3
- Files changed: 9 (+583 / -29 lines)
- All changes committed, working tree clean (except layer/events.jsonl + session files)

**Work completed:** Executed [[spec-patina-connect]] DESIGN.md commits 1-2 (model-out build).

1. **[[4b385bcd]] `paths: add connections path API`** — Added `paths::connections` module to `src/paths.rs` with `connections_dir()` → `~/.patina/connections/` and `connection_path(name)` → `~/.patina/connections/{name}.toml`. Replaces hardcoded path in `src/broker/connection.rs:42-48`. Test follows existing paths module pattern. Satisfies exit criterion `connection-paths` (partial — grep verification deferred to session 4 broker refactor).

2. **[[452e2b80]] `connect: add domain model`** — Created `src/connect/` module following [[dependable-rust]]: small `mod.rs` with curated re-exports, `internal/model.rs` hides 474 lines of types. Domain types:
   - `ConnectionRecord` with `[identity]` + `[auth]` TOML sections + `schema_version`
   - `ConnectionIdentity`: name, provider, account_id, auth_method, scopes, timestamps, scope
   - `AuthConfig`: injection strategy, secret_ref, child, allowed_domains, refresh/expiry
   - `InjectionStrategy`: Bearer | Header { name } | InProcess (tagged enum, serde `type` field)
   - `AuthMethod`: OAuth | Manual (lowercase serde)
   - `ConnectionScope`: Global (default, v1 only)
   - `AuthPlan` + `ResolvedCredential`: consumption-layer output for broker
   - `ConnectionStatus`: Connected | Missing | Expired | Errored | Unchecked
   - `ConnectError`: 7 variants with actionable Display messages
   - 12 tests: TOML round-trip (OAuth, Manual), all 3 injection strategies, hand-authored TOML parsing, error messages, status variant distinctness

3. **[[6fa40002]] `fmt: cargo fmt pass`** — Pre-existing formatting issues in `src/broker/` and `src/commands/schema/` exposed by cargo fmt. Includes collapsible-if clippy fix in `src/commands/schema/internal.rs`.

**Key decisions:**
- Used serde `#[serde(rename_all = "lowercase", tag = "type")]` for `InjectionStrategy` tagged enum — produces clean TOML `[auth.injection] type = "bearer"` format
- `AuthPlan` and `ResolvedCredential` intentionally do NOT derive `Serialize/Deserialize` — they are ephemeral runtime values, never persisted
- `ConnectError` is a standalone enum (not `thiserror`) — manual `Display` impl for actionable messages with recovery hints

**Pre-push checks:** All 8 checks pass (WIT, fmt, clippy, tests, schema consistency).

**Handoff to Session 2:** Domain model types ready. Session 2 builds commits 3-4: `connect/internal/store.rs` (TOML persistence + referential integrity using `paths::connections`) and auth plan resolution (`resolve_auth()` seam consuming `secrets::get_global_secret`, fail-closed enforcement).


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: feature
- Files Changed: 9
- Commits: 3
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260309-144700-claude-start..session-20260309-144700-claude-end

## User Prompts (2)

1. `[Pasted text #1 +72 lines]`
2. `/session-end`
