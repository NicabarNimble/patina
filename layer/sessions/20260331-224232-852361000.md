---
type: session
id: 20260331-224232-852361000
runtime_id: 5ca93173-790e-4b79-94f4-51f38e4d696f
title: Full Code Audit of Patina
status: active
llm: claude
interface: claude
created: 2026-04-01T02:42:32.852361+00:00
updated: 2026-04-01T15:10:57.254432+00:00
start_timestamp: 1775011352852
participants:
- id: nicabar-53726
  role: interface
  interface: claude
  adapter: claude
  display_name: nicabar
  joined_at: 2026-04-01T02:42:32.852361+00:00
interfaces:
- claude
git:
  project_uid: 2bdc808e
  branch: patina
  starting_commit: ac483961758f411ecdd63a2a03443d20e032ea4c
  start_tag: session-20260331-224232-852361000-claude-start
---

## Previous Session Context
Last session "spec review" (20260331-164603) completed `folder-text-to-parquet` and `scaffold-world-retirement` specs, abandoned 3 overtaken specs, and fixed a spec list display bug. Key handoff: a full code audit prompt was drafted anchored in `layer/core/` values, ready for execution in a fresh session — which is this session. Open items include updating `patina-durable-backup` for `state.db`/`graph.db` and the untouched `scrape-strategy-seam-exploration` spec.

## Goals
- [ ] Full Code Audit of Patina

## Activity Log
### 22:42 - Session Start
Session initialized with goal: Full Code Audit of Patina
Working on branch: patina
Tagged as: session-20260331-224232-852361000-claude-start

## Decisions

## Evidence

## Handoff

## Outcome

### 23:29 - Update (covering since 22:42)

**Work completed:** Full code audit of the Patina codebase (~88,731 lines across 175 Rust source files). Read all 8 `layer/core/values/` files, full `src/main.rs` (1840 lines), `src/lib.rs`, `AGENTS.md`, `layer/core/build.md`. Ran `cargo check --workspace`, `cargo test --workspace --lib` (693 pass, 2 ignored), `cargo test --workspace --tests` (17 pass, 2 fail on missing WASM artifact). Launched 8 parallel audit agents covering: scrape pipeline, scry+retrieval, beliefs+embeddings, session+layer, child+mother+connect, paths+db+project+git, spec+lake+eval+report, SDK+children+WIT+build. Directly audited: `eventlog.rs`, `interface/mod.rs`, `measure.rs`, `migration.rs`, `environment.rs`, `version.rs`, `core_tools/mod.rs`, `preflight.rs`, `lake.rs`, `commands/context.rs`, `commands/serve/mod.rs`, `commands/events.rs`, `commands/upgrade.rs`, `commands/assay/mod.rs`, all `unsafe` usage across `src/`.

**Discussion context:** Audit was anchored in [[session-20260331-164603]]'s handoff — a code audit prompt drafted in that session against `layer/core/` values. The audit classified every module as protocol core (5 verbs), protocol tooling, or protocol infrastructure per [[patina-identity]]. Each area was evaluated for: compile status, test coverage, dead code, alignment with [[dependable-rust]] (small public interfaces, private internals), [[unix-philosophy]] (one tool, one job), [[safety-boundaries]] (no unsafe in protocol logic), and [[adapter-pattern]] (trait-based external integration).

**Key findings — 6 high-severity:**
- A1: UTF-8 panic risk in `src/commands/scry/internal/enrichment.rs:417` (byte-slice truncation)
- A2: Thread-unsafe `set_current_dir` in `src/retrieval/engine.rs:284` (no RAII guard)
- A3: Divergent `check_capabilities` auto_granted lists between `src/child/internal/mod.rs:127` and `src/child/internal/knowledge_child.rs:754`
- A4: Stubbed `starting_commit()` returning `"none"` in `mother/src/state.rs:171`
- A5: Hardcoded `dimensions: 256` in `src/commands/belief/mod.rs:1064` vs raw E5 768-dim
- A6: Duplicated `SessionFrontmatter` with `Option<String>` vs `String` drift between `src/commands/session/internal.rs:37` and `src/session/internal/artifact.rs:32`

**Key findings — architecture:**
- A7: `retrieval/oracles/semantic.rs:13` imports from `commands::scry::internal` — architecture inversion
- A8: `src/embeddings/database.rs` (231 lines) appears entirely dead
- A9: `src/paths.rs` is NOT the single source of truth — 10+ sites hardcode `.patina/` paths
- A10: `src/db/sqlite.rs` wrapper has only 2 consumers; 50+ sites use `rusqlite::Connection::open()` directly
- A25: `src/spec.rs:21` library depends on CLI internal module — inverted dependency

**Additional findings:** 18 unarchived specs on disk (14 complete, 4 abandoned), `build.md` version 22 releases behind, deprecated `serve` module past its stated removal date, SDK tier sub-crates dead on disk, `wit/command/` and `wit/task/` directories missing despite AGENTS.md claims.

**All 26 addendum findings independently verified** by a separate audit agent running `cargo check`, `cargo test`, and static code inspection.

**Git Activity:**
- Commits this session: 0
- Files changed: 2 (1 modified: `src/commands/repo/internal.rs`, 1 session artifact)
- Last commit: 48 minutes ago


### 11:10 - Update (covering since 23:29)

**Work completed:** Created umbrella remediation spec [[code-audit-remediation]] with 30 exit criteria. After audit agent review caught counting errors, either/or ambiguity, and missing verification matrices — rewrote and split into 5 focused child specs per [[unix-philosophy]]: [[car-safety-a1-a6]] (7 gates), [[car-architecture-a7-a25]] (2 gates, A25 dropped), [[car-deadcode-a8-a24]] (10 gates), [[car-dedup-a9-a21]] (7 gates), [[car-cleanup-non-a]] (8 gates). Created 2 beliefs: [[audit-before-refactor]] and [[canonical-module-bypass-compounds]]. Added `[[child-construction-canon]]` backlinks to 6 key beliefs. Renamed belief `four-roles-no-overlap` → [[five-boundaries-no-overlap]] via `patina belief rename --rewrite-links` (7 wikilinks updated automatically).

**Discussion context:** Deep discussion about what Patina IS and the direction it's heading. Read [[child-construction-canon]] spec in full (the active architectural canon). Walked each A finding through git history to understand WHY each issue exists before deciding whether to fix, reframe, or drop. Key framing: protocol core stays native CLI (5 verbs), children are reusable WASM compute legos composed via events, toys are WASI-first capability grants (Patina delta only where WASI has no interface). Per [[core-primitives-are-not-children]], the pipeline is the binary — children feed INTO core, they don't replace it.

**Key decisions:**
- A25 (spec lib inversion) **dropped** — the import direction is intentional daemon-first dispatch. Spec-manager is a Mother-hosted child per [[child-construction-canon]]. The "inversion" IS the architecture.
- A2 scaled back from full path parameterization to RAII guard. Full parameterization is the daemon-readiness direction but larger scope than a safety fix.
- A3 elevated to security-critical — capability check is the gate for the entire child/toy lego system per [[children-have-agency-toys-are-capabilities]]. Must investigate before deleting.
- A6 reversed — `Option<String>` is correct for `project_uid` because 538 pre-UID sessions are permanent artifacts. Canonical type adopts the lenient form.
- A22 reframed — toy host functions may be "dead to Rust" but alive in the toybox. Three-category audit needed: live, toybox-live, genuinely dead.
- A12 confirmed delete — [[child-construction-canon]] chose manifest + runtime enforcement, not compile-time SDK tiers.
- Belief [[five-boundaries-no-overlap]] expanded from 4→5 roles: Patina (belief core), Mother (infra daemon), child+toy (knowledge worker), interface+agent (AI guest), projects (user workspace).

**Patterns observed:**
- Reading [[child-construction-canon]] before editing any spec prevented at least 3 wrong decisions (A25, A6 direction, A22 scope). The canon is the architectural anchor — every gate should be tested against it.
- The git history walk (why does this code exist?) changed the disposition of 4 gates. History > assumptions.
- Belief rename tool works well but scope is limited to `layer/surface/epistemic/beliefs/`. Active/draft specs need manual fixup.

**Git Activity:**
- Commits this session: 2 ([[commit-bc8229e4]], [[commit-6c7b5b0e]])
- Files changed: 22 modified, 5 untracked (spec files, belief files, session artifact)
- Last commit: 3 hours ago

