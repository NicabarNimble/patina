---
type: session
id: 20260305-170212
title: forge-plugin-extraction SESSION 3 of 4 — Deep Audit — User alignment before deletion
status: archived
llm: claude
created: 2026-03-05T22:02:13Z
start_timestamp: 1772748133053
git:
  branch: patina
  starting_commit: a028f8ec682b69e1fc0b95564e04e8597c2597d0
  start_tag: session-20260305-170212-claude-start
---

## Previous Session Context
Session 2 (20260305-160605) achieved full live end-to-end flow: extended `plugin run` for mother-child world, configured vault/secret grants for GitHub PAT, fixed User-Agent header (403 from GitHub API), and ran successful live sync — 3 issues + 3 PRs fetched from GitHub, emitted to events.db, projected into forge_issues/forge_prs tables, and indexed in FTS5. Also added CQRS projection (`project_from_events()`) that ATTACHes events.db to project forge events, tested resolve action (PR and issue fallback). All 579 tests pass. User explicitly asked for deep audit before any deletion — wants alignment on what was built hands-off.

## Goals
- [x] Deep audit of forge plugin architecture and code quality
- [x] Review alignment with layer/core values (unix-philosophy, dependable-rust, safety-boundaries)
- [x] Identify any concerns before proceeding to deletion of src/forge/ in session 4
- [x] User alignment on plugin design decisions made in sessions 1-2
- [ ] Formally check off spec exit criteria EC1-EC3 (deferred — reframing as pipe architecture)

## Activity Log
### 17:02 - Session Start
Session initialized with goal: [[spec-forge-plugin-extraction]] SESSION 3 of 4 — Deep Audit — User alignment before deletion
Working on branch: patina
Tagged as: session-20260305-170212-claude-start

### 17:10 - Phase 1: Plugin Code Audit
Walked through all plugin code with user:
- `plugins/forge/plugin.toml` — manifest, capabilities, minimal surface
- `plugins/forge/src/lib.rs` — ForgeChild, handle/sync/resolve/tick
- `plugins/forge/src/github.rs` — GitHubClient, REST API, JSON conversion

Findings: PR `updated_at` bug (used `created_at`), `linked_issues` always empty (REST limitation), `truncate()` UTF-8 panic risk, `since` silently ignored for PRs, comments emitted as flat text but WIT declares `list<comment>`.

### 17:30 - Phase 2: Host Infrastructure Audit
Walked through host-side code:
- `src/plugin/internal/host_support.rs` — emit_fact, http_get, leak_check, validate_http_url, check_secret_grant, resolve_credential
- `src/plugin/internal/mother_child.rs` — PluginEngine, WasmChild, Mutex pattern

Security model confirmed sound: three-layer defense (manifest → load-time → call-time), HTTPS-only, no IPs, redirect rejection, leak detection. Auth UX identified as pain point (4 manual steps for PAT). Outside agent concurred: keep model, fix funnel. Poisoned mutex should fail-closed.

### 17:50 - Phase 3: Projection Pipeline Audit
Walked through CQRS projection:
- `src/commands/scrape/forge/mod.rs` — `project_from_events()`, ATTACH DATABASE, "latest seq wins" dedup
- `src/commands/scrape/code/extract_v2.rs` — integration point with fallback

Found: `created_at` uses event insertion time not GitHub time, `merged_at` synthesized from state, old dual-write path coexists with new projection.

### 18:10 - Phase 4: Old vs New Comparison
Field-by-field comparison of `src/forge/types.rs` domain types vs plugin event JSON. Identified 4 bugs, 4 accepted regressions, 4 architecture wins. Read old `src/forge/github/internal.rs` (8 `gh` CLI call sites), `src/forge/sync/internal.rs` (`libc::fork`, PID files, background drain).

### 18:30 - Phase 5: Expert Advisory Review
Five-lens review (Gjengset, Kelley, Steenberg, Helland, Cantrill). Key findings: WASM boundary clean, CQRS projection correct, emit-time dedup needed, schema/data mismatch (WIT says structured comments, plugin emits flat text), operational observability sufficient for CLI but needs structured logging for daemon.

### 19:00 - Audit Fixes (1 commit)
[[commit-f98ecb06]] — fix(forge): audit fixes — timestamps, structured comments, projection correctness

6 fixes applied:
1. PR `updated_at` deserialized from GitHub REST response
2. Issue `created_at` added to event JSON
3. PR `merged_at` added to event JSON
4. Comments emitted as structured `list<comment>` matching WIT + `comments_text` for FTS
5. Projection uses domain timestamps with COALESCE fallback, added `ingested_at` column
6. `truncate()` UTF-8 safe

All 579 tests pass. Pre-push checks clean. Findings documented in [[spec-forge-plugin-extraction]] post-audit section.

### 19:30 - Architecture Discussion
User asked to understand WASM/WIT plugin architecture. Explained:
- What git gives vs what GitHub gives (code vs conversation layer)
- Why WASM (sandbox) vs native (no sandbox)
- 4 plugin worlds (mother-child, command, task, pipeline) × 4 roles (connector, grammar, extension, app)
- What streaming (WebSockets, SSE) can't do in WASM (no persistent connections)
- CRDTs + Iroh sit at host level (networking), WASM at compute level

### 20:00 - Category Error Discovery
User identified the core insight: forge is a data pipe, not a plugin. Plugins are for domain logic (spec system, grammars). Pipes are infrastructure (GitHub, Slack, RSS). Led to pipes-vs-plugins distinction:

| Pipes | Plugins |
|---|---|
| Move data in | Add behavior |
| Infrastructure (like git) | Third-party (like Obsidian plugins) |
| User-level | Project-level |
| `patina auth` (OAuth) | WASM sandboxed |
| GitHub, Slack, RSS | Spec system, grammars, doctor |

### 22:26 - Note [patina@f98ecb06]
Audit conversation revealed forge connector is wrong abstraction. Pipes (data drivers) are infrastructure, not plugins.

### 20:30 - Pipe Architecture Spec (2 commits)
[[commit-b8ede418]] — spec(draft): pipe-architecture — data drivers, not plugins
[[commit-472ad948]] — spec(draft): pipe-architecture — DESIGN.md + current state detail

New spec [[spec-pipe-architecture]] drafted with:
- Three-layer model: secrets (user-level) → pipes (Mother-level) → destinations (project/lake/block config)
- WIT-defined pipe interface (get-capabilities, check-health, fetch)
- `patina auth login <provider>` — OAuth device flow replacing manual PAT
- Destination configuration in TOML
- Migration path from forge WASM plugin to GitHub pipe
- Full DESIGN.md with secrets architecture, credential lifecycle, relationship to other specs

### 21:00 - Next Session Prompt
Wrote exploration prompt for next session: pipes, plugins, WASM, WIT, WebSockets, and MCP. Four tracks covering the full design space.

## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 5
- Commits: 4
- Patterns Modified: 3
- Beliefs Captured: 0
- Session Tags: session-20260305-170212-claude-start..session-20260305-170212-claude-end

## User Prompts (27)

1. `[Pasted text #1 +194 lines]`
2. `2 things .. i kinda hate we lost gh and moved to this other format .. also i have no idea why i h...`
3. `my thinking is the process i went through to make a pat was dumb and we need another way ... if w...`
4. `yeah we need to architect this differnt .. i'm ok with wasm and agree on its pros .. BUT we need ...`
5. `outside agent replied: [Pasted text #3 +82 lines] BUT agent has very little context so take with ...`
6. `outside agent: [Pasted text #4 +62 lines]`
7. `outside agent: [Pasted text #5 +70 lines]`
8. `i worry about pushing things off and overengineering .. but heres whatthe outside agent has to sa...`
9. `i say we make the fixes now and add what was found and done in the spec as a post audit area or s...`
10. `i need you to help me understand this wasm wit plugin architecture better than i do ... i know we...`
11. `so we are building connectors, data lakes, and data blocks AND we are building apps of sorts i gu...`
12. `no no 
  You're not really building "a GitHub connector." You're building a knowledge ingestion p...`
13. `right its just github .. and what it pulls from github is what? everything?`
14. `we had some sessions about making everything a wasm plugin wasnt always correct BUT we should mak...`
15. `if we tighten up issues we discovered is that universal and sdk value or github specific?`
16. `what could be a plugin that should wasm that benefits from any of this? and if not wasm you say n...`
17. `i guess i need help understanding how i can build a plugin system for patina .. we landed on wasm...`
18. `you say obsidian has a sanbox in electron? isnt that our wasm? wouldnt we lose that sandbox if no...`
19. `what would websockets and sse or whateverthat crypto streaming is in a wasm world? `
20. `what about cdrt and iroh and wasm where do they live togther?>`
21. `ok so local-first ++ the edge using cloudflare ,,, one thing that i'm thinking about .. so stream...`
22. `no the forge is a connectore that we jammed in a plugin design ... the spec system is to be moved...`
23. `git is core though because its how the belief system workls always .. github or gitea or slack or...`
24. `we need a spec`
25. `ok make sure to fill it in with context and add a design.md and fill that in`
26. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
27. `/session-end `
