---
type: session
id: 20260306-204943
title: PIPE ARCHITECTURE - Write all 5 DESIGN.md files.
status: archived
llm: claude
created: 2026-03-07T01:49:43Z
start_timestamp: 1772848183356
git:
  branch: patina
  starting_commit: 5f774ba89174e94dbfcb7bef5d568f69432b2105
  start_tag: session-20260306-204943-claude-start
---

## Previous Session Context
Session 12 conducted a full spec landscape audit through three expert lenses (Gjengset/Kelley/Helland), confirming the pipe-architecture decomposition into 5 child specs is sound. Key decisions: connectors use their own schema namespaces (`github.*` not `forge.*`), PipeError codes are transport detail, cursors are opaque, credentials use SecretString with zeroize-on-drop, cursor+fact writes are transactional (same SQLite txn), and no fan-out optimization. Added design constraints to 3 specs and wrote a build plan: session 0 writes all 5 DESIGN.md files (this session), then sessions 1-5 implement each spec in dependency order.

## Goals
- [x] Write DESIGN.md for [[spec-pipe-protocol-types]] (types, canonical JSON, PipeError, SecretString)
- [x] Write DESIGN.md for [[spec-pipe-native-transport]] (stdio JSON-RPC, sandbox, lifecycle)
- [x] Write DESIGN.md for [[spec-github-connector]] (native child, github.* schema, migration)
- [x] Write DESIGN.md for [[spec-patina-connect]] (OAuth device flow, connection config)
- [x] Write DESIGN.md for [[spec-mother-broker]] (routing, scheduling, transactional writes)

## Activity Log
### 20:49 - Session Start
Session initialized with goal: PIPE ARCHITECTURE - Write all 5 DESIGN.md files.
Working on branch: patina
Tagged as: session-20260306-204943-claude-start

### 20:55 - Grounding Complete
Read all code referenced in the session prompt: `src/plugin/internal/host_support.rs` (emit_fact, validate_emit, HTTP, credential injection), `src/plugin/internal/mod.rs` (PluginManifest, GrantedCapabilities, schema parsing), `src/eventlog.rs` (events.db schema, WAL, provenance), `src/mcp/server/mod.rs` + `src/mcp/protocol.rs` (stdio JSON-RPC pattern), `plugins/forge/src/lib.rs` + `plugins/forge/src/github.rs` (ForgeChild, GitHubClient, pagination, JSON shapes), `plugins/forge/plugin.toml` (manifest prototype), `.patina/schemas/forge/schema.toml` (schema format), `wit/deps/patina-host/host.wit` (WIT interfaces), `src/secrets/mod.rs` + `src/secrets/vault.rs` (vault, identity, age encryption), `src/mother/mod.rs` + `src/mother/child.rs` (MotherChild trait, ChildHealth), `src/plugin/internal/mother_child.rs` (WasmChild adapter, Host trait impls), `Cargo.toml` (workspace, deps), `cargo tree`. Also read all 4 values, [[spec-pipe-architecture]] SPEC.md + DESIGN.md, all 5 child SPEC.md files, all 5 skeleton DESIGN.md files.

### 21:10 - pipe-protocol-types DESIGN.md Written
Concrete design for `crates/patina-pipe-types/`: Fact struct (schema, fact_type, data as Value, content_hash, signature stub), PipeError enum (4 variants — Transient/Fatal/RateLimited/Partial — JSON-RPC codes as transport detail via `jsonrpc_code()` method, not enum fields), Capabilities, HealthStatus, FetchParams (opaque cursor via `since`), AuthConfig (plain String — zeroize boundary is Mother's code, not types crate), InitializeParams, canonical_json() (recursive sorted-key serializer, documented double-serialization cost, Unicode/float edge cases), content_hash() (blake3), ChildManifest parser for child.toml, SDK rename mapping (host_emit→emit, host_http→fetch, host_log→log, host_query→query). 5 commits planned. 2 open questions: SDK file layout, blake3 dep approval.

### 21:25 - pipe-native-transport DESIGN.md Written
Design for `crates/patina-pipe/`: Child trait (capabilities, initialize, fetch with `&mut self` + FactEmitter, health), JSON-RPC protocol types (Request/Response/Notification following `src/mcp/protocol.rs` pattern), FactEmitter (streaming, O(1) memory, computes content_hash), run() entry point (BufReader stdin, line-delimited, dispatch by method, explicit error handling for all protocol errors), macOS sandbox-exec profile template with domain regex injection, Linux Landlock stub, sandbox debug mode, Mother-side test harness (ChildConnection with interleaved notification handling), test-child example binary, protocol sequence diagrams (poll mode, error during fetch, rate limited). 6 commits planned. 2 open questions: sandbox-exec deprecation, stdout contention in stream mode.

### 21:35 - github-connector DESIGN.md Written
Design for `children/github-connector/`: Child trait impl (GitHubConnector with auth_token from pipe/initialize), github.rs migration from `plugins/forge/src/github.rs` (host_http→reqwest, host_emit→emitter.emit, Result<_,String>→PipeError, line-by-line mapping table), HTTP error mapping to PipeError variants (401/403→check rate limit, 429→RateLimited, 5xx→Transient), github.* schema definition (slot 6, same data shape as forge), child.toml manifest, parity verification plan (SQL queries comparing forge.* vs github.*), src/forge/ deletion checklist (2,216 LOC + 705 LOC scrape command, exact file list with dependency check). 6 commits planned. 1 open question: chrono dep for cursor.

### 21:45 - patina-connect DESIGN.md Written
Design for `src/connect/` module: OAuth device flow (RFC 8628) step-by-step implementation (device authorization request, user code display, browser open, token polling with slow_down/expired_token/access_denied handling), ConnectionConfig format (connection meta + oauth meta at `~/.patina/connections/<name>.toml`), config.rs (load/save/remove/list), public API (connect_github, connect_status, connect_refresh, connect_remove), CLI commands (patina connect github/status/refresh/remove), credential delivery path (Mother reads connection → decrypts vault → passes via pipe/initialize → zeroize after serialize), GitHub OAuth App registration steps and scope justification (repo + read:org), manual fallback for PAT users. 5 commits planned. 2 open questions: OAuth app registration timing, multi-account UX.

### 21:55 - mother-broker DESIGN.md Written
Design for `src/broker/` module: sources.toml format spec (connection, params, types, schedule fields), sources reader with scan_all_sources() across registered projects, BrokerChild trait (unified over WASM and native — fetch with on_fact callback, health, shutdown), NativeChild implementation (interleaved notification/response reading), WasmBrokerChild adapter (wraps existing MotherChild, facts bypass broker routing via host_emit), native child spawn (resolve binary from ~/.patina/children/ or PATH or target/release/, sandbox profile generation, pipe/initialize handshake, credential delivery), fact routing (validate_fact extracts pattern from host_support::validate_emit, content_hash verification, dedup via data string comparison), transactional cursor management (write_facts_with_cursor in single SQLite transaction), broker public API (run_source orchestrating full flow, status showing all sources), CLI wiring (patina mother run/sources), on-scrape scheduling trigger, explicit no-fan-out-optimization decision. 8 commits planned. 3 open questions: events.db per-project path, schema loading scope, WASM fact routing bypass.

### 22:00 - All 5 DESIGN.md Files Committed
Committed [[commit-1159c660]]: 3,952 lines of design across all 5 pipe-architecture child specs. Total: 30 planned commits across 5 implementation sessions.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 5
- Commits: 1
- Patterns Modified: 5
- Beliefs Captured: 0
- Session Tags: session-20260306-204943-claude-start..session-20260306-204943-claude-end

## User Prompts (4)

1. `[Pasted text #1 +178 lines]`
2. `nope keep going`
3. `did we write them all we are out of context `
4. `/session-end `
