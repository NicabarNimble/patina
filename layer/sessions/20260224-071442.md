---
type: session
id: 20260224-071442
title: Spec Module Split + Spec Create — Design Refinement
status: archived
llm: claude
created: 2026-02-24T12:14:42Z
start_timestamp: 1771935282934
git:
  branch: patina
  starting_commit: b742e06332010a2f64f9debf25a2b0ce558afa92
  start_tag: session-20260224-071442-claude-start
---

## Previous Session Context
Completed spec-workflow-rigor (v0.30.0) in 9 commits: wired `/spec` skill into both deployment paths, removed deprecated `spec status` (-229 lines), and ran `spec complete`. Discovered `adapter refresh` bug that drops `settings.local.json` and drafted 3 new specs: adapter-refresh-preserves-user-state (fix), spec-create (feat), and spec-module-split (refactor). Key design decision: spec types are system data not user config, and spec-module-split blocks spec-create so the refactor lands first.

## Goals
- [x] Refine spec-module-split design — review and improve the draft spec
- [x] Refine spec-create design — review and improve the draft spec
- [x] Ensure dependency ordering: spec-module-split blocks spec-create
- [x] Add DESIGN.md walkthrough docs to both spec folders
- [x] Add matrix-org/matrix-spec as reference repo

## Activity Log
### 07:14 - Session Start
Session initialized with goal: Spec Module Split + Spec Create — Design Refinement
Working on branch: patina
Tagged as: session-20260224-071442-claude-start

### 07:36 - Update (covering since 07:14)

**Git Activity:**
- Commits this session: 3 ([[commit-b01f9876]], [[commit-465a7568]], [[commit-9adf9d05]])
- Files changed: 4 (SPEC.md x2, DESIGN.md x2)
- Last commit: recent

**Work completed:**

Refined both draft specs after thorough code review of all source files:

**[[spec-module-split]] refined** ([[commit-9adf9d05]]): Verified section boundaries against actual `internal.rs` (2063 lines, 7 `// ====` banner-delimited sections). Reduced from 8 files to 7 — dropped `util.rs` (30 lines would be artificial; `find_spec` belongs in `archive.rs`, `mutate_spec`/`with_yaml_rollback` in `mutations.rs`, `tag_exists`/`is_tree_clean` in `queue.rs`). Added: cross-module dependency map documenting all inter-file calls, full re-export contract matching current `mod.rs`, shared constant handling, comparison with assay/scry/eval patterns, and 7-step implementation order.

**[[spec-create]] cleaned up** ([[commit-465a7568]]): Removed module-split content (SpecType registry definition, internal/ directory layout, BumpType refactor) that belonged in [[spec-module-split]]. Added `blocked_by: [spec-module-split]` to frontmatter. Restructured solution around three-prong bundle (CLI + MCP + Skill). Updated key files to reference `internal/create.rs` instead of `internal.rs`. Spec now assumes `types.rs` and `internal/` already exist.

**DESIGN.md added to both spec folders** ([[commit-b01f9876]]): Step-by-step implementation walkthroughs with exact import blocks, code sketches, verification checklists. spec-module-split DESIGN covers 10 steps from directory creation through the safe swap strategy. spec-create DESIGN covers 6 steps including full `create.rs` sketch, MCP handler code, clap tests, and 5 design decisions. Both docs archive with their spec folder on completion.

**Reference repo added:** `matrix-org/matrix-spec` indexed as reference repo (5,644 commits, 488 semantic vectors, 39,350 co-change relationships). Queryable via `--repo matrix-org/matrix-spec`.

**Continuation prompt written:** Self-contained prompt for next session covering implementation of both specs, anchored in layer/core values, with full file reading list and step summaries.

**Key decisions:**
- Spec folders are the right place for supporting docs — archive takes the whole directory (`git rm -rf`), preserved in git tag
- 7 files not 8 for the split — each helper has a natural home with its callers
- `pub(super)` for internal re-exports, `pub(crate)` only for `types` module (needed by `release/mod.rs`)


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 4
- Commits: 3
- Patterns Modified: 4
- Beliefs Captured: 0
- Session Tags: session-20260224-071442-claude-start..session-20260224-071442-claude-end

## User Prompts (8)

1. `[Pasted text #1 +101 lines]`
2. `can you add this to our ref repos https://github.com/matrix-org/matrix-spec.git patina repo --hel...`
3. `so what is it?`
4. `where are we with create spec? should we add a design and walthrough docs? also when we archive a...`
5. `yes, add design docs to both spec folders`
6. `/context `
7. `i want to continue the writing of spec-module-split and spec-create next session. write a prompt ...`
8. `/session-end`
