---
type: session
id: 20260307-234302
title: Fix GitHub Connector Projection Gap — [[spec-github-child-owns-forge]]
status: archived
llm: claude
created: 2026-03-08T04:43:02Z
start_timestamp: 1772944982202
git:
  branch: patina
  starting_commit: 2c810e428eda4ac8ea5f6f6f4d56bcd96030c243
  start_tag: session-20260307-234302-claude-start
---

## Previous Session Context
Audited all code moved during the v0.40.0 forge deletion (~4K LOC removed). Discovered a critical **projection gap**: `project_from_events()` in `events.rs` hardcodes `WHERE event_type = 'forge.issue'` but the github-connector now emits `github.issue`/`github.pr` events — making GitHub issues/PRs invisible to `scry --include-issues` and `assay`. Removed ~300 lines of dead forge staging code and WASM tests, then created [[spec-github-child-owns-forge]] (4 phases: fix projection gap, create github schema, move ForgeWriter into github child as pipe verbs, delete forge artifacts). Phase 1 (projection gap fix, ~10 lines SQL) is the entry point for this session.

## Goals
- [x] Phase 1: Fix projection gap — extend WHERE clauses for github.* event types
- [x] Phase 2: Create `wit/schema/github/` (schema.toml + github.wit)
- [x] Phase 4: Delete forge artifacts (`grammars/forge/`, `wit/schema/forge/`)
- [x] Update `build.md` architecture diagram
- [x] Fix oxidize hardcoded event types (discovered during Phase 2 work)
- [x] Update tests in `schema/internal.rs` to reference github schema
- [x] Update [[spec-github-child-owns-forge]] — check 5 ECs, defer ForgeWriter
- [x] Update [[spec-mother-broker-github]] — add ECs to frontmatter, both verified
- [x] Create [[spec-schema-driven-projection]] — captures architectural debt from hardcoded approach
- [ ] Phase 3: ForgeWriter → pipe verbs (DEFERRED — separate concern)

## Activity Log
### 23:43 - Session Start
Session initialized with goal: Fix GitHub Connector Projection Gap — [[spec-github-child-owns-forge]]
Working on branch: patina
Tagged as: session-20260307-234302-claude-start

### 00:00 - Phase 1 Complete
Read [[spec-github-child-owns-forge]] SPEC.md and DESIGN.md, then all 5 target files. Extended 8 SQL WHERE clauses in `src/commands/scrape/events.rs` (projection + dedup + FTS5), fixed event type matching in `src/commands/scry/internal/enrichment.rs` and `src/commands/assay/internal/search.rs`. Build clean, installed, verified: `patina scrape` projects 17 issues + 87 PRs, `patina scry --include-issues` and `patina assay search --include-issues` return results.

### 00:15 - Phases 2 + 4 Complete
Created `wit/schema/github/schema.toml` and `wit/schema/github/github.wit`. Deleted `grammars/forge/` and `wit/schema/forge/`. Updated `build.md` diagram ("scrape forge" → "github-connector"). Updated schema tests. Discovered and fixed hardcoded event types in `src/commands/oxidize/mod.rs` (not in original Phase 1 scope).

### 00:30 - Mother Architecture Research
Explored mother daemon, broker, pipe protocol, and github-connector to understand Phase 3 scope. Mother has two channels: microserver (UDS/TCP for daemon API) and broker (subprocess spawn for native children). github-connector is one-shot fetch via broker. ForgeWriter replacement needs pipe protocol expansion for request/response verbs.

### 00:45 - Architectural Discussion
User identified core design problem: hardcoded `IN ('forge.issue', 'github.issue')` means every new connector requires core code changes. The schema system (`schema.toml`) already declares event_type → table mappings, but the pipeline ignores them. A gitea/gitlab connector should be self-contained.

### 01:00 - Created [[spec-schema-driven-projection]]
New spec with 5 ECs: projection reads schemas, FTS5 reads schemas, search discovers event types from schemas, oxidize reads corpus_query, litmus test (install new schema → zero core changes). Full DESIGN.md with schema_registry table, dynamic discovery approach, convention vs registry tradeoffs.

### 01:15 - Updated All Specs
- [[spec-github-child-owns-forge]]: Checked 5 ECs (removed ForgeWriter EC as deferred), updated SPEC.md and DESIGN.md with what was done and what's deferred
- [[spec-mother-broker-github]]: Added ECs to frontmatter (`mother-run-github` + `github-events-searchable`), both checked, verification documented
- Both specs pass `patina spec check`

## Beliefs Captured: 0
_Key insight captured as [[spec-schema-driven-projection]] instead — pipeline should read schema metadata, not hardcode event types_


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 18
- Commits: 5
- Patterns Modified: 6
- Beliefs Captured: 0
- Session Tags: session-20260307-234302-claude-start..session-20260307-234302-claude-end

## User Prompts (15)

1. `[Pasted text #1 +63 lines]`
2. `did this fix the grammar forge thing`
3. `ahh can we do it all?`
4. `lets do 2 + 4 and then pause and discuss`
5. `how does mother work now .. we have specs in the queue to expand mothers function`
6. `mother-broker-github was built to not use gh anymore in our system ... we are also looking to pul...`
7. `all the init and fork and laucnher stuff is a problem for another day .. but the bit about github...`
8. `i guess i am still confused and its late .. but my understanding is we had 3 methods to capture i...`
9. `yes .. but i have a question about the design of the new  github-connector ... its suposed to be ...`
10. `where should it live as a spec?`
11. `yes`
12. `yes reality should be clear in all the needed specs`
13. `whats next for next session?`
14. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
15. `/session-end`
