---
type: session
id: 20260301-151107
title: reviewing spec [[mcp-thin-handlers]]
status: archived
llm: claude
created: 2026-03-01T20:11:07Z
start_timestamp: 1772395867909
git:
  branch: patina
  starting_commit: 01c50e967d7bc0ecf504e51412d90205df6f34f9
  start_tag: session-20260301-151107-claude-start
---

## Previous Session Context
Completed the [[mcp-typed-handlers]] spec (v0.35.2) in a single pass — defined 6 `#[derive(Deserialize)]` structs, rewrote scry.rs/spec.rs/assay.rs to eliminate 200+ type soup operations, introduced `require!()` macro and `ERR_INVALID_PARAMS` constant. Then explored the deeper architectural question of MCP handler duplication, captured [[mcp-is-discovery-cli-is-execution]] belief, and drafted the [[mcp-thin-handlers]] spec (3 phases, 10 steps, 6 exit criteria) targeting collapse of scry.rs from 1,225→~200 LOC and assay.rs from 605→~150 LOC. Session ended with a prompt to review the spec before promotion.

## Goals
- [x] reviewing spec [[mcp-thin-handlers]]

## Activity Log
### 15:11 - Session Start
Session initialized with goal: reviewing spec [[mcp-thin-handlers]]
Working on branch: patina
Tagged as: session-20260301-151107-claude-start

### 16:15 - Update (covering since 15:11)

**Git Activity:**
- Commits this session: 3 ([[commit-495ed4eb]], [[commit-bc1a4231]], [[commit-fd6b615d]])
- Files changed: 2 (`SPEC.md`, `DESIGN.md` in [[mcp-thin-handlers]])
- Last commit: spec: add 6 ADRs to mcp-thin-handlers DESIGN.md

**Deep code-verified review of [[mcp-thin-handlers]] spec.** Read all ~5,000 LOC across 14 files: MCP handlers (`src/mcp/server/scry.rs` 1,255 LOC, `src/mcp/server/assay.rs` 603 LOC), CLI equivalents (`src/commands/scry/internal/subcommands.rs`, `logging.rs`, `src/commands/scry/mod.rs`, `src/commands/assay/internal/*.rs`), reference implementation (`src/mcp/server/spec.rs`), all 3 driving beliefs ([[mcp-is-discovery-cli-is-execution]], [[mcp-is-shim-cli-is-product]], [[bridges-become-permanent]]), the predecessor [[mcp-typed-handlers]] spec+design (from git tag), and the structural audit report.

**Review findings (6 tasks):**

1. **Verified current-state claims** — function-by-function with grep and line-range comparison. Corrected: `handle_why()` shares QueryEngine (formatting-only duplication, not parallel search); `format_results()` has partial overlap with `semantic.rs` (not purely MCP-only); added `handle_use()` missing `mark_edge_usage_from_query()` gap.

2. **Assessed 3-phase structure** — Phase 1 (assay) correctly lowest-risk. Phase 2 needed restructuring: `why` separated from `detail/use` (different duplication categories). Phase 3 simplified: dropped "move format_results to CLI" (MCP-specific formatting should stay in MCP per [[mcp-is-discovery-cli-is-execution]]).

3. **Identified 4 risks** — FusedResult/ScryResult type split, edge-marking gap in MCP `handle_use()`, output format divergence requiring both sides to add formatters, QueryEngine lifetime differences.

4. **Challenged the belief** — strongest counter-argument: scry is search with consumer-specific needs, thin wrappers couple MCP evolution to CLI internals. Conclusion: trade is worth it — shared code makes divergence visible at compile time; silent drift in parallel implementations is worse.

5. **Revised exit criteria (6→7)** — relaxed scry criterion to allow MCP formatting; LOC target 500→700; added `feedback-loop-preserved`; expanded `zero-duplicate-formatters` to include log unification; grounded verification commands per [[ground-assertions-or-pay-review-tax]].

6. **Wrote DESIGN.md** with 6 ADRs resolving all pre-coding ambiguities: helper signatures (`&Connection` not `&QueryEngine` for SQL), verification enforcement (pre-push script step 6/6), `collect_rows` move order (first, not last), `expanded_terms` rollout (add to `QueryOptions` with empty default), shared formatting boundaries (typed `#[derive(Serialize)]` structs as schema contract), feedback loop testing (integration test in `logging.rs`).

**Prepared next-session prompt** for building [[mcp-thin-handlers]] — 10-commit plan, all files listed with read order, all ADR decisions referenced.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 2
- Commits: 3
- Patterns Modified: 2
- Beliefs Captured: 0
- Session Tags: session-20260301-151107-claude-start..session-20260301-151107-claude-end

## User Prompts (8)

1. `[Pasted text #1 +68 lines]`
2. `let's revise the spec with those corrections`
3. `is there a design.md for this spec?`
4. `yes fill it in`
5. `[Pasted text #1 +12 lines]`
6. `looks great .. one last cloud of doubt .. am i losing anything by pushing to cli? `
7. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
8. `/session-end`
