---
type: session
id: 20260223-092355
title: Consolidate Spec Tree (7 → 4)
status: archived
llm: claude
created: 2026-02-23T14:23:55Z
start_timestamp: 1771856635811
git:
  branch: patina
  starting_commit: 00111e7ec14afc4441055fcbaad2eada77e2dbb3
  start_tag: session-20260223-092355-claude-start
---

## Previous Session Context
Performed a full read-only audit of all 9 remaining specs — classified each as IMPLEMENTED/PARTIAL/NOT STARTED/STALE. Discovered a worldview split between Feb 17 specs (Mother-as-authority, multi-agent) and Feb 22 specs (git-as-authority, tracking rigor), abandoned 2 Feb 17 specs (agent-checklists, remote-ingestion), and finalized a **7→4 consolidation plan**: abandon mother-design, demote git-tag-system to draft, merge blocking-queue + session-hardening into one workflow spec, keep knowledge-evolution/measurement-coverage/fact-crdt-substrate as drafts. Wrote a handoff prompt for this session to execute that plan — spec-management only, no feature code.

## Goals
- [ ] Consolidate Spec Tree (7 → 4)

## Activity Log
### 09:23 - Session Start
Session initialized with goal: Consolidate Spec Tree (7 → 4)
Working on branch: patina
Tagged as: session-20260223-092355-claude-start


### 11:37 - Update (covering since 09:23)

**Git Activity:**
- Commits this session: 7 ([[commit-2f2d6dfe]], [[commit-5553219e]], [[commit-80aaee8a]], [[commit-76dcd5e0]], [[commit-7fc3b371]], [[commit-98753533]], [[commit-30aa35cb]])
- Files changed: 0 (spec management only — SPEC.md writes + archives)
- Last commit: 39 seconds ago — clean tree

**Work completed:**
- **Executed consolidation plan (7 → 5 specs)**:
  - Abandoned [[mother-design]] (design doc, not spec) — tagged `spec/mother-design`
  - Demoted [[git-tag-system]] from active → draft (zero code, misleading status)
  - Wrote new consolidated [[spec-workflow-rigor]] from [[spec-blocking-queue]] + [[session-hardening]]
  - Abandoned both source specs — tagged `spec/spec-blocking-queue` and `spec/session-hardening`
- **Audited 1-to-1 consolidation completeness** — found 3 gaps (session-hardening Phase 2a, Phase 3a, Open Questions) and fixed all
- **Triaged deferred items** — abandoned pre-commit hook, reclassified eventlog consumer as measurement concern, kept session state machine deferred
- **TLDR'd all 5 remaining specs** and mapped how they fit together
- **Deep code review of spec system** — read `src/spec.rs`, `src/commands/spec/mod.rs`, `src/commands/spec/internal.rs`, scrape layer code. Discovered existing infrastructure: `blocked_by`/`blocks` fields in `SpecFrontmatter`, `spec_deps` table, `get_ready_specs()`/`get_blocked_specs()` queries
- **Researched enterprise spec patterns** — Rust RFCs, AWS ADRs, structured RFCs. All share the same gap: `active` is a black hole with no mid-flight pause/resume
- **Major rewrite of [[spec-workflow-rigor]]** anchored to code reality:
  - Added `paused` + `blocked` as distinct statuses (the mystery middle)
  - Added `spec pause/resume/block/split/next` commands
  - Git tags on every state transition (not just archive)
  - WIP commits on pause, context diffs on resume
  - `spec split` for shipping done work while drafting remainder
  - Queue pressure: paused specs age, system nudges decisions

**Discussion context:**
- User identified core challenge: "wander-and-return pattern" — working a spec, something diverts, need exit path + return path + queue pressure
- Discussed whether `blocked` should be a formal status or derived from `blocked_by` field — resolved: both `paused` and `blocked` are formal statuses with different rules (paused = user chose to stop, blocked = can't continue)
- Compared Patina's spec system to enterprise patterns — Patina already ahead (blocking deps, archive recovery, version-linked, session tracking) but shares the same middle gap
- User emphasized: "don't rabbit hole into belief schema today — focus through the spec window"
- User pushed for `spec split` — tension mechanism preventing LLM from shipping MVPs
- User asked to lean into git — led to tag-on-every-transition design, WIP commits over stash, diff-as-context-recovery

**Key decisions:**
- **`paused` vs `blocked` are separate statuses** — different reasons, different resume rules, different queue behavior
- **Git tags on every state transition** — not just death/archive. `spec/X-paused-1`, `spec/X-resumed-1`, `spec/X-blocked-1`. Full timeline.
- **WIP commits on pause** — not stash. Durable, diffable, annotated.
- **`spec split` ships done work** — complete the done half (release), draft the remainder with `split_from` provenance. Forces honest assessment of what's actually done.
- **Queue pressure via age** — paused specs show age in `spec list` and `spec next`, escalating warnings
- **Infrastructure is 60% built** — spec-workflow-rigor is mostly wiring automation for existing infrastructure (`blocked_by`, `spec_deps`, `ready`/`blocked` queries)

**Patterns observed:**
- **"One side of the coin" problem** — query commands exist (spec blocked, spec ready) but mutation commands don't (spec block, spec unblock). Without the mutation side, the query side shows empty results.
- **Specs written without reading code are wrong** — original spec-workflow-rigor proposed building infrastructure that already existed. Code review before spec rewrite was essential.
- **Enterprise patterns share the same gap** — Rust RFCs, AWS ADRs, structured RFCs all have `active` as a black hole. Nobody solves the middle.
- **Git already has the primitives** — branches, tags, cherry-pick, diff between refs. The spec system just needs to use them intentionally.


### 12:01 - Final Update (covering since 11:37)

**Git Activity:**
- Commits this session: 9 (added [[commit-fa79ec97]], [[commit-83a2faa8]] since last update)
- Files changed: 0 (spec management + beliefs only)
- Last commit: 2 minutes ago — clean tree

**Work completed:**
- **Captured 2 beliefs** from design discussion:
  - [[mutation-completes-query]] — query commands without mutation commands are dead infrastructure
  - [[active-is-a-black-hole]] — every spec system treats active as one state with no mid-flight handling
- **Discussed spec system with user** — identified the "wander-and-return" pattern as the core use case, distinguished `paused` (user choice) from `blocked` (dependency), designed `spec split` for the half-done problem
- **Integrated external agent review feedback** — 7 concerns addressed with Design Decisions section (D1-D7):
  - D1: WIP commit optional on clean tree, rollback on failure
  - D2: Tag counter stateless (parse existing tags)
  - D3: `spec block` appends to existing `blocked_by`, updates DB inline
  - D4: `paused_at_tag` stored in YAML for resume diffs
  - D5: Split ID defaults `<id>-v2`, `--id` override
  - D6: ISO 8601 UTC dates
  - D7: Session integration contract deferred to Phase 5
- **Established one-paused-spec rule** — the queue constraint that replaces day-based staleness timers. Can't pause another spec until you resolve the existing one.

**Key decisions:**
- **One paused spec at a time** — if you have two paused specs, you're avoiding, not pausing. The queue itself is the pressure, not a timer.
- **`paused_at_tag` in YAML** — resume reads this directly instead of parsing tags. Explicit over derived.
- **No day-based staleness** — artificial timers are "making up stuff." The constraint (one pause slot) creates real pressure.
- **Design Decisions section in specs** — external review concerns resolved as one-liner rules, not edge case bloat.

**Patterns observed:**
- **Constraints > timers** — a structural limit (one paused spec) is better pressure than an arbitrary day count. The system forces decisions, not nagging.
- **External review finds real gaps** — the 7 concerns were legitimate (tag sequencing, DB sync, resume discovery). Addressing them tightened the spec without bloating it.


## Beliefs Captured: 2

## Session Classification
- Work Type: pattern-work
- Files Changed: 8
- Commits: 9
- Patterns Modified: 7
- Beliefs Captured: 2
- Session Tags: session-20260223-092355-claude-start..session-20260223-092355-claude-end

## User Prompts (25)

1. `[Pasted text #1 +85 lines]`
2. `lets discuss the specs you edited .. are we 1 to 1 what was removed and why`
3. `yes fix all 3`
4. `lets chat about deferred as deferred usually is a grave yard .. what could they do for us and why...`
5. `abandon Phase 2c: Pre-commit hook it feels like 2b is measurement right? oh and keep 3a deffer`
6. `ok wheer are we now with our specs .. give me a tldr of each and how they may fit togther? then i...`
7. `one of the challenges i face is the problem wher i am working a spec and something comes up the d...`
8. `so that i understand is spec workflow rigor gives us a foundation that we can later build on to h...`
9. `ok deep dive into our spec code and spec commands and let me see the full picture of spec. patina...`
10. `so workflow-rigor is mostly wiring up automation for infrastructure that already exists?`
11. `so lets chat about this schema redesign as it realtes to what we are to wire up and finish with w...`
12. `compare this concept that 1 side of a 2 side coin is enough with out other spec subcommands`
13. `ok so we are circling an architecture vocab and data flow design it sounds like (my description i...`
14. `makes sense yes? i really dont want to rabbit hole this though and spend the day chasing clouds ....`
15. `yes sharpen it .. lets make sure it anchors in our reality as it seems to have been written witho...`
16. `ok .. i love this all .. tell me how others do this at the enterprise level ... we afre getting t...`
17. `maybe paused with strict rules to move it to active or abandoned also a way to split spec .. so t...`
18. `blocked is still a thing .. and we need a better split loop but this is on the right path .. we n...`
19. `ok lets update the spec with all of this`
20. `/session-update`
21. `yes capture both`
22. `here are some concrens from outside agent review: [Pasted text #1 +14 lines] I am also concerned ...`
23. `days is sucha bad metric .. 7 days feel slike a lifetime but the real issue is we are making up s...`
24. `yes and update the spec with all the review feedback`
25. `/session-end`
