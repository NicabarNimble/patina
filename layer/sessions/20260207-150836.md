---
type: session
id: 20260207-150836
title: continue retrieval-tuning — semantic sanity battery + FTS5 audit
status: archived
llm: claude
created: 2026-02-07T20:08:36Z
start_timestamp: 1770494916220
git:
  branch: patina
  starting_commit: 00389b27bf63e607821f092547a04bec91de734c
  start_tag: session-20260207-150836-claude-start
---

## Previous Session Context
Expanded NL eval from 25→52 queries with train/test split (32/20), added per-intent metrics, then ran Phase 2 intent-aware weighting: suppressed semantic/persona oracles (0% contribution from E5-base-v2) and redirected dead boosts, yielding NL P@10 +4.4pp (37.7%), co-change P@10 +14.9pp (73.1%), test set validated at +3.3pp. Key insight: removing noise beats adding signal in RRF. Deferred Phase 3 (belief multiplier) and identified two quick diagnostics from external review: **semantic sanity battery** (verify embeddings pipeline actually works before concluding model mismatch) and **FTS5 corpus audit** (check if core docs are indexed, tokenization issues).

## Goals
- [x] A0: Semantic sanity battery — diagnose pipeline
- [x] C0: FTS5 corpus audit — check layer/core indexing
- [x] Fix F6 doc_id mapping bug
- [ ] ~~Phase 3: Belief score multiplier~~ — blocked by error analysis findings

## Activity Log
### 15:08 - Session Start
Session initialized with goal: continue retrieval-tuning — semantic sanity battery + FTS5 audit
Working on branch: patina
Tagged as: session-20260207-150836-claude-start

### 15:10-15:30 - A0: Semantic sanity battery
Traced the full semantic pipeline: `create_embedder()` → e5-base-v2 (768 dims) →
projection (768→256) → USearch index → enrichment. Found:
- Semantic oracle IS functional — returns 10 results per query
- Index contains ~27K items: 23,820 session events (88%), 1,883 code facts,
  806 patterns, 1,778 commits, 47 beliefs
- Session events dominate — session-trained projection maps NL queries to
  session-space. Results are mostly session IDs, not code/patterns
- Verdict: model+training mismatch, not pipeline bug. Semantic suppression
  (weight=0.0) from Phase 2 is correct.

### 15:30-15:45 - C0: FTS5 corpus audit
Checked `layer/core/*.md` indexing in FTS5:
- 7 core files + 2 core beliefs ARE indexed in `pattern_fts` (806 total patterns)
- FTS5 tokenization works correctly — `porter unicode61` splits hyphens
- **Found F6:** `scry_lexical()` returns `source_id: id` (e.g., "dependable-rust")
  for pattern results, but eval expects `file_path` (e.g., "layer/core/dependable-rust.md")
- Same bug in semantic enrichment (`enrichment.rs`)
- 19/52 queries (37%) expect `layer/` file paths → all get 0 credit

### 15:45-15:55 - Spec-first, then code
User enforced spec-before-code discipline. Updated [[retrieval-tuning]] SPEC.md
with Phase 2.5 diagnostics (A0 findings, C0 findings, F6 bug, fix plan) before
making code changes.

Committed: [[70ded419]] `spec: document Phase 2.5 diagnostics — A0 semantic battery + C0 FTS5 audit`

### 15:55-16:10 - F6 fix implementation
Changed `source_id: id` → `source_id: file_path` in:
- `src/commands/scry/internal/search.rs` (scry_lexical pattern results)
- `src/commands/scry/internal/enrichment.rs` (semantic enrichment)
- Updated self-filters in `src/commands/scry/internal/search.rs` (scry_belief)
  and `src/commands/belief/mod.rs` to handle file_path format

Committed: [[94ef9485]] `fix: use file_path as pattern doc_id in lexical and semantic results (F6)`

### 16:10-16:20 - F6 eval results — near-zero impact
Ran eval after F6 fix: P@10 38.3% (unchanged), P@5 -1.3pp, test P@10 -1.7pp.
Applied [[andrew-ng-over-shoulder]]: fix changed 37% of queries but moved nothing.
Instead of moving to Phase 3, user demanded error analysis.

Committed: [[e9c04eee]] `spec: document Phase 2.5 results — F6 fix has near-zero eval impact`

### 16:20-16:40 - Error analysis (in progress, interrupted)
Systematic trace of 19 queries expecting `layer/` files:
- **7/24 expected files appear in top 10, 17 do not**
- Found `dependable-rust.md` collision: dust archive version overwrites core
  version because both derive `id: dependable-rust` (core from frontmatter,
  dust from filename). PRIMARY KEY conflict → last-processed wins.
- `safety-boundaries.md` has highest BM25 score (11.63) in pattern_fts but
  doesn't appear in top 10 — **BM25 scores are incomparable across FTS5 tables**
  (different column counts: code_fts=4, pattern_fts=6, commits_fts=3).
  `scry_lexical()` naively mixes them.
- Root cause chain: F6 (doc_id mapping) was real but surface-level.
  Deeper issues: (1) pattern ID collision from dust archives, (2) BM25 score
  scale mismatch across FTS5 tables in `scry_lexical()`.

### Patterns Observed
- **Spec-before-code discipline** — user enforced CLAUDE.md rule. Writing the
  spec first would have caught that "fix doc_id" was necessary-but-not-sufficient
  before wasting a commit on it.
- **Andrew Ng check caught us** — we rationalized a zero-impact change as
  "necessary but not sufficient" instead of investigating why it didn't work.
  The error analysis revealed two deeper bugs we would have missed.
- **BM25 cross-table comparison is invalid** — `scry_lexical()` mixes scores
  from 3 FTS5 tables with different column counts. This is a fundamental
  ranking bug, not just a weight issue.
- **Pattern ID collision** — `dependable-rust` exists in both `layer/core/` and
  `layer/dust/archive/`. The dust version has no frontmatter, so it derives the
  same `id` from filename. PRIMARY KEY insert overwrites the core version.


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: pattern-work
- Files Changed: 4
- Commits: 3
- Patterns Modified: 1
- Beliefs Captured: 0
- Session Tags: session-20260207-150836-claude-start..session-20260207-150836-claude-end

## User Prompts (5)

1. `[Pasted text #1 +68 lines]`
2. ` rule to make sure we have a spec before we make changes .. i get that we can use git to watch bu...`
3. `whats andrew ng going to tell us`
4. `yes do the error analysis`
5. `/session-end `
