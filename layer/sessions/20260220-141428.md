---
type: session
id: 20260220-141428
title: ssh patina not working .. it opens and launches claude but claude show mcp not starting AND it wants user to login when it should be pulling the long-term token.
status: archived
llm: claude
created: 2026-02-20T19:14:29Z
start_timestamp: 1771614869080
git:
  branch: patina
  starting_commit: 987b6e85c36a6cac990898744f256fbb145e749d
  start_tag: session-20260220-141428-claude-start
---

## Previous Session Context
Fixed keychain SSH access by changing policy from `AfterFirstUnlockThisDeviceOnly` to `AlwaysThisDeviceOnly` - the Feb 19 fix solved macOS 26 API breakage but introduced SSH regression. Migrated vault, verified local token injection works. Created spec-keychain-macos26-regression and updated two beliefs. Left open: SSH testing from MacBook Air to verify launcher works in new sessions - which is now the current issue.

## Goals
- [ ] ssh patina not working .. it opens and launches claude but claude show mcp not starting AND it wants user to login when it should be pulling the long-term token.

## Activity Log
### 14:14 - Session Start
Session initialized with goal: ssh patina not working .. it opens and launches claude but claude show mcp not starting AND it wants user to login when it should be pulling the long-term token.
Working on branch: patina
Tagged as: session-20260220-141428-claude-start


### 14:21 - Update (covering since 14:14)

**Git Activity:**
- Commits this session: 0
- Files changed: 4
- Last commit: 89 minutes ago

**Work completed:**
- Diagnosed root cause of SSH launcher failure: traced token injection code in `src/commands/launch/internal.rs:556`
- Found `try_get_claude_token()` function calls `patina::secrets::get_global_secret("claude-oauth")`
- If keychain read fails, error prints to stderr but launcher continues without token → causes login prompt
- MCP startup failure likely secondary symptom of missing authentication
- Explored launcher code flow: vault read → token injection → exec Claude Code with CLAUDE_CODE_OAUTH_TOKEN env var

**Discussion context:**
- User asked key question: "we need a window into why its failing? can we log the info?"
- Explored options: (1) add debug logging, (2) reboot Mac Studio (hard), (3) new SSH session (easy)
- User prefers new SSH session over reboot, wants visibility into failure mode
- Hypothesis: Previous session's [[commit-a4f68d52]] fixed keychain policy but may not apply to existing SSH sessions
- Keychain access might require new SSH connection to pick up `AlwaysThisDeviceOnly` policy

**Key decisions:**
- **Test first, then log**: Start with new SSH session test before adding verbose logging (faster diagnostic)
- **Three-step validation plan**:
  1. New SSH session to Mac Studio (avoids stale keychain access)
  2. Direct vault test: `patina secrets run -- printenv CLAUDE_CODE_OAUTH_TOKEN`
  3. If vault works, capture launch stderr: `patina launch 2>&1 | tee /tmp/launch-debug.log`
- If keychain still blocks SSH → investigate why `AlwaysThisDeviceOnly` policy not working
- If keychain works but launch fails → separate MCP server initialization issue

**Challenges faced:**
- Initial search for launcher scripts looked in wrong places (resources/adapters, .config/claude)
- Launcher code is embedded in Rust, not external shell scripts
- Error messages go to stderr and may not be visible in SSH context

**Patterns observed:**
- [[context-before-code]]: Read launch/internal.rs to understand token injection flow before proposing changes
- Fail-soft design: Vault errors don't block launch, but cause degraded UX (login prompt)
- SSH environment isolation: Existing sessions may not see keychain policy changes without reconnecting

**Next steps (when user returns):**
- [ ] Review test results from new SSH session
- [ ] If `patina secrets run` fails → keychain policy still blocking SSH access
- [ ] If vault works but launch fails → investigate MCP server initialization
- [ ] Consider adding verbose debug logging to `get_global_secret()` for better SSH diagnostics
- [ ] May need to examine macOS keychain SSH access rules for `AlwaysThisDeviceOnly` policy


## Beliefs Captured: 0
_No beliefs captured this session_

## Session Classification
- Work Type: exploration
- Files Changed: 0
- Commits: 0
- Patterns Modified: 0
- Beliefs Captured: 0
- Session Tags: session-20260220-141428-claude-start..session-20260220-141428-claude-end

## User Prompts (3)

1. `we need a window into why its failing? can we log the info? or no? do maybe we need to reboot the...`
2. `ok i will need to /session-end and then exit to tes this so make sure to capture all this context...`
3. `/session-update `
