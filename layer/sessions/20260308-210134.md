---
type: session
id: 20260308-210134
title: Finalize Protocol Alignment — PipeError vs Transport Errors
status: archived
llm: claude
created: 2026-03-09T01:01:34Z
start_timestamp: 1773018094287
git:
  branch: patina
  starting_commit: 4925e21401006cd12de15bb6c1e5a608196fdd26
  start_tag: session-20260308-210134-claude-start
---

## Previous Session Context
Last session "Resolve Cross-Spec Conflicts — Sandbox, Streaming, Protocol Surface" (20260308-184638) resolved 4 cross-spec conflicts and 1 open question across 8 commits: promoted pipe/ingest to first-class method in pipe-architecture, parameterized sandbox profiles by child type (scoped filesystem access, not weakened globally), bounded batching for streaming (O(1) transport, O(batch) broker), replaced HashMap source identity with opaque `source_path` + `path_template` in schema.toml. An outside agent review caught 4 additional issues (layout ownership split, missing ingest dispatch, HashMap ordering, lakehouse overclaim) — all fixed. Created belief [[sandbox-profiles-are-parameterized]] and revised [[events-are-autobiography-not-telemetry]] to project-scope. **One residual issue identified:** PipeError vs transport error handling is inconsistent in spec — the code (`lib.rs:254`) correctly uses `-32601` directly but the spec run() example uses `PipeError::Fatal`. This is the direct input for this session.

## Goals
- [ ] Finalize Protocol Alignment — PipeError vs Transport Errors

## Activity Log
### 21:01 - Session Start
Session initialized with goal: Finalize Protocol Alignment — PipeError vs Transport Errors
Working on branch: patina
Tagged as: session-20260308-210134-claude-start


### 21:06 - Update: Residual resolved + cross-spec audit clean

**Git Activity:**
- Commits this session: 2 ([[commit-01cebc0a]], [[commit-a0b04fad]])
- Files changed: 2 (pipe-architecture DESIGN.md, raw-lake-ingestion DESIGN.md)
- Working tree: clean (uncommitted changes only in `layer/events.jsonl`)

**Residual issue resolved — Option (a): spec aligns to code** ([[commit-01cebc0a]])
Read all 4 core principles, 4 code files (`patina-pipe/src/lib.rs`, `protocol.rs`,
`patina-pipe-types/src/lib.rs`, `error.rs`), then the spec sections.

The code was already correct:
- `lib.rs:254` — `_ =>` arm uses `Response::error(id, -32601, "Method not found")` directly
- `error.rs` doc comment explicitly says "JSON-RPC codes are transport detail, not here"
- All standard JSON-RPC errors (-32600..-32700) bypass PipeError throughout `run()`

Three contradictions in [[spec-pipe-architecture]] DESIGN.md §7.2:
1. Description claimed "Child trait has default implementations that return MethodNotFound" — false, the `run()` dispatcher handles this at transport level
2. pipe/ingest comment said "return MethodNotFound, which is correct" — misleading source attribution
3. `_ =>` arm used `PipeError::Fatal { message: "Method not found" }` — routes through child-facing enum for a transport concern

Applied [[dependable-rust]] test: Option (a) keeps PipeError at 4 variants (smallest interface).
Applied [[unix-philosophy]] test: Option (a) gives PipeError one job (child-facing errors only).
Options (b) and (c) grow the interface for something the code already handles correctly.

Also added explicit **boundary rule** to §1.5: `-32001..-32004` pass through PipeError,
`-32600..-32700` are serialized directly by transport layer, never touch PipeError.

**Cross-spec consistency audit** — ran across all 6 files (3 SPECs + 3 DESIGNs):
- [[spec-pipe-architecture]], [[spec-raw-lake-ingestion]], [[spec-connector-owns-tables]]
- 5-question role alignment test: all pass
- Cross-references: all aligned
- Stale language: 1 minor finding — raw-lake-ingestion §Sandbox Profile referenced
  an outdated "conflict" with pipe-architecture §8.3 that was already resolved

**Stale reference fixed** ([[commit-a0b04fad]])
Updated raw-lake-ingestion DESIGN.md sandbox profile note to reference the current
pipe-architecture §8.3 parameterized sandbox spec instead of claiming to "resolve a conflict."

**Compression check:** No code written. No duplication across spec files — each spec
references the others via wikilinks rather than repeating content. Clean.


### 21:35 - Update: Gap analysis + outside audit → build plan

**Git Activity:**
- Commits this session: 6 ([[commit-01cebc0a]], [[commit-a0b04fad]], [[commit-3ef89ee4]], [[commit-b4b549a1]], [[commit-41b84311]], [[commit-39304424]])
- Files changed: 5 (pipe-architecture SPEC.md, pipe-architecture DESIGN.md, raw-lake-ingestion DESIGN.md, 2 belief files)
- Working tree: uncommitted changes only in `layer/events.jsonl`

**Implementation gap analysis** ([[commit-3ef89ee4]])
Analyzed code vs spec across all three specs. The remaining work is not blank-slate —
it is where existing pieces assume "project events.db only." Identified three major seams:
- **Seam 1:** Multi-destination broker (transport/spawn first, then routing)
- **Seam 2:** Schema as contract layer (parallel, but model before consumers)
- **Seam 3:** Lake control plane (depends on Seam 1)

DESIGN.md §Key Files reclassified: `patina-pipe-types`, `patina-pipe`,
`github-connector`, `src/broker/` moved from "new code to build" to "already built."

**Outside audit integration** ([[commit-41b84311]])
Audit agent identified the key risk: each seam contains a shared contract change
that build agents underestimate. Five specific traps documented:
1. Generalizing `write_facts_with_cursor()` for lakes (wrong guarantees)
2. Schema validation too weak for lake dedup + contract projection
3. Solving Seam 2 only in `scrape/events.rs` (4 other files have suffix logic)
4. Accidentally unifying the WASM path (scope creep)
5. Underestimating schema migration (live table names + schema_registry)

Intra-seam ordering rule added: transport/spawn before broker routing (Seam 1),
schema model before consumer rewiring (Seam 2), lakehouse binary last (Seam 3).

**Beliefs captured:**
- [[implementation-is-three-seams]] ([[commit-b4b549a1]]) — seam decomposition
- [[contracts-before-consumers]] ([[commit-39304424]]) — intra-seam ordering rule

**Next session decision:** Option B — Seam 2 schema contract model. Promote
[[spec-connector-owns-tables]] to active. Expand schema parser to handle
`identity_fields`, `projections`, `contracts`, `path_template`, lake metadata.
Design as shared contract, not CLI command logic. Stop before touching consumers.
Prompt written and printed to screen.

**Compression check:** No code written. No duplication — seam analysis lives in
pipe-architecture SPEC.md only, individual specs reference it via wikilinks.


## Beliefs Captured: 2

## Session Classification
- Work Type: pattern-work
- Files Changed: 5
- Commits: 6
- Patterns Modified: 5
- Beliefs Captured: 2
- Session Tags: session-20260308-210134-claude-start..session-20260308-210134-claude-end

## User Prompts (7)

1. `[Pasted text #1 +78 lines]`
2. `[Pasted text #1 +17 lines]`
3. `ignore the note as we will depricate that soon .. but do wire this into our spec`
4. `from my outside audit agent: [Pasted text #2 +25 lines]`
5. `ok i think we are ready for a build next session .. where should we start? lets discuss`
6. `write a prompt (print to screen) for next session, we won't have context. anchor in layer/core va...`
7. `/session-end`
