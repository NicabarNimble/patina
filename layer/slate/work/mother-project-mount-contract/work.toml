id = "mother-project-mount-contract"
title = "Enforce project mount contract for filesystem children"
kind = "build"
status = "complete"
human_request = "Move host-to-guest project path mapping out of Slate child fallback logic and into Patina/Mother runner behavior so project filesystem children receive /project rather than host absolute paths."
allium_anchors = ["layer/slate/work/mother-project-mount-contract/allium/mother-project-mount-contract.allium"]
user_alignment = "User wants filesystem child path/mount behavior to become a Mother/runtime invariant: Patina resolves the host project, the runner mounts it at /project, children receive guest paths only, and Slate stops guessing /input/Users mappings."
belief_refs = [
    "control-plane-authority-distributed-execution",
    "explicit-fail-closed-over-hidden-fallbacks",
    "contracts-outlive-runtimes",
    "safety-boundaries",
]
proof_plan = [
    "[x] `cargo test slate::tests` passes in Patina.",
    "[x] `cargo test commands::mother::daemon::tests::builtin_spec_dispatch_execute_forwards_project_route_to_slate_manager` passes in Patina.",
    "[x] `cargo test commands::mother::loader::tests` passes in Patina.",
    "[x] `cargo test --manifest-path /Users/nicabar/Projects/Patina/patina-child-slate/Cargo.toml` passes after removing fallback logic.",
]
closure_evidence = [
    "Added canonical guest project mount `patina::child::engine::GUEST_PROJECT_ROOT` with value `/project` and `FilesystemPreopen::project_read_write(...)` for read-write dynamic project mounts.",
    "Updated child instantiation to let dynamic preopens override manifest preopens with the same guest path, so stale/local manifest `/project` scopes cannot shadow the runner-selected project mount.",
    'Updated `patina slate` to resolve the host project root, mount it dynamically at `/project`, and pass `project = "/project"` to Slate instead of a host absolute path.',
    "Updated generic `patina child run` / `patina child call` and Mother daemon child loading to add `/project` when a current Patina project is available and the child has filesystem authority.",
    "Updated builtin spec dispatch to forward `/project` to Slate while preserving the host route as `origin_project` for fallback envelopes.",
    "Removed Slate child source fallback that remapped host absolute paths to `/input/<host-path>` in `/Users/nicabar/Projects/Patina/patina-child-slate` commit `aadfba8`.",
    "Built and locally reinstalled slate-manager from `/Users/nicabar/Projects/Patina/patina-child-slate/target/wasm32-wasip1/release/patina_ai_child_slate_manager.wasm` after removing fallback logic.",
    "Verified installed Slate through the new runner with `cargo run -- slate show mother-project-mount-contract --json`, which succeeded via the `/project` mount contract.",
    "Validated Patina with `cargo test slate::tests`, `cargo test commands::mother::daemon::tests::builtin_spec_dispatch_execute_forwards_project_route_to_slate_manager`, `cargo test commands::mother::loader::tests`, `cargo clippy --lib -- -D warnings`, and `cargo fmt --check`.",
    "Validated external Slate child with `cargo test --manifest-path /Users/nicabar/Projects/Patina/patina-child-slate/Cargo.toml`, targeted path fallback test, `cargo clippy --manifest-path /Users/nicabar/Projects/Patina/patina-child-slate/Cargo.toml --all-targets -- -D warnings`, and `cargo fmt --check --manifest-path /Users/nicabar/Projects/Patina/patina-child-slate/Cargo.toml`.",
]
blocked_by = []
implementation_plan = [
    "[x] Add a canonical guest project mount constant/helper and dynamic read-write project preopen for filesystem children.",
    "[x] Update child runner/daemon load paths to provide the same `/project` dynamic preopen when a project is available and the child has filesystem authority.",
    "[x] Remove Slate child `/input/<host-absolute-path>` fallback and validate project resolution fails closed for host absolute paths inside the child.",
    "[x] Update `patina slate` to mount the resolved host project at `/project` and pass a guest project path to Slate.",
]
belief_harvest_decision = "No new belief file created. This work applies existing beliefs about control-plane-owned authority, explicit fail-closed boundaries, runtime-independent contracts, and safety boundaries to enforce the /project guest mount contract."
created_at = "1779294475"
updated_at = "1779295112"
closed_at = "1779295112"
