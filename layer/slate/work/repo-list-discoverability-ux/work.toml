id = "repo-list-discoverability-ux"
title = "Improve repo list discoverability"
kind = "build"
status = "active"
human_request = "Make registered reference/source repos easier for humans and LLMs to locate: agents should use `patina scry`/`patina assay` first, then `patina repo list <partial-name>` to find cached source paths; JSON output should expose stable repo metadata for tool use."
allium_anchors = ["layer/allium/reference-repo-discovery.allium"]
user_alignment = "Split from the completed Mother child skill bundle projection work and sharpened into reference-repo discovery: this supports HITL/Mother capability research without becoming skill projection lifecycle runtime behavior."
belief_refs = []
belief_harvest_decision = "no new belief required; the reusable source-research guidance is captured as behavior in `layer/allium/reference-repo-discovery.allium` and as operational guidance in `.pi/skills/patina-mother-system/SKILL.md`"
proof_plan = ["""
[x] Allium anchor `layer/allium/reference-repo-discovery.allium` captures source-research guidance, partial repo-name lookup, filtered path visibility, JSON output, and no-match success semantics.
[x] `allium check layer/allium/reference-repo-discovery.allium` passes and `layer/allium/reference-repo-discovery.plan.json` is generated.
[x] `patina repo list <partial-name>` filters registered repo names by case-insensitive substring; for example `flu` can match `withastro/flue`.
[x] Filtered human output includes local cache paths by default, while unfiltered default list remains compact.
[x] `patina repo list <partial-name> --json` emits `patina.repo.list.v1` with query, count, and repository metadata including name, github, path, domains, mode, contrib, registered, and synced commit.
[x] No-match searches succeed with clear human guidance and JSON `{ count: 0, repositories: [] }` semantics.
[x] Tests cover partial-name filtering, JSON output, and no-match behavior; manual CLI checks cover filtered path display and `--status` preservation.
"""]
closure_evidence = [
    "Split out from `mother-child-skill-bundle-projection` after the HITL skill lifecycle proof was satisfied; repo-list UX supports future capability research but does not belong to the skill projection lifecycle closure gate.",
    "Added linked Allium anchor `layer/allium/reference-repo-discovery.allium` and generated `layer/allium/reference-repo-discovery.plan.json`; `allium check layer/allium/reference-repo-discovery.allium` passes.",
    "Implemented `patina repo list <partial-name>` as case-insensitive registered-name substring filtering in `src/commands/repo/mod.rs`.",
    "Implemented `patina repo list <partial-name> --json` with stable `patina.repo.list.v1` envelope containing query, count, and repository metadata including path, domains, mode, contrib, registered timestamp, and synced commit.",
    "Filtered human output now includes cache paths by default; unfiltered default output remains compact, and `--status` remains opt-in while also working with filtered path output.",
    "No-match partial searches return success with human next-action guidance; JSON no-match output returns `count: 0` and `repositories: []`.",
    "Updated `.pi/skills/patina-mother-system/SKILL.md` to direct agents toward `patina scry`/`patina assay` before raw source reads, then `patina repo list <partial-name>` for cached source paths.",
    "Validation passed: `cargo fmt --check`, `cargo check`, `cargo test repo::tests -- --nocapture`, `allium check layer/allium/reference-repo-discovery.allium`, and `allium analyse layer/allium/reference-repo-discovery.allium`.",
    "Manual CLI checks passed for `target/debug/patina repo list flu`, `target/debug/patina repo list flu --json`, no-match human/JSON output, and `target/debug/patina repo list flu --status`.",
]
blocked_by = []
implementation_plan = ["""
[x] Read current repo registry/list implementation and cached repo metadata shape.
[x] Define precise behavior in linked Allium: name-only partial lookup, JSON for LLMs/tools, filtered path visibility, and no-match success.
[x] Promote this Slate to active before source edits.
[x] Implement `patina repo list <partial-name>` as case-insensitive substring filtering over registered repo names.
[x] Implement `--json` for list output with stable `patina.repo.list.v1` envelope.
[x] Include cache paths in filtered human output by default without changing compact unfiltered output.
[x] Add focused tests for partial filtering, JSON, no-match, plus manual checks for filtered path display and `--status` behavior.
[x] Document/encode source-research guidance: use `patina scry` and `patina assay` first, then `patina repo list <partial-name>` when direct cached source inspection is needed.
"""]
created_at = "1778770837"
updated_at = "1778790954"
