id = "pre-push-scaffold-paths-fix"
title = "Fix scaffold paths pre-push blocker"
kind = "fix"
status = "complete"
human_request = "Push the current Patina branch and open a PR for merge; the pre-push hook blocked on a hardcoded project .patina path in child scaffolding, so fix that minimal issue and continue the push/PR flow."
allium_anchors = ["layer/slate/work/pre-push-scaffold-paths-fix/allium/pre-push-scaffold-paths-fix.allium"]
user_alignment = "no-behavior-change rationale for a refactor: this scalpel change replaces a hardcoded project .patina path with the existing crate::paths project helper so the runtime boundary drift pre-push check passes, then push and create the PR the user requested. It does not change scaffold output or command behavior."
belief_refs = [
    "scalpel-commits",
    "read-code-before-write-code",
]
proof_plan = [
    "[x] `cargo test --test child_init_typed_default` passes.",
    "[x] `cargo fmt --check` passes.",
    "[x] `git push origin patina` passes Tier 1 pre-push checks.",
]
closure_evidence = [
    'Replaced `project_dir.join(".patina")` in `src/child/scaffold.rs` with `crate::paths::project::patina_dir(&project_dir)`, preserving the generated `.patina/children-dev.toml` layout while using the canonical project path helper.',
    "Validated focused behavior with `cargo test --test child_init_typed_default` and formatting with `cargo fmt --check`.",
    "Validated the pre-push path guard by rerunning `git push origin patina`; Tier 1/Tier 2 checks passed and `patina` pushed to origin through `[[commit-2ad54d69]]`.",
]
blocked_by = []
implementation_plan = [
    "[x] Replace child scaffold hardcoded project .patina join with the crate::paths project helper.",
    "[x] Run focused scaffold/template checks and pre-push path validation via git push.",
]
belief_harvest_decision = "No new belief file created. This work applies existing `scalpel-commits` and `read-code-before-write-code` guidance to a no-behavior-change pre-push blocker fix."
created_at = "1779203026"
updated_at = "1779203291"
closed_at = "1779203291"
