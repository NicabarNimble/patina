id = "mother-markdown-artifact-buffer"
title = "Mother markdown artifact buffer"
kind = "build"
status = "complete"
human_request = "Add a markdown artifact major mode so Mother can open README.md as a Mother-owned view buffer and frames such as SvelteKit can render it as markdown."
allium_anchors = [
    "layer/allium/mother/mother-view-composer-target.allium",
    "layer/allium/mother/mother-view-composer-current.allium",
]
user_alignment = "User identified the Emacs-like buffer model and asked to create a Slate for the missing README markdown artifact view: shape/source/major_mode/payload fields so Mother can open *README.md*."
belief_refs = [
    "[[control-plane-authority-distributed-execution]]",
    "[[contract-first-execution]]",
]
proof_plan = [
    "[x] Mother exposes a README markdown artifact shape with `major_mode = markdown` and `payload_contract = framed-json`.",
    "[x] Opening the README shape creates a live Mother-owned `*README.md*` buffer whose payload includes path, content, modified_at, git_status, and diff metadata where available.",
    "[x] `GET /api/view-buffers/<buffer_id>/payload` returns the markdown framed-json payload after reopen/reconnect.",
    "[x] SvelteKit frame renders the README markdown payload without owning runtime state.",
    "[x] Tests cover markdown artifact payload construction and observability-gap behavior when README.md is missing/unreadable.",
]
closure_evidence = [
    "Implemented README markdown shape `project.artifact.README.md.markdown` with `major_mode = markdown`, `payload_contract = framed-json`, and `*README.md*` buffer naming through existing Mother view buffer APIs.",
    "Implemented README artifact facts for path, content, modified_at, git_status, and diff_hunks; payload replay via opened_buffer_payload reconstructs framed JSON from Mother-owned buffer state.",
    "Updated SvelteKit BufferPayload to render markdown `content` directly with path and git status metadata while keeping Mother as runtime state owner.",
    "Validation passed: `cargo test -q -p mother`, `cargo check -q`, `cd frames/sveltekit && npm run check`, `cd frames/sveltekit && npm run build`.",
    "Restarted launchd-managed Mother on installed v0.71.0 and live-smoked README markdown buffer over UDS: shape listed, open returned `*README.md*` markdown buffer, payload path resolved to project README, and payload replay returned content via `/api/view-buffers/<buffer_id>/payload`.",
    "Added `patina mother view` CLI under Mother with `shapes`, `buffers`, `open`, and `payload`; `cargo run -q -- mother view open README.md` opens the Mother-owned `*README.md*` buffer and renders markdown content in terminal.",
    "Added `patina mother view open <target> --tmux` with Doom/vi-style `-s/--below` and `-v/--right`; tmux panes run `patina mother view payload <buffer-id> | less -R -X` so mouse/tmux scrollback behaves better than the initial plain less pane.",
]
blocked_by = []
target = "Mother View Composer markdown artifact/readme buffer follow-up"
implementation_plan = [
    "Add a project/file artifact source path for README.md to Mother view buffer catalog/service without making SvelteKit own file state.",
    "Define a markdown major-mode shape for README.md with `payload_contract = framed-json` and payload fields: path, content, modified_at, git_status, and optional diff hunks.",
    "Expose/open the buffer through existing Mother view APIs so frames can render `*README.md*` by fetching `/api/view-buffers/<buffer_id>/payload`.",
    "Update SvelteKit BufferPayload markdown rendering if needed so the framed-json markdown payload is displayed as readable README content.",
]
belief_harvest_decision = "no new belief; reinforces existing Mother-owned buffers/renderers-do-not-own-state and contract-first execution doctrine"
created_at = "1778531492"
updated_at = "1778586922"
closed_at = "1778535965"
