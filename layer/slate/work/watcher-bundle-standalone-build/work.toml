id = "watcher-bundle-standalone-build"
title = "Build watcher children externally"
kind = "build"
status = "complete"
human_request = "Move/copy folder-watch-actor and watch-null-sink into ~/Projects/Patina/patina-child-watcher-system and make both children buildable/installable from that external repo using published patina-sdk and clean child manifests."
allium_anchors = ["layer/slate/work/children-storage-cleanup/allium/children-storage-cleanup.allium"]
user_alignment = "User already has an empty watcher-system repo and wants the existing non-legacy watcher children moved there like Slate, without breaking Patina core during extraction."
belief_refs = []
proof_plan = ["""
[x] `/Users/nicabar/Projects/Patina/patina-child-watcher-system` contains standalone child packages for `folder-watch-actor` and `watch-null-sink` with no monorepo-relative `patina-sdk` path dependencies.
[x] Both children have normalized `child.toml` metadata, including name, version, kind, role, ingress/contract where applicable, needs/toys/scopes, and Patina minimum when decided.
[x] Both children build from the external repo with `cargo component build --release` or documented per-child build commands.
[x] Both children install locally via `patina child install <child-package> --wasm <artifact> --force` without manual file copying.
[x] Local smoke proves the actor and null sink can be loaded/called from installed external artifacts.
[x] Patina monorepo sources remain untouched except for documentation/evidence until external proof is complete.
"""]
closure_evidence = [
    "External watcher repo `/Users/nicabar/Projects/Patina/patina-child-watcher-system` now contains `Cargo.toml`, `README.md`, `RELEASING.md`, `children/folder-watch-actor`, and `children/watch-null-sink`; committed externally as [[commit-d2be416]]; no push was attempted because the external repo has no git remote configured.",
    'Both external child crates use published `patina-sdk = "0.22.0"` and no monorepo-relative SDK path dependencies remain under the external `children/` tree.',
    'Both external manifests now include `[child].version = "0.1.0"` and `patina_min = "0.71.0"`; `folder-watch-actor` keeps WIT-only watch control contract metadata, and `watch-null-sink` now declares WIT-only ingress plus `patina:watch/events@0.1.0.emit` contract metadata so typed calls do not require legacy runtime lifecycle exports.',
    "Added explicit `cargo-component` target dependency mappings for WIT deps, including `wasi:io` directory deps for `folder-watch-actor`, so external `cargo component build` resolves without Patina monorepo assumptions.",
    "Validation passed in the external repo: `cargo fmt --all --check`, `cargo component build --release -p patina-ai-child-folder-watch-actor`, and `cargo component build --release -p patina-ai-child-watch-null-sink`.",
    "Installed both external artifacts locally with `patina child install children/folder-watch-actor --wasm target/wasm32-wasip1/release/patina_ai_child_folder_watch_actor.wasm --force` and `patina child install children/watch-null-sink --wasm target/wasm32-wasip1/release/patina_ai_child_watch_null_sink.wasm --force`.",
    """Local smoke passed: `patina child call folder-watch-actor 'patina:watch/control@0.1.0.status' '[]'` returned watcher stats, and `patina child call watch-null-sink 'patina:watch/events@0.1.0.emit' '[{...}]'` returned `{ "ok": null }` after logging the dropped event.""",
    "Patina monorepo child source directories remained untouched; Patina-side changes are limited to Slate/session evidence and events.",
]
blocked_by = []
implementation_plan = ["""
[x] Copy `children/folder-watch-actor` and `children/watch-null-sink` into the external watcher-system repo according to the bundle contract.
[x] Convert each child from local path `patina-sdk` dependency to a published SDK version.
[x] Normalize manifests and align child manifest versions with Cargo package versions.
[x] Tighten WIT dependency layout so external `cargo component build` resolves without monorepo assumptions.
[x] Build and test both children in the external repo.
[x] Install both children locally from the external repo and run smoke checks.
[x] Commit the external watcher-system repo changes separately from Patina core changes; push is not applicable yet because no external remote is configured.
"""]
belief_harvest_decision = "no new belief required; this Slate applied the already-captured external-child extraction pattern to the watcher bundle and recorded implementation evidence"
created_at = "1778870180"
updated_at = "1778872044"
closed_at = "1778872044"
