id = "watcher-child-bundle-contract"
title = "Define watcher child bundle contract"
kind = "build"
status = "active"
human_request = "Define the watcher system as a child bundle repo: one external repository that owns the core folder-watch actor, the watch-null-sink downstream, shared watch WIT contracts, release-unit rules, and room for future downstream children such as email or database sinks."
allium_anchors = ["layer/slate/work/watcher-child-bundle-contract/allium/watcher-child-bundle-contract.allium"]
user_alignment = "User wants watcher children to follow the Slate external-child model while treating folder-watch-actor and watch-null-sink as a bundle/ecosystem rather than unrelated children."
belief_refs = []
belief_harvest_decision = "no new belief required; this contract applies the existing external-child extraction pattern from Slate to the watcher bundle and records the design decision as Slate-local evidence"
proof_plan = ["""
[x] Bundle vocabulary is defined: child-bundle repo, release unit, child package, producer actor, downstream sink, shared WIT contract, and optional skills/config artifacts.
[x] Current watcher pair is classified: `folder-watch-actor` is the core producer/service actor; `watch-null-sink` is the reference/null downstream sink; future sinks are expected to be owned by the same watcher ecosystem unless proven otherwise.
[x] Target repo layout for `/Users/nicabar/Projects/Patina/patina-child-watcher-system` is documented, including multiple child crates, shared WIT, release workflow, README/RELEASING, and optional skills/config schema/default files.
[x] Release-unit strategy is chosen for two children in one repo, including version alignment, asset names, checksums, and how Mother should distinguish child artifacts.
[x] The contract explicitly says this Slate is design/contract only and does not remove in-tree Patina children.
"""]
closure_evidence = [
    "Added Slate-local Allium anchor `layer/slate/work/watcher-child-bundle-contract/allium/watcher-child-bundle-contract.allium` with generated plan `watcher-child-bundle-contract.plan.json`; `allium check` and `allium analyse` pass cleanly.",
    "Added `layer/slate/work/watcher-child-bundle-contract/watcher-child-bundle-contract.md` defining bundle vocabulary, current child classification, target repo layout, manifest/SDK rules, release-unit strategy, Mother registry implications, and non-goals.",
    "Selected per-child release tags as the v1 extraction strategy (`folder-watch-actor-v0.1.0`, `watch-null-sink-v0.1.0`) so standalone build/local install can proceed before the separate `mother-multi-child-bundle-registry` Slate implements richer bundle discovery.",
]
blocked_by = []
implementation_plan = ["""
[x] Review Slate extraction history and identify reusable proof gates: packageability, external repo, install/smoke, then monorepo removal.
[x] Identify watcher pair as a bundle/ecosystem rather than unrelated children.
[x] Inventory current `children/folder-watch-actor` and `children/watch-null-sink` manifests, WIT, SDK deps, installs, and relationship.
[x] Write a watcher bundle contract artifact under this Slate describing repo layout, release units, shared WIT, downstream extension model, and non-goals.
[x] Decide whether v1 uses per-child release tags, per-child asset selectors, or a bundle manifest for Mother registry discovery.
[ ] Update parent cleanup Slate evidence with the watcher bundle contract decision.
"""]
created_at = "1778870179"
updated_at = "1778870548"
