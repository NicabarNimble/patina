id = "watcher-monorepo-retirement"
title = "Retire in-tree watcher children"
kind = "refactor"
status = "complete"
human_request = "After external watcher-system build, install, and registry proof succeeds, remove the in-tree folder-watch-actor and watch-null-sink sources from Patina core and update workspace/docs/tests to use the external child bundle as source of truth."
allium_anchors = []
user_alignment = "User wants to shrink Patina core by moving child behavior out like Slate, but only after proof gates show the external watcher bundle works. No behavior change is intended: watcher behavior remains available through the external GitHub releases already installed and smoke-tested."
belief_refs = []
proof_plan = ["""
[x] `children/folder-watch-actor` and `children/watch-null-sink` are no longer tracked in-tree source-of-truth children in the Patina monorepo.
[x] Root workspace metadata, lockfile, tests, docs, and fixtures no longer require those in-tree crates.
[x] External watcher-system repo is documented as the source of truth and is clean/pushed at the removal point.
[x] Installed external watcher children still load and pass relevant smoke checks after in-tree removal.
[x] Patina core keeps only generic Mother/SDK support needed to host watcher children, not watcher-specific domain behavior.
"""]
closure_evidence = [
    "Confirmed external watcher GitHub release/install proof was already complete via archived `watcher-github-release-install-proof`; `folder-watch-actor@0.1.0` and `watch-null-sink@0.1.0` were discovered, approved, installed, assigned, and smoke-tested from GitHub release assets.",
    "Removed in-tree source directories `children/folder-watch-actor` and `children/watch-null-sink` plus their in-tree WIT deps and standalone `Cargo.lock` in [[commit-39f01ff5]].",
    "Removed watcher-specific WASM integration tests that depended on in-tree watcher WIT/source paths, and replaced remaining generic test examples with fixture names so Patina core no longer carries watcher-domain test fixtures.",
    "Updated `children/README.md` to document `/Users/nicabar/Projects/Patina/patina-child-watcher-system` and `https://github.com/NicabarNimble/patina-child-watcher-system` as the watcher source of truth.",
    "External watcher repo remained clean and pushed at `526dbf1` with tags `folder-watch-actor-v0.1.0` and `watch-null-sink-v0.1.0`.",
    "Validation passed after removal: `cargo fmt --all --check`, `cargo check`, `cargo test -p mother --lib -- --nocapture`, `cargo test --test wasm_integration -- --nocapture`, and installed external-child typed-call smoke checks for both watcher children.",
]
blocked_by = []
implementation_plan = ["""
[x] Confirm external watcher-system build/install/registry proof is complete before touching monorepo sources.
[x] Remove in-tree watcher child crates and generated artifacts from the Patina workspace.
[x] Refactor or remove tests that directly depend on in-tree watcher crates, replacing with fixtures or installed-child smoke where appropriate.
[x] Update docs such as `children/README.md` to point watcher development at `/Users/nicabar/Projects/Patina/patina-child-watcher-system`.
[x] Run cargo checks/tests and installed external-child smoke checks.
[x] Archive this Slate only after the monorepo is clean and watcher behavior is still available externally.
"""]
belief_harvest_decision = "no new belief required; this Slate applies the existing external-child extraction principle and records removal evidence"
created_at = "1778870182"
updated_at = "1778876372"
closed_at = "1778876372"
