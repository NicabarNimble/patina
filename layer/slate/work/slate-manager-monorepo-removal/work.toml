id = "slate-manager-monorepo-removal"
title = "Remove in-tree Slate manager"
kind = "refactor"
status = "complete"
human_request = "Archive/remove the in-tree Slate manager so Patina works with Slate from /Users/nicabar/Projects/Patina/patina-child-slate as the source of truth."
allium_anchors = []
user_alignment = "User wants the monorepo to be good with Slate gone from children/slate-manager and future work to happen in the external repo location."
belief_refs = []
proof_plan = [
    "[x] `children/slate-manager` is no longer a tracked in-tree child source in the Patina monorepo.",
    "[x] Workspace metadata and dev-dependencies no longer require `patina-ai-child-slate-manager` from `children/slate-manager`.",
    "[x] Tests that depended on the in-tree Slate manager are removed or refactored without weakening runtime fail-closed coverage.",
    "[x] `cargo check -q` and relevant tests pass after removal.",
    "[x] `patina slate show/check` still works using the installed external Slate child from `/Users/nicabar/Projects/Patina/patina-child-slate`.",
    "[x] Documentation points to the external Slate repo as the working location.",
]
closure_evidence = [
    "Removed tracked `children/slate-manager` files from the Patina monorepo and deleted the remaining generated `children/slate-manager/src/bindings.rs`; `find children -maxdepth 2 -type d -name slate-manager` returns no in-tree directory.",
    "Removed `children/slate-manager` from workspace members and removed the `patina-ai-child-slate-manager` dev-dependency from root `Cargo.toml`; `Cargo.lock` no longer contains `patina-ai-child-slate-manager`.",
    "Removed direct test dependency on `patina_ai_child_slate_manager`; retained Mother dispatch fail-closed and routing tests using local test doubles. Verified `cargo test -q -p patina-ai commands::mother::daemon::tests::builtin_spec_dispatch` passes.",
    "Verified `cargo check -q` passes after Slate manager removal. `cargo test -q --workspace` still fails on pre-existing WIT cdylib link issues in other child/adapter crates, not on Slate manager removal.",
    "Verified `cargo run -q -- slate show slate-manager-monorepo-removal` and `cargo run -q -- slate check slate-manager-monorepo-removal` still work via the installed external child package from `/Users/nicabar/Projects/Patina/patina-child-slate`.",
    "Updated `children/README.md` and `layer/slate/README.md` to point Slate manager work at `/Users/nicabar/Projects/Patina/patina-child-slate` / `https://github.com/NicabarNimble/patina-child-slate`. External repo `main` is clean and tracks `origin/main`.",
    "Updated `layer/slate/child-skill-discovery.md` to show the external child package layout and current `patina mother skills ...` surface instead of the removed `children/slate-manager` path.",
]
blocked_by = []
implementation_plan = ["Remove `children/slate-manager` from the Patina workspace as an in-tree source of truth; adjust workspace/dev-dependencies/tests/docs that assumed the in-tree crate; rely on the installed external child package for `patina slate ...`; verify cargo checks/tests and Slate commands still work from the external install."]
belief_harvest_decision = "no new belief required; this operationalizes contract-first execution and explicit fail-closed behavior by making Slate an installed external child"
created_at = "1778606794"
updated_at = "1778607501"
closed_at = "1778607501"
