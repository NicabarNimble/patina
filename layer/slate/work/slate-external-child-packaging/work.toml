id = "slate-external-child-packaging"
title = "External Slate child packaging"
kind = "build"
status = "active"
human_request = "Make Slate extractable as an app-like WIT/WASI Patina child that can live outside the Patina monorepo: package patina-sdk, stabilize WIT toy contracts, fix cargo component build, add first-class child packaging/install, and expose Mother child skills/help."
allium_anchors = ["layer/allium/mother/mother-child-toy-orchestration.allium"]
user_alignment = "User wants app-like WIT/WASI Patina children that can be built as standalone projects outside the Patina monorepo, with Slate as the first concrete extraction target."
belief_refs = [
    "[[contract-first-execution]]",
    "[[mother-brokers-child-skills]]",
    "[[read-code-before-write-code]]",
    "[[scalpel-commits]]",
]
proof_plan = [
    "[x] `patina-sdk` is consumable by an external child crate without `path = ../../sdk/patina-sdk`; v0.22.0 is published on crates.io.",
    "[x] Stable WIT toy contracts required by Slate are present in the external child package and declared as cargo-component target dependencies.",
    "[x] External Slate child build works through `cargo component build -p patina-ai-child-slate-manager --release`, including the previously failing `wasi:logging@0.1.0` dependency story.",
    "[x] First-class `patina child install` command installs Slate child `.wasm`, `child.toml`, skills, and WIT metadata into Patina/Mother without manual copy steps.",
    "[x] `patina mother skills show slate-manager` lists Slate child skill packages and `patina mother skills help slate-manager slate-code` renders command/help surface.",
    "[x] A standalone Slate child repo/package smoke test can create/show/check work through `patina slate ...` after installation.",
]
closure_evidence = [
    "Bumped local `sdk/patina-sdk` to 0.22.0 and included `TOY_GOVERNANCE.md` in the published crate package. Commit: [[commit-ddba7f85]].",
    "Validated SDK package locally: `cargo check -q -p patina-sdk`, `cargo package -p patina-sdk --allow-dirty --list` included `TOY_GOVERNANCE.md`, and clean `cargo publish -p patina-sdk --dry-run` verified v0.22.0.",
    "Attempted real `cargo publish -p patina-sdk`; blocked locally by missing crates.io token: `no token found, please run cargo login or use CARGO_REGISTRY_TOKEN`.",
    'Published `patina-sdk` v0.22.0 to crates.io; verified with `cargo search patina-sdk --limit 1` showing `patina-sdk = "0.22.0"`.',
    'Updated `children/slate-manager` to depend on published `patina-sdk = "0.22.0"` instead of a monorepo path dependency; `cargo tree -p patina-ai-child-slate-manager --depth 2` shows crates.io `patina-sdk v0.22.0`.',
    "Fixed the `cargo component build -p patina-ai-child-slate-manager --release` WIT dependency resolution by declaring `[package.metadata.component.target.dependencies]` for `wasi:logging`, `patina:git`, `patina:measure`, and `patina:slate`, and arranging local WIT deps in package directories.",
    "Added `patina child install <path> --wasm <component> --force`, which installs `.wasm`, `child.toml`, `skills/`, `wit/`, and `wit-contract/` into `~/.patina/plugins/<child>/` while preserving existing local filesystem scope additions by default.",
    "Live-smoked `cargo run -q -- child install children/slate-manager --wasm target/wasm32-wasip1/release/patina_ai_child_slate_manager.wasm --force`; install preserved local `/project` read-write mount and `cargo run -q -- slate show slate-external-child-packaging` succeeded through the installed child.",
    "Added `patina mother skills list/show/help` to discover installed child skill packages from `~/.patina/plugins/<child>/skills`; live smoke shows `patina mother skills show slate-manager` lists `slate-code` and `slate-version-control`.",
    "Standalone smoke: copied `children/slate-manager` to `/tmp/patina-child-slate-standalone.*`, built there with `cargo component build --release` against crates.io `patina-sdk v0.22.0`, installed with `patina child install`, then verified `patina slate create`, `patina slate show`, and `patina slate check` worked through the installed standalone-built child.",
]
blocked_by = []
target = "Standalone app-like WIT/WASI Slate child outside Patina monorepo"
implementation_plan = [
    "Package `sdk/patina-sdk` so external child repos can depend on it by version instead of monorepo path dependency.",
    "Define stable WIT toy contract distribution for child repos: copy/publish/version `patina:git`, `patina:measure`, logging/keyvalue compatibility, filesystem guidance, and child runtime imports.",
    "Fix the `cargo component build -p patina-ai-child-slate-manager --release` path or document/replace it with a supported external-child build command.",
    "Add first-class child packaging/install flow so a standalone Slate child repo can produce a `.wasm`, `child.toml`, WIT package, skills, and installable bundle.",
    "Expose Mother child skill/help discovery so installed children can advertise skills and commands through `patina mother skills ...`.",
    "Prove Slate can be extracted by creating or simulating an external Slate child package that builds and installs without relying on monorepo-relative paths.",
]
created_at = "1778600907"
updated_at = "1778605042"
