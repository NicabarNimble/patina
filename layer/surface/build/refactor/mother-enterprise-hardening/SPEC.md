---
type: refactor
id: mother-enterprise-hardening
status: draft
created: 2026-03-30
sessions:
  origin: 20260330-083255-177610000
beliefs:
  - "[[four-roles-no-overlap]]"
  - "[[mother-is-the-daemon]]"
  - "[[observation-at-the-boundary]]"
  - "[[children-have-agency-toys-are-capabilities]]"
related:
  - mother/src/registry.rs
  - mother/src/state.rs
  - mother/src/http_daemon.rs
  - mother/src/daemon_runner.rs
  - mother/src/daemon_heartbeat.rs
  - mother/src/daemon_lifecycle.rs
  - mother/src/microserver.rs
  - mother/src/secrets_authority_backend/vault.rs
  - layer/surface/build/refactor/greenfield-mother-patina-data-platform/
exit_criteria:
  - id: meh1-failing-tests-fixed
    text: "All mother crate tests pass. The 3 failing tests fixed: state_checkpoints_and_offsets and task_dedupe_and_leasing (project_uid scoping), tcp_http_returns_error_when_port_in_use (port binding). cargo test -q -p mother passes with zero failures."
    checked: true
  - id: meh2-panic-recovery
    text: "Request handler threads use std::panic::catch_unwind. A panicking handler logs the panic and returns 500 Internal Server Error instead of killing the thread silently. Proven with a test that triggers a panic in a handler and verifies the accept loop continues serving."
    checked: true
  - id: meh3-bounded-thread-pool
    text: "Accept loops use a bounded thread pool (not unbounded thread::spawn per connection). Pool size configurable via DaemonBootstrapConfig, default 16. Connections exceeding pool capacity receive 503 Service Unavailable."
    checked: true
  - id: meh4-structured-logging
    text: "Mother uses tracing with a JSON file subscriber. Log output to ~/.patina/mother/logs/mother.jsonl. Log levels: INFO for lifecycle events, WARN for degradation, ERROR for failures. All eprintln! calls in mother/ replaced with tracing macros."
    checked: true
  - id: meh5-wal-checkpoint-scheduling
    text: "Heartbeat thread periodically checkpoints project SQLite databases (PRAGMA wal_checkpoint(PASSIVE)). Interval configurable, default every 5 minutes. WAL files for active projects stay bounded."
    checked: true
  - id: meh6-graceful-shutdown
    text: "SIGINT/SIGTERM triggers a drain period (default 5s) where in-flight requests complete but new connections are refused. After drain, checkpoint all databases, clean up PID + socket files and exit. Heartbeat thread stops cleanly."
    checked: true
  - id: meh7-health-endpoint-deep
    text: "/health returns structured JSON: daemon uptime, child count, child health per child, registered project count, database sizes for active project. Not just 'ok'."
    checked: true
  - id: meh8-supervisor-integration
    text: "launchd plist generated by patina mother install. Mother auto-starts on login, auto-restarts on crash (KeepAlive=true). patina mother uninstall removes the plist. Linux systemd unit tracked but not built here."
    checked: false
  - id: meh9-stale-pid-detection
    text: "On startup, if PID file exists, check if process is alive. If stale, clean up PID + socket and start. If alive, refuse to start with clear error. Prevents address-in-use confusion."
    checked: false
  - id: meh10-constant-time-token-compare
    text: "Bearer token comparison uses constant-time equality. Eliminates timing side channel on auth."
    checked: false
  - id: meh11-expect-audit
    text: "Production expect() calls in vault.rs and encrypted_file.rs replaced with proper Result propagation. Mother production paths have zero panic-capable calls outside startup preconditions."
    checked: false
  - id: meh12-workspace-compiles-tests-pass
    text: "cargo check --workspace -q. cargo test -q --lib. cargo test -q -p mother. patina mother start + /health returns deep health JSON."
    checked: false
---
# refactor: Mother Enterprise Hardening

> Make Mother lean, stable, and secure. No new features — harden what exists. Fix broken tests, add panic recovery, bound resources, structure logging, schedule WAL checkpoints, enable supervisor integration, and eliminate timing side channels. Gjengset's principle: library code never panics on bad input; errors propagate, callers decide.

## Problem

Mother is a working prototype daemon. The architecture is sound — clean separation of concerns, bearer token auth, signal handling, small HTTP surface (7 routes), custom microserver with input validation. But operational maturity gaps exist:

1. **3 tests failing** — 2 from the runtime split (state_checkpoints_and_offsets, task_dedupe_and_leasing need project_uid scoping), 1 pre-existing (tcp_http_returns_error_when_port_in_use).

2. **Unbounded thread spawn** per connection in accept loops. No backpressure, no capacity limit.

3. **No panic recovery** on request threads. A panicking handler dies silently — no logging, no 500 response, no visibility.

4. **All logging is eprintln!** — unstructured, no levels, no rotation, no file output. In supervisor mode, stderr goes nowhere useful.

5. **No WAL checkpoint scheduling.** SQLite WAL files grow unbounded between connections.

6. **No graceful shutdown.** Signal handlers clean up PID + socket but don't drain in-flight requests.

7. **No supervisor integration.** Mother requires manual `patina mother start`. No auto-start, no auto-restart.

8. **Timing side channel on auth.** Bearer token comparison uses `==` (short-circuit).

9. **Two production expect() calls** that should propagate errors.

## What's already good (do NOT touch)

The audit found Mother's foundation is stronger than expected:

- **Zero production unwrap()** — all unwrap() calls are in `#[cfg(test)]` blocks
- **Microserver is solid** — 150 lines, httparse-based, rejects chunked, enforces Content-Length, caps headers at 32KB and body at 1MB, 7 tests
- **Security headers** — X-Content-Type-Options, X-Frame-Options on all responses
- **Auth on all sensitive routes** — /health and /version public; everything else requires bearer token
- **Signal handling** — SIGINT/SIGTERM registered, PID + socket cleanup
- **Connection: close** — no keep-alive, one request per connection
- **Body size enforcement** — 413 if body exceeds 1MB
- **Thread-per-connection isolation** — bad request can't corrupt other connections
- **Only 2 production expect()** — startup preconditions (token write, thread spawn), plus 2 that need fixing

Do NOT refactor the microserver, HTTP types, route structure, or request parsing. They're lean and correct.

## Core Value Anchors

- `dependable-rust`: Small, stable external interface. Mother's 7 routes don't change. All hardening is internal — callers see no change except better error responses and richer health data.
- `unix-philosophy`: One daemon, one job. Mother coordinates children and manages project state. She doesn't grow features; she gets reliable.
- `patina-identity`: Mother is protocol infrastructure. Infrastructure must be dependable. The protocol depends on Mother staying up.
- `observation-at-the-boundary`: Mother observes herself — uptime, request handling, child health, database sizes. The health endpoint is the self-observation boundary.

## Gjengset Principles Applied

1. **Errors propagate, callers decide.** Every runtime `expect()` becomes `?` or `.map_err()`. Startup preconditions may panic — that's fail-fast, not error handling.

2. **Panic is not error handling.** `catch_unwind` on request threads is a safety net, not a design pattern. The real fix is ensuring handlers return Result. The safety net is defense in depth.

3. **Types prevent misuse.** `ProjectUid` validates at construction. Thread pool capacity is typed config, not a magic number. Log levels are tracing spans, not format strings.

4. **Resources are bounded.** Thread pool has a ceiling. WAL files get checkpointed. Shutdown has a drain window. Nothing grows unbounded.

5. **The API surface is honest.** `/health` returns real data, not "ok." Errors return structured JSON, not empty 500s.

## Non-Goals

- Adding new HTTP routes or daemon features
- Changing the WASM child runtime or KnowledgeChild trait
- Building multi-Mother federation or cross-project DuckDB queries
- Changing the auth model (bearer token is appropriate for local daemon)
- Adding TLS (reverse proxy concern, not daemon concern)
- Rate limiting (single user, local machine)
- Changing the microserver or request parsing (already correct)
- Database connection pooling (SQLite connections are cheap)
- Linux systemd unit (tracked for future, not this spec)

## Current State

### Mother surface

7 HTTP routes over UDS or TCP:

| Method | Path | Auth | Purpose |
|--------|------|------|---------|
| GET | /health | No | Health check |
| GET | /version | No | Version info |
| POST | /api/scry | Yes | Cross-project search |
| GET | /secrets/cache | Yes | Read cached secret |
| POST | /secrets/cache | Yes | Cache a secret |
| POST | /secrets/lock | Yes | Lock secret session |
| * | /child/{name}/* | Yes | Route to WASM child |

### Module inventory (11K lines, 24 files)

| Module | Lines | Concern | Touch in this spec? |
|--------|-------|---------|-------------------|
| state.rs | 1432 | Runtime store, sessions, project registry | G1 (fix tests) |
| registry.rs | 487 | Child registry, handle routing, observation | — |
| http_api.rs | 293 | Route handler implementations | G7 (health) |
| secrets_authority_api.rs | 281 | Secrets API | — |
| microserver.rs | 270 | HTTP parser | **NO** |
| bootstrap_config.rs | 201 | Daemon config | G3 (pool size), G8 (install) |
| http_daemon.rs | 138 | Accept loops | G2 (catch_unwind), G3 (pool), G6 (drain) |
| lifecycle.rs | 131 | PID file, signals | G6 (drain), G9 (stale PID) |
| runtime.rs | 126 | KnowledgeChild trait | — |
| protocol.rs | 123 | Typed contracts | — |
| events.rs | 113 | Event stream support | — |
| http_routes.rs | 83 | Router dispatch | G10 (constant-time token) |
| daemon_bootstrap.rs | 81 | Child loading | — |
| daemon_runner.rs | 80 | Server launch | G3 (pool integration) |
| eventlog_schema.rs | 79 | Shared events schema | — |
| daemon_heartbeat.rs | 25 | Heartbeat thread | G5 (WAL checkpoints), G6 (shutdown) |
| vault.rs | — | Secrets vault | G11 (expect audit) |
| encrypted_file.rs | — | Encrypted storage | G11 (expect audit) |

### Test inventory (35 total)

- microserver: 7 (all pass — parser is solid)
- state: 10 (2 fail — runtime split)
- registry: 4 (all pass)
- bootstrap_config: 5 (all pass)
- broker: 7 (all pass)
- lifecycle, protocol, events: 2 each (all pass)

## Execution Contract

1. **Scalpel over shotgun.** Each gate changes one concern.
2. **Read before write.** Inspect the exact code path before changing it.
3. **Test before marking.** Gate exit proofs pass before checking the criterion.
4. **No new features.** If a gate reveals a missing capability, track it separately.
5. **No Co-Authored-By.** (per CLAUDE.md)
6. **Atomic commits.** One per gate, referencing gate ID.

## Solution — Phase Gates

### MEH-G1: Fix failing tests

Fix 3 failing tests:
- `state_checkpoints_and_offsets` and `task_dedupe_and_leasing`: update to use `new_with_project()`. The reopen path at state.rs:1177 uses `new()` after writing with `new_with_project()` — must use consistent project-scoped access. Possible test-state bleed from shared runtime DB path under temp parent with fixed project uid.
- `tcp_http_returns_error_when_port_in_use`: diagnose port binding issue in daemon_bootstrap_config.rs:82.

- **Entry**: Read all 3 failing tests. Run each in isolation to distinguish test-state bleed from real bugs.
- **Exit proof**: `cargo test -q -p mother` — zero failures.

### MEH-G2: Panic recovery on request threads

Wrap handler invocation in `std::panic::catch_unwind` in accept loops. On panic: log payload, return 500 JSON error. Accept loop continues.

- **Entry**: MEH-G1 passes. Read `http_daemon.rs:100-138`.
- **Exit proof**: Test with panicking handler → 500 response → next request served. `cargo test -q -p mother`.

### MEH-G3: Bounded thread pool

Replace unbounded `thread::spawn` with bounded pool. Semaphore-guarded spawn or channel-based worker pool. Size from config, default 16. Excess connections get 503.

- **Entry**: MEH-G2 passes. Read `daemon_runner.rs`, `daemon_bootstrap_config.rs`.
- **Exit proof**: Config accepts pool size. pool+1 connections → last gets 503. `cargo test -q -p mother`.

### MEH-G4: Structured logging

Add `tracing` and `tracing-subscriber` to mother/Cargo.toml (not currently listed). Replace `eprintln!` with `tracing` macros. JSON file subscriber to `~/.patina/mother/logs/mother.jsonl`. Initialize in daemon bootstrap.

- **Entry**: MEH-G3 passes. Count `eprintln!` in mother/. Add tracing dependencies.
- **Exit proof**: `rg "eprintln!" mother/src/ --type rust` zero in non-test code. JSON log file written. `cargo test -q -p mother`.

### MEH-G5: WAL checkpoint scheduling

Heartbeat thread checkpoints project SQLite databases every N heartbeats (default 5 = 5min). `PRAGMA wal_checkpoint(PASSIVE)` on each project's databases.

- **Entry**: MEH-G4 passes. Read `daemon_heartbeat.rs`, `paths::mother::projects`.
- **Exit proof**: Test creates WAL-dirty databases, runs checkpoint, WAL bounded. `cargo test -q -p mother`.

### MEH-G6: Graceful shutdown with drain

SIGINT/SIGTERM sets `AtomicBool`. Accept loop refuses new connections. Drain window (5s) for in-flight requests. Then checkpoint databases, cleanup, exit. Heartbeat thread stops.

- **Entry**: MEH-G5 passes. Read `daemon_lifecycle.rs`, accept loops.
- **Exit proof**: SIGTERM during request → response completes → clean exit. `cargo test -q -p mother`.

### MEH-G7: Deep health endpoint

Expand existing `/health` response (currently returns `status`, `version`, `uptime_secs`, `children` vector). Add: registered project count, per-project database sizes, pool capacity/active connections, last checkpoint timestamp. CLI lifecycle.rs:14 parses the health response — either maintain backwards compatibility (additive fields only) or update CLI parser simultaneously.

- **Entry**: MEH-G6 passes. Read `http_api.rs:53,97` health handler and `lifecycle.rs:14` CLI parser.
- **Exit proof**: `/health` returns all new fields alongside existing ones. CLI `patina mother status` still works. Schema shape test. `cargo test -q -p mother`.

### MEH-G8: Supervisor integration (macOS launchd)

`patina mother install` generates launchd plist at `~/Library/LaunchAgents/com.patina.mother.plist`. KeepAlive=true, RunAtLoad=true. `patina mother uninstall` removes it.

- **Entry**: MEH-G7 passes. Read `src/commands/mother/`.
- **Exit proof**: Install creates plist. `launchctl list | grep patina` shows service. Uninstall removes it. Manual verification.

### MEH-G9: Stale PID detection

On startup: if PID file exists, check process alive via `kill(pid, 0)`. Alive → refuse start. Dead → clean up, proceed. Log stale cleanup.

- **Entry**: MEH-G8 passes. Read `daemon_lifecycle.rs`.
- **Exit proof**: Stale PID file → startup succeeds. Live PID file → startup refuses. `cargo test -q -p mother`.

### MEH-G10: Constant-time token compare

Replace `==` in `check_auth` (http_routes.rs:77) with constant-time comparison. `subtle` is a transitive dep of `age` but NOT in mother/Cargo.toml — add it explicitly. Use `subtle::ConstantTimeEq`.

- **Entry**: MEH-G9 passes. Add `subtle` to mother/Cargo.toml. Read `http_routes.rs:77-82`.
- **Exit proof**: Token comparison is constant-time. Auth tests pass. `cargo test -q -p mother`.

### MEH-G11: Production expect() audit

`vault.rs:158` → `anyhow::bail!`. `encrypted_file.rs:305` → `.ok_or_else()`. Verify remaining expect() calls are startup-only.

- **Entry**: MEH-G10 passes. Read both call sites.
- **Exit proof**: `rg "\.expect\(" mother/src/ --type rust` non-test hits are only daemon_runner (startup) and daemon_heartbeat (thread spawn). `cargo test -q -p mother`.

### MEH-G12: Final verification

- **Entry**: All previous gates pass.
- **Exit proof**: `cargo check --workspace -q`. `cargo test -q --lib`. `cargo test -q -p mother`. `patina mother start`. `/health` returns deep JSON. `patina mother stop` clean exit. Verify log file written.

## Implementation Decisions (concrete)

These resolve ambiguities flagged by audit. Build agents: follow these exactly.

### Thread pool mechanism (MEH-G3)

std has no native semaphore. Use `Arc<(Mutex<usize>, Condvar)>` — a counting semaphore built from std primitives. No new dependency.

```rust
struct ConnectionPool {
    max: usize,
    active: Mutex<usize>,
    slot_available: Condvar,
}

impl ConnectionPool {
    fn try_acquire(&self) -> bool {
        let mut active = self.active.lock().unwrap();
        if *active < self.max {
            *active += 1;
            true
        } else {
            false
        }
    }
    fn release(&self) {
        let mut active = self.active.lock().unwrap();
        *active -= 1;
        self.slot_available.notify_one();
    }
}
```

Accept loop: `if !pool.try_acquire() { write 503; continue; }`. Handler thread calls `pool.release()` on drop (use a guard struct). No crossbeam, no rayon, no async. Pure std.

### tracing init (MEH-G4)

Single global subscriber, set once in daemon bootstrap before any logging:

```rust
fn init_logging() -> Result<()> {
    let log_dir = paths::mother::logs_dir();
    std::fs::create_dir_all(&log_dir)?;
    let log_file = std::fs::OpenOptions::new()
        .create(true).append(true)
        .open(log_dir.join("mother.jsonl"))?;
    let subscriber = tracing_subscriber::fmt()
        .json()
        .with_writer(log_file)
        .with_max_level(tracing::Level::INFO)
        .finish();
    tracing::subscriber::set_global_default(subscriber)
        .map_err(|e| anyhow::anyhow!("failed to set tracing subscriber: {}", e))?;
    Ok(())
}
```

**Test safety**: `set_global_default` panics if called twice. Tests must NOT call it. Mother tests that log should either:
- Use `tracing::subscriber::with_default()` (per-test, thread-local)
- Or skip logging setup entirely (tests use stderr by default)

The daemon is the only code path that calls `init_logging()`.

### Signal handling contract (MEH-G6)

Signal handlers must be minimal (async-signal-safe). The current handler does file I/O in the signal handler — that's technically unsafe. Fix:

```
Signal handler (async-signal-safe):
  └── Set AtomicBool SHUTDOWN_REQUESTED = true
      (that's ALL the handler does — atomic store is signal-safe)

Main accept loop (checks flag):
  └── if SHUTDOWN_REQUESTED.load(Relaxed) { break; }

Coordinated shutdown (runs after accept loop breaks):
  1. Log "draining in-flight requests"
  2. Wait for connection pool to drain (pool.active == 0, with 5s timeout)
  3. Checkpoint all project databases (PRAGMA wal_checkpoint(TRUNCATE))
  4. Stop heartbeat thread (set separate AtomicBool, join thread)
  5. Remove PID file
  6. Remove socket file
  7. Log "shutdown complete"
  8. exit(0)
```

PID + socket cleanup moves OUT of the signal handler and INTO the coordinated shutdown path. Signal handler only sets a flag. This is the correct POSIX pattern.

### /health compatibility policy (MEH-G7)

Current `HealthInfo` (parsed by lifecycle.rs:14):

```rust
pub struct HealthInfo {
    pub version: String,
    pub uptime_secs: u64,
    #[serde(default)]
    pub children: Vec<ChildHealthInfo>,
}
```

**Policy: additive only.** New fields are added alongside existing ones. Existing fields (`version`, `uptime_secs`, `children`) never change shape or are removed. The `#[serde(default)]` pattern means the CLI parser ignores unknown fields — new fields don't break old CLI versions.

New fields added by MEH-G7:

```json
{
  "version": "0.43.17",
  "uptime_secs": 3621,
  "children": [...],
  "registered_projects": 2,
  "project_databases": {
    "2bdc808e": {
      "events_db_bytes": 11534336,
      "patina_db_bytes": 206569472,
      "runtime_db_bytes": 54272
    }
  },
  "state_db_bytes": 3276800,
  "pool": { "max": 16, "active": 2 },
  "last_checkpoint_at": "2026-03-30T20:45:00Z"
}
```

CLI `lifecycle.rs` HealthInfo struct gets `#[serde(default)]` on new fields so old responses still parse. No breaking change.

## Verification

```bash
patina spec check mother-enterprise-hardening --json
cargo check --workspace -q
cargo test -q --lib
cargo test -q -p mother
```

## Build Readiness

Mother is 11K lines across 24 modules. The surface is small and correct. The internals need operational maturity — not more features. 12 gates, each targeting one concern. No gate changes the external interface. Same pattern as greenfield-mother-clean-continued (12 gates) and greenfield-mother-patina-data-platform (12 gates).
