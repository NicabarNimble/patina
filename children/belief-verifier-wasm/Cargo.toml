[package]
name = "patina-plugin-belief-verifier"
version = "0.1.0"
edition = "2021"
description = "Belief verification knowledge child for Mother"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
patina-sdk = { path = "../../sdk/patina-sdk", features = ["knowledge-child"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
