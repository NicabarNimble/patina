[schema]
name = "github"
version = "1.0.0"
package = "patina:schema/github@1.0.0"
description = "GitHub issues and pull requests (native connector)"

[[facts]]
name = "issue"
event_type = "github.issue"
record = "issue"
identity_fields = ["number"]

[[facts]]
name = "pull-request"
event_type = "github.pr"
record = "pull-request"
identity_fields = ["number"]

[embedding]
offset_slot = 5
corpus_query = """
SELECT seq,
       json_extract(data, '$.title') || ' ' ||
       COALESCE(json_extract(data, '$.body'), '')
       as content
FROM eventlog
WHERE event_type LIKE 'github.%'
"""

[[indexes]]
fact = "issue"
fts_fields = ["title", "body"]
table = "github_issues"

[[indexes]]
fact = "pull-request"
fts_fields = ["title", "body", "comments_text"]
table = "github_prs"

[[projections]]
fact = "issue"
table = "github_issues"
primary_key = "number"
columns = [
    { name = "number", type = "INTEGER", json_path = "$.number" },
    { name = "title", type = "TEXT", json_path = "$.title" },
    { name = "body", type = "TEXT", json_path = "$.body" },
    { name = "state", type = "TEXT", json_path = "$.state" },
    { name = "labels", type = "TEXT", json_path = "$.labels" },
    { name = "author", type = "TEXT", json_path = "$.author" },
    { name = "updated_at", type = "TEXT", json_path = "$.updated_at" },
    { name = "url", type = "TEXT", json_path = "$.url" },
]

[[projections]]
fact = "pull-request"
table = "github_prs"
primary_key = "number"
columns = [
    { name = "number", type = "INTEGER", json_path = "$.number" },
    { name = "title", type = "TEXT", json_path = "$.title" },
    { name = "body", type = "TEXT", json_path = "$.body" },
    { name = "state", type = "TEXT", json_path = "$.state" },
    { name = "labels", type = "TEXT", json_path = "$.labels" },
    { name = "author", type = "TEXT", json_path = "$.author" },
    { name = "merged_at", type = "TEXT", json_path = "$.merged_at" },
    { name = "url", type = "TEXT", json_path = "$.url" },
    { name = "comments_text", type = "TEXT", json_path = "$.comments_text" },
    { name = "linked_issues", type = "TEXT", json_path = "$.linked_issues" },
    { name = "approvals", type = "INTEGER", json_path = "$.approvals" },
]

[[contracts]]
name = "issues"
event_type = "github.issue"
display_kind = "Issue"

[[contracts]]
name = "pull-requests"
event_type = "github.pr"
display_kind = "PR"

[lake]
path_template = "{owner}/{repo}"
