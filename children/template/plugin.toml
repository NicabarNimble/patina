[plugin]
name = "{{ package_name }}"
version = "0.1.0"
description = "{{ description }}"
world = "knowledge-child"
role = "app"
patina_min = "0.41.0"

[needs]
toys = ["log"]

[provides]
child = "{{ child_name }}"

[relationships]
emits = []
listens = []
