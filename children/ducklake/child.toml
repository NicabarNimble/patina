[child]
name = "ducklake"
version = "0.1.0"
type = "lakehouse"
runtime = "native"
lifecycle = "poll"
description = "Autonomous DuckDB + DuckLake data lake"

[capabilities]
methods = ["run"]
agency = true
