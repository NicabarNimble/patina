/target/
.cargo/
libduckdb/
libduckdb-*.zip
**/*.rs.bk

# Ignore vendored grammar Cargo.lock (we compile C sources directly, not via cargo)
patina-metal/grammars/**/Cargo.lock
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.iml
.claude/
.backup/

# Patina Dust
layer/dust/

# Module Go Artifacts
modules/*/server
modules/**/*.exe
modules/**/*.test
modules/*/vendor/

# Local development
docker-compose.yml.backup/

# Database files (should not be committed)
*.db
*.db-shm
*.db-wal

# Patina local data (regeneratable by patina scrape/oxidize)
.patina/local/

# Environment detection (auto-generated by patina init, machine-specific)
ENVIRONMENT.toml

# ONNX models (large binary files, download on-demand)
# Downloaded via scripts/download-active-model.sh
resources/models/*/
resources/models/*.onnx
resources/models/tokenizer.json
!resources/models/registry.toml
!resources/models/README.md

# Added by Patina for safety
node_modules/
.env
*.key
*.pem
.trunk/

# Project-specific patina files (local data only)
# Note: config.toml IS tracked - only [environment] section is machine-specific
