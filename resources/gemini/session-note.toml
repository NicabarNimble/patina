description = "Add a note to the current session"
prompt = """
Add a note to the current Patina session:

1. Execute the session note command:
   `patina session note "{{args}}"`

2. The note has been added to `.patina/local/active-session.md`

Notes are valuable for capturing insights, decisions, and observations during development.
"""
