version: '3.8'

services:
  {{.name}}:
    build: .
    image: {{.name}}:latest
    container_name: {{.name}}
    restart: unless-stopped
    environment:
      - RUST_LOG=info
    # Add your service-specific configuration here
    # ports:
    #   - "8080:8080"
    # volumes:
    #   - ./data:/app/data