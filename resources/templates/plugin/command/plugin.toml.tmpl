[plugin]
name = "__NAME__"
version = "0.1.0"
description = "Patina command plugin"
world = "command"
patina_min = "0.21.0"

[capabilities]
host_log = true
host_layer = true
# host_query = ["scry", "context", "assay"]

[provides]
commands = ["__NAME__"]
