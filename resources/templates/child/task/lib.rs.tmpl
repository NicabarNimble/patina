use patina_sdk::{register_task_child, TaskChild, Toy};

#[derive(Default)]
struct __NAME_STRUCT__;

impl TaskChild for __NAME_STRUCT__ {
    fn name(&self) -> String {
        "__NAME__".into()
    }

    fn description(&self) -> String {
        "A Patina task child".into()
    }

    fn run(&mut self, _args: &[String]) -> i32 {
        0
    }

    fn toys(&self) -> Vec<Toy> {
        vec![]
    }
}

register_task_child!(__NAME_STRUCT__);
