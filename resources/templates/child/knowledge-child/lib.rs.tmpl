use patina_sdk::knowledge_child::{ChildHealth, HealthStatus, KnowledgeChild};
use patina_sdk::register_knowledge_child;

#[derive(Default)]
struct __NAME_STRUCT__;

impl KnowledgeChild for __NAME_STRUCT__ {
    fn name(&self) -> String {
        "__NAME__".into()
    }

    fn handle(&mut self, _action: &str, _payload: &str) -> Result<String, String> {
        Ok("{}".into())
    }

    fn health(&self) -> ChildHealth {
        ChildHealth {
            status: HealthStatus::Healthy,
            reason: None,
        }
    }
}

register_knowledge_child!(__NAME_STRUCT__);
