wit_bindgen::generate!({
    path: "wit",
    world: "mct-child",
    generate_all,
});

struct __NAME_STRUCT__;

impl exports::patina::mct::child::Guest for __NAME_STRUCT__ {
    fn echo(input: String) -> Result<String, String> {
        wasi::logging::logging::log(
            wasi::logging::logging::Level::Info,
            "__NAME__",
            "echo invoked",
        );
        patina::measure::measure::counter("echo_calls", 1.0)?;
        Ok(input)
    }
}

export!(__NAME_STRUCT__);
